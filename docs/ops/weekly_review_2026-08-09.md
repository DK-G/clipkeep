# Weekly Review Record #009 — ClipKeep Growth

日付: 2026-08-09（日／週次スロット）
対象期間: 直近28日（GA4/GSC as of 2026-08-09, snapshot `growth-2026-08-08T20-43-03-182Z.json`）
比較対象: 前回 週次レビュー #008（2026-08-02・**7日前**＝正常間隔を2週維持）
担当: 自動週次レビュー（無人実行）
正本: `docs/strategy/growth-strategy.md`（北極星: Monetag タグロード数/日 = GA4 `ad_script_load`）

> 注: Launch-Phase（Phase L）運用中。先行指標は **GSC impressions / URL Inspection「Google 未発見」比率 / インデックス数 / Google オーガニックセッション**。CVR・ファネル微調整は凍結（AGENTS.md / growth-strategy.md §2）。実数のみ記載・**TBD 禁止**（取得できない値は「取得不能: 理由」を明記）。
>
> **#007 で確定した読み方を踏襲**: `ad_script_load` とセッションは Phase L の判定指標から外す（§1.1 で本週も追認）。

---

## ✅ 0. 計測健全性（実装前ゲート）

**`npm run growth:review`: 成功（GA4・GSC・URL Inspection の3系統とも SA で fresh 取得）。**

- 認証: 🔐 ANALYTICS AUTH = **OK**。SA 化（06-28）以降 **6 週連続で無人失効なし**。
- スナップショット: `docs/analytics/history/growth-2026-08-08T20-43-03-182Z.json`（28日窓 as of 2026-08-09 JST）。
- **本週の計測系の変更（記録）**: 08-05 に SA キーが `D:\dev\repos\clipkeep\.secrets\` から **`D:\secrets\clipkeep\`（リポジトリ外の保管庫）へ退避**され、リポジトリ側は gitignore 済み `.env.analytics.local` がパスを指す構成に変わった（`D:\dev\docs\SAFETY_CHECKLIST.md` Red Lines＝`D:\dev` 配下に秘密を置かない、への準拠）。**本日の取得が成功したこと自体が、新経路が無人で機能している実証**である。この整備の docs 4ファイルが未コミットのまま残り 08-08 の週次健全性チェックをスキップさせていたため、**本レビューで先にコミットした**（§4.2）。

### 本番健全性（`npm run check:loop` 実測, 2026-08-09）

| 対象 | 実測 | 判定 |
|---|---|---|
| `npx wrangler whoami`（デプロイ経路） | **未ログイン＝デプロイ不能** | **BLOCKER（26日目）** |
| `GET /api/v1/health` | **200** | 健全 |
| `GET /sitemap.xml` | **200**（`<loc>` **509**） | 応答健全（前週と同数＝**4週間 新規URLゼロ**） |
| `GET /api/v1/platform-status` | **200**（platforms **6** / 劣化1件） | 稼働・**旧構成**（`3bd3cb6` の 9 PF 化は未反映 16日目） |

`npm run check:loop` の総合判定: **BLOCKED — blocker 1 件（wrangler 未ログイン）／warning 1 件（twitter degraded）**。

---

## 1. 北極星 & KPI サマリー（28日）

| Metric | 2026-08-09（実測） | 2026-08-02（#008） | WoW | 判定 |
|---|---:|---:|---:|---|
| ad_script_load（北極星, 28d 合計） | **336** | 237 | **+99** | 増だが**獲得の指標として無効**（§1.1） |
| ad_script_load（7d） | **112** | 121 | −9 | 同上 |
| zone 10760541（In-Page Push, 28d） | **169** loads / err 3 / timeout 9・**load 率 93%** | 119 / 3 / 8・92% | 量+50 | 成功率は横ばい（92→93%） |
| zone 10969428（Push Notification, 28d） | **167** loads / err 2 / timeout 4・**load 率 97%** | 118 / 2 / 4・95% | 量+49 | 成功率は横ばい（95→97%） |
| セッション/28d（GA4 summary） | **57** | 65 | **−8** | **初めて減少**（§1.1） |
| アクティブユーザー/28d | **57** | 65 | −8 | 同上 |
| エンゲージメント率/28d | **59.6%** | 55.4% | +4.2pp | 先週の急落から一部戻し（母数小） |
| イベント総数/28d | **458** | 520 | −62 | — |
| **Google オーガニック セッション/28d** | **0** | 0 | ±0 | **3週連続で完全にゼロ** |
| GSC impressions/28d（**query 次元**＝従来系列・撤退基準の根拠） | **8** | 7 | **+1** | 増だが**全件がブランド名クエリ**（§2.1＝今週の最重要発見） |
| GSC impressions/28d（**page 次元**＝#008 から併記） | **32** | 33 | **−1** | 微減（実質横ばい） |
| GSC clicks/28d | **0** | 0 | ±0 | 母数ゼロ域 |
| GSC 平均順位（en・query 次元） | **16.4** | 30.1 | 「改善」 | **順位改善ではない**＝表示クエリ構成が変わっただけ（§2.1） |
| GSC indexed（URL Inspection サンプル100） | **37 / 100** | 36 / 100 | **+1** | **改善が再開**（§2.0） |
| 「Google 未発見」URL（サンプル100） | **62 / 100** | 63 / 100 | **−1** | **改善が再開**（72→68→66→65→63→63→**62**）（§2.0） |
| Crawled - not indexed（サンプル100） | 1 | 1 | ±0 | — |
| canonical mismatch / blocked | 0 / 0 | 0 / 0 | ±0 | 健全 |
| sitemap URL 数 | **501**（`<loc>`509） | 501（509） | ±0 | **4週間 新規 URL ゼロ**（stub 量産停止方針の帰結＝想定内） |

### 1.1 北極星の増加が獲得と無関係であることを、本週はより強い形で追認した

GA4 acquisition（28d, `sessionDefaultChannelGroup` × `sessionSourceMedium`）:

| channelGroup | sourceMedium | sessions | engagedSessions |
|---|---|---:|---:|
| **Direct** | (direct) / (none) | **173** | 105 |
| Unassigned | (not set) | 5 | 0 |
| Organic Search | yandex / organic | 1 | 1 |
| Organic Search | yandex.com.tr / referral | 1 | 0 |

- **Google からのオーガニック セッションは 0 件**（3週連続）。
- **Direct が 173/180 = 96%**（#008 は 91%、#007 も 91%）＝**比率がさらに上がった**。
- **Referral が本週ついに 0 件**（#008 の `compfight.com` 1 件も消滅）＝**被リンク由来の流入は観測上ゼロ**。
- ファネルは全段 **0**（Form Interest 0 / Extract Submit 0 / processing_complete 0 / download_actual_start 0 / share 0 / discovery 0 / related 0）。
- ★**今週の新しい傍証**: **セッションは減った（65→57）のに ad_script_load は増えた（237→336）**。1 セッションあたりのタグロードは **3.6 → 5.9 回**に上昇している。人間の利用が増えたなら両者は同方向に動くはずで、**「セッション減・ロード増・ファネル全段ゼロ」の同時成立は、同一ページを反復ロードする非人間トラフィックという読みと最も整合する**。
- 計測上の注意（#007 から不変・正直な記載）: acquisition 行の合計は **180** で、無ディメンションの summary セッション **57** と一致しない。GA4 はディメンションを付けるとセッションが重複計上されうるため、**チャネル構成比（約96% Direct）は方向性として読み、絶対値は summary の 57 を正とする**。GA4 の `views` は本週も **0**＝当プロパティで実質未計測（KPI 判断には用いていない）。
- **結論（不変）**: 北極星 `ad_script_load` は現在「獲得の進捗」を測っていない。

---

## 2. SEO シグナル（GSC, 28日）— 今週の最重要セクション

### 2.0 発見（インデックス）の改善は「頭打ち」ではなかった — #008 の判定を1週で訂正する

- Submitted and indexed: **37**（#008 36・**+1**）
- URL is unknown to Google: **62**（#008 63・**−1**）
- Crawled - currently not indexed: **1** / canonical mismatch: 0 / errors: 0

「未発見」比率の推移: **72 → 68 → 66 → 65 → 63 → 63 → 62**。

**#008 §2.0 は「単調改善が完全に停止した」「発見はもう伸ばせるレバーではないことが確定した」と書いたが、これは 1 週で覆った。** 正しくは **1 週の踊り場**であって天井ではなかった。sitemap が 4 週間 501 で不変のまま未発見が減っている以上、**Google は既存の在庫を今も少しずつ消化し続けている**と読むのが正確である。

ただし過度な修正も避ける: **+1 は 100 サンプル中の 1 件＝ノイズ域**であり、「改善が再開した」も「頭打ちだった」も同程度に弱い主張である。**今週の正しい教訓は「1 週分の横ばいを構造的な天井と断定したのが早計だった」というプロセス上の反省**であり、発見レバーの評価は **在庫消化が続く限り緩やかに前進する（ただし在庫 501 が上限）** に戻す。

### 2.1 【新規発見・今週の最重要】撤退基準を駆動している impressions は、**全件がブランド名クエリ**だった

`latest-gsc-query-pages.csv`（28日窓・`dimensions:["query","page","country","device"]`＝**撤退基準（§6）が使ってきた系列そのもの**）の**全行**は以下である:

| query | page | country | device | impressions | position |
|---|---|---|---|---:|---:|
| **clipkeep** | `/` | idn | MOBILE | 1 | 6 |
| **clipkeep** | `/` | ind | DESKTOP | 1 | 7 |
| **clipkeep** | `/` | nld | DESKTOP | 1 | 8 |
| **clipkeep** | `/` | phl | MOBILE | 1 | 7 |
| **clipkeep** | `/` | usa | DESKTOP | 3 | 15.7 |
| **tubekeep** | `/` | gbr | DESKTOP | 1 | 56 |

**＝6 行すべてが「clipkeep」（自サイトのブランド名）と「tubekeep」（類似名）というナビゲーショナル・クエリであり、`{platform} 動画 保存できない` 系の問題解決クエリ＝柱1 が狙っている検索需要からの impression は 1 件も無い（非ブランド impressions = 0）。**

これが意味することは #008 の指摘より深刻である:

1. **撤退基準は「ノイズが大きい」のではなく「測る対象を間違えている」。** §6 は「8週連続で impressions が成長しないなら撤退」と定めているが、この系列が実際に測っているのは**ブランド名の想起数**であって、戦略が賭けている**ロングテール検索の獲得**ではない。7→8 の「成長」は、**誰かが 1 回多く「clipkeep」と検索した**ことを意味するにすぎない。
2. **平均順位 30.1 → 16.4 の「改善」は順位改善ではない。** #008 は表示ページが 2 件だったのに対し今週は `/` の 1 件のみで、**低順位のページが表示されなくなった結果として平均値が下がった**だけである。同一クエリの順位が上がった証拠は 1 つも無い。**成果として読んではならない。**
3. **副次的な実害の発見: 自サイトのブランド名で 1 位を取れていない。** `clipkeep` の position は idn 6 / ind 7 / nld 8 / phl 7 / **usa 15.7** で、**8 impressions すべてで clicks 0**。自社名検索で 6〜16 位というのは、**ドメイン権威が「自分の名前を主張する」水準にすら達していない**ことの直接的な証拠であり、#006 以来の診断（律速＝ドメイン権威）を最も端的に裏付ける実測である。同時に、これは**ディレクトリ登録（柱4' B-2）が最初に効く場所**でもある（§9）。

### 2.2 page 次元: **pt の優位は 2 週連続で再現した**（#008 優先6 のデータゲート通過）

`latest-gsc-pages.csv`（28日窓・`dimensions:["page"]`）をロケール別に集計:

| locale | impressions/28d（#008比） | 平均position | ページ内訳（position） |
|---|---:|---:|---|
| en | **19**（19・±0） | — | `/` **12**(11.75) / `blog/twitter-video-download-private-account` 1(11) / `contact` 1(3) / `legal/cookies` 1(7) / `solution/bilibili-...-not-working` 1(9) / `solution/...-on-android` 1(42) / `trending/bilibili` 1(5) / `trending/threads` 1(6) |
| **pt** | **6**（7・−1） | **4.2** | **`/pt` 1(2)** / **`/pt/blog/safest-video-downloader-sites` 1(2)** / `blog/how-to-download-twitter-videos?locale=pt` 1(4) / `/pt/latest` 1(4) / `blog/tiktok-video-download-no-watermark?locale=pt` 1(6) / `/pt/blog/twitter-video-download-mp4` 1(7) |
| ja | **4**（4・±0） | 9.8 | `blog/tiktok-video-download-private?locale=ja` 2(9) / `?locale=ja` 1(10) / `/ja/solution/video-download-slow` 1(11) |
| es | **2**（2・±0） | 13.5 | `?locale=es` 1(8) / `blog/telegram-video-download-bot-safe?locale=es` 1(19) |
| hi | **1**（1・±0） | 9 | `?locale=hi` 1(9) |
| **ar** | **0**（0・±0） | — | **2週連続で impression ゼロ** |
| 合計 | **32**（33・−1） | | |

**#008 優先6 が設けたゲート（「#009 で 2 週分の page 次元データを確認してから確定する」）は、本週クリアした**:

- **pt は 6 impressions のうち 6 件すべてが position 2〜7**（平均 4.2）。#008 は 7 件中 6 件だったので、**比率はむしろ上がった（86% → 100%）**。
- 対照的に **en の主力 `/` は 11.75**、ja は平均 9.8、es は 13.5。**position 2〜7 の帯に安定して入っているのは pt だけ**という構図が 2 週連続で再現した。
- **ar は 2 週連続で page 次元でも 0**。柱1 の定義「ja/pt/ar 優先」のうち、**ar は実測に一切支持されていない**。
- 但し書き（#008 から継続・不変）: 各行は 1〜2 impression であり、「position 2」は**1 回そこに表示された**という意味しかない。**2 週連続の再現は「偶然ではない」の根拠にはなるが、「pt は上位表示できている」という一般化の根拠にはならない。**

### 2.3 結論

律速の診断は #006 から不変＝**「index されたページの順位化＝ドメイン権威・被リンク」**。今週の 3 つの事実を加えると:

1. **発見は止まっていない**（62・#008 の「頭打ち」判定は訂正）。ただし在庫 501 が上限。
2. **非ブランド検索からの露出は依然としてゼロ**（§2.1）。柱1 が狙う需要には 1 度も到達していない。
3. **自サイト名ですら 6〜16 位**（§2.1-3）＝権威不足の最も直接的な実測。
4. **pt の優位は 2 週連続で再現**（§2.2）＝賭け先の絞り込みは実測に支持された。

---

## 3. ファネル / グロースループ（28日, 凍結中）

Phase L につき凍結。参考実測のみ: Form Interest 0 / Intent-to-Clip 0 / Success 0、share 0 / discovery 0 / related 0、viral factor 0.00。**セッション 57・イベント 458 に対しファネル進入は全段ゼロ**＝§1.1 を補強。Phase G（indexed≥50 かつ impressions≥1,000）到達まで着手しない。

---

## 4. 障害 / 健全性

- 5xx率: 0（health 200）／degraded 発動: なし（**アプリ配信本体は健全**）
- **柱1' status 資産が示す上流の異常（本番実測 2026-08-09）**:

  | platform | status | http | uptime | samples |
  |---|---|---:|---:|---:|
  | **twitter** | **limited** | **401** | **0%** | 56 |
  | telegram | operational | 200 | 100% | 56 |
  | reddit | operational | 403 | 100% | 56 |
  | threads | operational | 302 | 100% | 56 |
  | bluesky | operational | 200 | 100% | 56 |
  | tiktok | operational | 403 | 100% | 56 |

  - **twitter は uptime 0%（n=56＝直近14日の全プローブ失敗）が約30日継続**。根因側の起点は 07-30 に特定済＝`src/lib/extract/twitter.ts:186-215` の fx-api 分岐が **HTTP 401 をどの枝でも分類していない**ため、本番が出し続けている 401 が extractor 自身のログに一切現れない。
  - bluesky は 93→96→**100%** に回復（一過性だったと確定）。
  - X 抽出の実地確認は依然として無人不可（`POST /api/v1/extract/prepare` は `403 TURNSTILE_MISSING`＝Turnstile は設計通り機能。CAPTCHA 回避は行わない）。
- **【4週連続・最大の運用障害】デプロイ経路が 26 日間 断絶**: `wrangler whoami` → 未ログイン。07-14（ver `1d946b55`）を最後に OAuth が失効。`3bd3cb6`（6→9 PF）は**本番未反映 16 日目**。**復旧はユーザーの 1 回操作のみ**。

### 4.1 ★【今週の最重要発見・根本原因の特定】日次ループは「発火率が低い」のではなく、**スケジューラ上で無効化されていた**

**`docs/ops/daily/` に 2026-08 の log は 1 件も存在しない。最後の daily log は `2026-07-30.md`＝10 日間ゼロ。**

#008 §7 提案5 は「日次ループの発火率が 5 割を切っている」と記録し、「**スケジューラ側の調査はリポジトリ外の作業であり、ここでは記録に留める**」として原因究明を見送った。**本レビューでその調査を実施し、原因を特定した**:

```
taskId:     clipkeep-daily-growth-loop
schedule:   At 06:06 AM, Monday through Friday  (cron: 0 6 * * 1-5)
enabled:    false          ← ★これ
lastRunAt:  2026-07-29T21:46:04Z  (= 2026-07-30 06:46 JST)
nextRunAt:  (なし)
```

**日次グロースループのスケジュールタスクは `enabled: false`＝無効化されている。** `lastRunAt` は最後の daily log（07-30）と正確に一致し、**それ以降 1 度も発火していない**。

- **未発火スロット: 07-31（金）／08-03（月）／08-04（火）／08-05（水）／08-06（木）／08-07（金）＝ 6 スロット連続。**
- #008 が「発火率が 5 割を切っている」と読んだ現象（07-27・07-29・07-31 に log が無い）は、**単なる取りこぼしではなく無効化の予兆または初期段階**だった可能性が高い。
- 有効/無効の切替はリポジトリ外の設定であり、**いつ・誰が・意図的に無効化したのかはここからは判定できない**（正直な記載）。7/15〜7/23 にもスケジュールタスク登録が消失した前例があるため、**意図的な操作でなく環境側の事故という可能性も残る**。
- **これは戦略ではなく運用の問題だが、影響は戦略上のどの論点より大きい**: バックログをどれだけ丁寧に並べ替えても、**消費する主体が停止している以上 1 件も実行されない**。#008 が「デプロイ不要のタスクを 3 件用意した＝全部ブロックで日次枠が溶けるのを避けるため」と書いた配慮は、**そもそも日次枠自体が存在しなかったため空振りに終わった**。
- **再有効化は Claude 側では行わない**（スケジューラの永続設定の変更＝ユーザーの領分）。§8 優先0 として依頼する。

### 4.2 本週の稼働状況と、未コミット4ファイルの扱い（正直な記載）

| ループ | 状態 |
|---|---|
| 日次グロースループ（月〜金 06:06） | **無効化により 6 スロット連続 未発火**（§4.1） |
| 週次 健全性チェック（土 10:33） | **発火したが `status: skipped`**（作業ツリーが汚れており手順1 の規定で全手順を実施せず終了・`1fcf218`） |
| 週次 戦略レビュー（日 05:39） | **本レビュー＝正常稼働** |

**＝本週も出荷 0 件。3 つある自動ループのうち 2 つが停止または空振りし、動いているのは観測系（本レビュー）のみ**という状態が可視化された。

**未コミット4ファイルの扱い**: 08-08 の健全性チェックは「内容の妥当性判断は書いた本人＝ユーザーの領分」として、あえてコミットせず `skipped` で終了した。この判断自体は保守的で妥当だが、**放置すると週次の守りのループが毎週スキップし続ける**（08-08 レポート自身がその害を指摘している）。本レビューでは以下の理由で**先にコミットする**判断を採った:

- 変更は **docs 4ファイルのみ・アプリケーションコードは 1 行も含まない**（`AGENTS.md` / `docs/analytics/README.md` / `docs/ops/analytics-auth-recovery.md` / `task.md`）。
- 内容は **SA キーを `D:\dev` 配下から `D:\secrets\clipkeep\` へ退避した実作業を文書へ反映したもの**であり、`D:\dev\docs\SAFETY_CHECKLIST.md` の Red Lines に沿う方向の変更。**本日の `growth:review` が成功したこと自体が、記述どおりの構成が実在し機能していることの実証**である（推測ではない）。
- `task.md` の変更は CURRENT ブロックの運用注記＋`CURRENT-END` 位置の修正で、内容の書き換えではない。
- 判断の記録: **破棄ではなくコミットを選んだ**（破棄は実作業の記録を失う不可逆操作）。commit は本レビューとは別コミットに分離し、来歴を追えるようにした。

---

## 5. 柱別 効果判定（変更単位ではなく柱単位）

| 柱 | 直近の打ち手（08-02〜08-09 出荷物） | 観測 | 判定 | 次アクション |
|---|---|---|---|---|
| 柱1 多言語ロングテール | **新規出荷なし**（sitemap 501 で 4 週間不変） | 未発見 **63→62**・indexed **36→37**＝**改善が再開**（#008 の「頭打ち」は誤読・§2.0）。page 次元は 32（33）で横ばい、**pt は 6/6 が pos 2〜7 で 2 週連続再現**（§2.2）。ar は 2 週連続 0。★**非ブランド検索からの impression は 0**（§2.1） | **仮説は棄却されない。賭け先の絞り込みは実測に支持された**（pt）。ただし柱1 が狙う問題解決クエリには**まだ 1 度も到達していない** | ①**優先4 に昇格＝pt 既存ページの内容深化**（データゲート通過。新規 URL は増やさない）②ar は柱1 の優先ロケールから外すことを検討（§7 提案2）③page 次元の週次計器化（優先1）は**未着手のまま**＝日次ループ停止のため |
| 柱1' linkable asset（主軸） | **新規出荷なし**。`3bd3cb6` は 07-24 push 済・**16 日間 未デプロイ** | 資産は稼働（6 PF 全応答・`checkedAt` fresh）。**26 日間 更新が本番へ届いていない**。資産自身が twitter 0%（約30日）を出し続けている | **"稼働"は維持、"成長"は 4 週連続 停止**。律速は資産の設計でなく**デプロイ経路の断絶**（不変） | ①デプロイ復旧後ただちに `3bd3cb6` を出荷 ②その後 A-v2（`extractor_jobs` per-PF 実成功率の併記） |
| 柱2 トレンド鮮度 | **新規出荷なし**（最後の変更は 07-13 の 60s 緩和） | **27 日・約108 cron 窓 稼働して産出ゼロ**（`/trend/` が sitemap 0 件） | **#007/#008 の「60s 緩和は失敗」判定を、さらに 7 日分のデータで追認**（3 週連続） | **二択（(a) 有料 Browser Rendering＝危害ゲート①要承認／(b) 凍結）への回答が 2 週間 未取得**。推奨は引き続き **(b) 凍結**（§7 提案3） |
| 柱3 回遊 | 凍結（Phase L） | — | 凍結継続 | Phase G まで着手しない |
| 柱4' 外部流入・権威 | **新規出荷なし**（07-26・08-02 の outreach 下書きの実施有無は依然 未確認） | **GA4 referral が本週ついに 0 件**（#008 の `compfight.com` 1 件も消滅）＝**被リンク由来の流入は観測上ゼロ**。代理指標（badge 外部 Referer 集計）は KV binding 必須で**未実装のまま 4 週間**。★新規: **自サイト名 `clipkeep` ですら 6〜16 位**（§2.1-3）＝権威不足の最も直接的な実測 | **効果は未観測。かつ効果を測る計器が無い状態が 4 週間 継続**。ただし**「何が足りないか」の実測的な裏付けは今週むしろ強まった** | ①KV namespace 作成＝ユーザー承認クラス（優先5）②**ブランド名 SERP の確立はディレクトリ登録が最初に効く場所**＝AlternativeTo 登録の価値が実測で裏付いた（§9・優先11） |

**律速の結論**: 戦略的な律速は #006 から不変で「**順位化＝ドメイン権威/被リンク**」であり、今週それは **「自サイト名でも 1 位を取れない」** という最も直接的な形で観測された。一方 **実務上の律速は 4 週連続でデプロイ経路の断絶（26日）だが、今週それに輪をかけて「日次ループ自体が無効化されていた（6 スロット連続 未発火）」ことが判明した**（§4.1）。**バックログの優先順位より先に、実行主体と出荷経路を戻すことが優先する。**

---

## 6. 撤退基準の判定（毎回必須）

- 基準: **8週連続**で GSC impressions(28d) が成長しない → 改修停止・戦略変更提案。
- **query 次元（従来系列＝§7 の文言が指す系列）** = **8**（#008 = 7）→ **+1＝成長**。
- **page 次元（#008 提案1 で併記開始）** = **32**（#008 = 33）→ **−1＝非成長**。
- #008 提案1(c) の運用（**両系列がともに非成長のときだけ非成長とカウント**）に従い、**今週は非成長にカウントしない**。
- 実測ベースのカウント履歴: #002（06-15, 17→15 減＝非成長, 連続1）→ #003（06-24, 測定不能＝非加算）→ #004（06-29, 15→3 減＝非成長, 連続2）→ #005（07-05, 3→4 増＝成長, リセット=0）→ #006（07-12, 4→4 横ばい＝非成長, 連続=1）→ #007（07-26, 4→5 増＝成長, リセット=0）→ #008（08-02, 5→7 増＝成長, リセット=0）→ **#009（08-09, 7→8 増＝成長, リセット=0）**。
- **判定: 撤退基準 非該当（連続非成長=0）。**
- **重要な留保（#008 から決定的に強化）**:
  1. **今週の「成長」の実体は、誰かが 1 回多く「clipkeep」とブランド名で検索したことである。** §2.1 のとおり query 次元の 8 impressions は**全件がブランド名（`clipkeep` 7 / `tubekeep` 1）**で、**柱1 が狙う非ブランドの問題解決クエリからの impression は 0**。
  2. **したがって撤退基準は「ノイズが大きい」ではなく「測る対象を間違えている」。** §7 が守ろうとしているのは「ロングテール SEO 戦略が機能しているか」だが、実際に測っているのは「ブランド名の想起数」であり、**戦略が完全に失敗していてもブランド検索が 1 件増えれば毎週リセットされ続ける**。**カウンタが 8 期にわたり一度も 3 に達していないこと自体が、基準が機能していないことの証拠**である。
  3. 正直な要約: 3 つの実質指標は **非ブランド impressions 0（不変）・未発見 62%（+1 改善・ノイズ域）・Google オーガニックセッション 0（3週連続）**。**実体としては「前進ほぼゼロ」が正しい**。ただし §2.0 の反省に基づき、これを「天井に達した」と断定はしない。
  4. §7 の文言変更はユーザー承認が要るため、本レビューでは**基準そのものは変更せず、判定を機械的に適用したうえで実体を併記**した（§7 提案1 で是正を提案）。

---

## 7. 戦略変更提案（growth-strategy.md は変更せず、ここに記載）

1. **【最優先・計測の是正／#008 提案1 を強化】撤退基準の指標を「非ブランド impressions」に切り替える**: #008 は「query 次元は匿名化で過小（7 vs 33）だから page 次元を主系列に」と提案した。**今週の発見はより根本的である**＝query 次元は過小なだけでなく、**残った行が全件ブランドクエリ**で、戦略の成否と因果的に無関係だった。是正案: (a) 週次レビューは **`query != ブランド名` でフィルタした impressions を主系列**として記録する（`latest-gsc-query-pages.csv` に query 列があり**新規 API 呼び出しゼロ**で算出可能）、(b) page 次元・query 次元も併記して連続性を壊さない、(c) 撤退カウントは移行の断絶を避けるため**当面は「非ブランド系列が非成長 かつ page 次元も非成長」のときだけ非成長とカウント**する。**実装は `scripts/growth-summary.mjs` の集計追加のみ・デプロイ不要・自律実行可**（優先1 に統合）。§7 本文の文言変更はユーザー承認が要るため、**運用上の併記から始める**。
2. **【新規・柱1 のロケール定義の是正】`ar` を柱1 の優先ロケールから外し、`pt` を筆頭に繰り上げることを提案する**: growth-strategy.md §4 柱1 は「ja/pt/ar 優先、次に id/hi/tr」と定義しているが、page 次元 2 週分の実測は **pt 6（6/6 が pos 2〜7）・ja 4（平均 9.8）・ar 0（2週連続ゼロ）**。**ar は 2 週連続で impression が 1 件も無く、優先ロケールに置く実測的根拠がない**。一方 hi は 1、es は 2 で ar を上回る。提案: 優先順を **pt > en > ja**、ar は「優先」から外す（削除ではなく、既存ページは維持し新規投資の対象外とする）。※これは §4 本文の書き換えに当たるため**ユーザー承認が要る**。承認が無くても優先4（pt 深化）は実行可能なので、**日次ループを止める理由にはしない**。
3. **【3週連続・再提示／柱2】60s 緩和は失敗。二択への回答が 2 週間 得られていない**: 07-13 出荷の 60s 緩和は**本番 27 日・約108 cron 窓で `/trend/` 産出ゼロ**。結論は 3 週連続で不変。**推奨は (b) 凍結**（検索流入が実質ゼロの現状ではトレンドページを産出できても順位化しない）。凍結は戦略文書の書き換えに当たるため**ユーザーの一言が要る**。回答が無い間 柱2 は事実上すでに凍結されており、実害は「バックログ上に判定不能の項目が居座ること」のみ。
4. **【継続・柱4'／今週 実測の裏付けを得た】ディレクトリ登録の価値が「ブランド SERP」の観点から具体化した**: §2.1-3 で **自サイト名 `clipkeep` の position が 6〜16（usa 15.7）・clicks 0** と判明した。これは被リンク不足の最も直接的な実測であると同時に、**ディレクトリ登録（AlternativeTo 等）が最初に効く場所**でもある（信頼性の高い第三者ドメインが「ClipKeep」という固有名を自サイトへリンクすると、ブランドクエリの実体解決が進む）。**#008 までの「登録すべき」という一般論に、今週初めて実測の根拠が付いた**。優先11 の説明文を更新した。
5. **【プロセス・最重要】自動ループの「稼働そのもの」を週次で検査対象にする**: #008 提案5 は「週次レビューが daily log の存在有無を数える」ことで穴を可視化する運用を導入したが、**それは穴を数えるだけで原因を特定できなかった**（§4.1）。本レビューでスケジューラの登録状態を直接照会したところ **`enabled: false`** が判明した。**「log が無い」の原因は「失敗した」「スキップした」「そもそも登録が無効/消失している」で対処が全く違う**ため、**週次レビューは毎回スケジュールタスクの `enabled` / `lastRunAt` / `nextRunAt` を実測して記録する**運用へ改める（本レビューから実施＝§4.1）。※7/15〜7/23 にも登録消失の前例があり、**この種の障害は再発している**。

> いずれも growth-strategy.md 本文は未変更（KPI 履歴行の追記のみ）。**提案1・5 は自律実行しうる**（計測運用）。**提案2 は §4 本文の書き換え＝要ユーザー承認**、**提案3 の (a) 有料切替は危害ゲート①**、**提案4 に伴う KV namespace 作成は「一度きりの人間操作」**＝いずれもユーザー承認が必要。

---

## 8. 翌週アクション（task.md 戦略バックログへ反映済み）

`task.md` の戦略バックログを並べ替え・補充し、**計 13 件**（優先0〜12。うち 優先0・6・8・12 はユーザー操作/判断、**残り 9 件が日次ループの消費対象**）を維持した（≥7 要件クリア）。**うち 4 件（優先1・3・7・10）はデプロイ経路が塞がったままでも完遂できる**ように選んである（#008 の 3 件から 1 件増やした）。

> ★**ただし率直に記す**: **日次ループが無効化されている限り、この並べ替えは 1 件も消費されない**（§4.1）。優先0 が解けない場合、#010 でもこの表はそのまま残る。**バックログの精緻化より優先0 の解消が先である。**

| 優先 | タスク | 柱/種別 | デプロイ要否 | 成功指標 |
|---|---|---|---|---|
| **0** | **【ユーザー操作・全タスクの前提／3点まとめて】** ★**(a) 日次ループの再有効化**＝スケジュールタスク `clipkeep-daily-growth-loop` が **`enabled: false`**（07-30 以降 6 スロット連続 未発火・§4.1）。**これを戻さない限り以下のどのタスクも実行主体が存在しない**。**(b) `npx wrangler login`**（または `CLOUDFLARE_API_TOKEN` 設定）でデプロイ経路を復旧 → 直後に `npm run deploy:prod && npm run check:release:prod` で **`3bd3cb6`（6→9 PF）を出荷して閉じる**。**(c) 優先5 用の KV namespace 作成**（同じ Cloudflare 作業なので往復を1回に） | 運用/blocker | — | (a) `enabled: true` かつ翌営業日に daily log が生成される／(b) `wrangler whoami` 認証済・`/api/v1/platform-status` の platforms が 6→**9**／(c) namespace 作成済み |
| **1** | **【自律可・デプロイ不要／#008 優先1 を強化して継続】計測: `growth:review` に (i) 非ブランド impressions（`query` がブランド名でない行の集計）と (ii) page 次元 impressions（locale 別・position 別）を追加**（§7 提案1。`latest-gsc-query-pages.csv` / `latest-gsc-pages.csv` は既に毎回取得済み＝**新規 API 呼び出しゼロ**。`scripts/growth-summary.mjs` の集計とサマリー出力の追加のみ）。★**#008 で優先1 に置いたが日次ループ停止のため未着手**。今週の §2.1 の発見により**重要度がさらに上がった** | 計測 | **不要** | `npm run growth:review` の出力とスナップショット JSON に「非ブランド impressions」と page 次元ロケール別内訳が出る |
| **2** | **柱1/修復: fxTwitter 401 の明示分類とログ化**（`src/lib/extract/twitter.ts:186-215` が 401 をどの枝でも分類せず黙って落ちる＝本番の**約30日間**の異常が extractor ログに現れない根因。まず 401 を明示分類してログに出すのが最小の一手。**実装・テスト・push はデプロイ非依存で先行できる**） | 柱1/修復 | 検証のみ要 | 401 が失敗分類に現れる・vitest green／（復旧後）status の twitter が operational へ |
| **3** | **【自律可・デプロイ不要】健全性 HC-9: 認証/レート制限レイヤーのユニットテスト**（`src/lib/rate-limit/extract.ts` の `getClientKey`／窓・上限判定＝純粋に近い部分から。**"fail-closed であること"を契約として固定する**） | 健全性/品質 | **不要** | rate-limit の純関数にテストが入り green（production 非改変） |
| **4** | ★**【昇格・データゲート通過】柱1: pt ロケール既存ページの内容深化**（#008 優先6 は「#009 で 2 週分の page 次元データを確認してから着手」というゲート付きだった。**本週それをクリア**＝pt は **6/6 が pos 2〜7**（平均4.2）で 2 週連続再現、ar は 2 週連続 0（§2.2）。**新規 URL は増やさない**＝thin/doorway リスク非増。対象候補: `/pt`(pos2) / `/pt/blog/safest-video-downloader-sites`(pos2) / `/pt/latest`(pos4) / `/pt/blog/twitter-video-download-mp4`(pos7)） | 柱1/内容 | 要 | pt ページの impressions/position が 2 週連続で改善 |
| **5** | **柱4' 測定: badge 外部 Referer の KV 集計＝被リンク代理指標**（**要ユーザー承認: KV namespace 作成**。主軸 A+B の唯一の計器＝**4週間 不在**。優先0(c) と同時依頼） | 柱4'/測定 | 要 | 外部 Referer ホスト数を週次で取得できる・本番200 |
| **6** | **柱2: 凍結の可否をユーザーに確認する（§7 提案3）**（(a) 有料 Browser Rendering＝危害ゲート① / (b) 凍結。**推奨は (b)**。**2週間 未回答**） | 柱2/判断 | — | ユーザーの一言を得て task.md に明記 |
| **7** | **【自律可・デプロイ不要】健全性 HC-8 残: `extractTwitter`／`extractTikTok` の I/O モック付きテスト**（失敗分類の網羅。優先2 と同じレイヤーなので連続実施が自然） | 健全性/品質 | **不要** | extract 層の失敗分類にテストが入り green |
| **8** | **健全性 HC-6: `next` 15.2.9 → 15.5.22（security・緊急度 高）**（middleware redirect SSRF / rewrites の HTTP request smuggling / App Router Middleware・Proxy bypass / cache poisoning / CSP nonce XSS が構成に該当。`package.json` の版指定変更を伴うため**要ユーザー承認**・要本番フル検証） | 健全性/security | 要 | `next@15.5.22` で typecheck/lint/build/vitest PASS・本番フル検証 PASS |
| **9** | **柱1' Track A: A-v2 = `extractor_jobs` の per-PF 実成功率を status 資産へ併記**（合成プローブ＝上流到達性 と 実ジョブ成功率 は別物。二軸併記は被引用性を上げる。優先2 の結果とも接続） | 柱1'/資産 | 要 | status page/JSON に per-PF 実成功率が出る・本番200 |
| **10** | **【新規・自律可・デプロイ不要】健全性 HC-7: `knip.json` の `ignoreBinaries: ["powershell"]` 削除**（knip の Configuration hint に従う設定の陳腐化除去・極小・production 非改変）。★**デプロイ断絶が 4 週続いているため、デプロイ不要枠を 3→4 件に増やす目的で繰り上げた**（#008 まで健全性バックログに眠っていた項目）。※`powershell` は `check:prod` 等で実使用中＝「常に Windows で実行する」前提の明示とセットで実施 | 健全性/品質 | **不要** | knip PASS・`npm run check:*` が従来どおり動作（production 非改変） |
| **11** | 柱1: downloader help リンク（sns/telegram/tiktok/twitter 4本）＋ extractor-form/result-client の status 連動 help リンクの `?locale=`→path 形式化（06-29 積み残し） | 柱1/発見 | 要 | 残る solution help リンクの path 形式化・本番200 |
| **12** | **柱4' B-2（ユーザー一度きり・任意）: AlternativeTo 登録**（★**今週 実測の裏付けを得た**＝自サイト名 `clipkeep` で position 6〜16・clicks 0（§2.1-3）＝**ブランド SERP の確立にディレクトリ登録が最初に効く**。文面は `docs/ops/outreach/2026-08-09.md`／authority-plan.md B-2） | 柱4'/権威 | — | 登録1回・被リンク発生（ユーザー実行分） |

> **落としたもの・変えたもの（明示）**:
> - **#008 優先6（pt 深化）を 6→4 に昇格**。#008 自身が設けた「2 週分の page 次元データ確認」ゲートを本週クリアしたため（§2.2）。
> - **HC-7 を健全性バックログから優先10 へ繰り上げ**。理由は重要度ではなく**「デプロイ不要で消費できる枠を増やす」という運用上の判断**（デプロイ断絶 4 週目）。
> - 優先0 に **(a) 日次ループ再有効化**を追加し、**先頭に置いた**。デプロイ復旧(b) より先に書いたのは、**(b) だけ直しても実行主体が無ければ何も動かない**ため。
> - **HC-10（レート制限 env parse の NaN ガード欠落）は今週もバックログに積んでいない**。理由不変: セキュリティ性質を変える修正であり本番検証なしに出荷すべきでない＝優先0(b) の復旧後に HC-9（優先3）とセットで扱う。実害は現時点でなし。
> - HC-5（yaml moderate）は**ユーザーの承認マーカーが 5 週連続で未付与**のため据え置き。

---

## 9. 柱4' outreach（ホワイトハット限定）

- 本週分: **`docs/ops/outreach/2026-08-09.md`**。**#008 §9 の予告どおり、今週から「毎週フル下書きを作り直す」運用をやめ、差分のみを記す形式へ切り替えた**（#008 の停止条件＝「#009 でも AlternativeTo 未実施かつ KV 承認も無い場合」に**両方とも該当**したため）。文面の正本は `docs/ops/outreach/2026-08-02.md` と `docs/strategy/authority-plan.md` に据え置き、本週分には**今週新たに得られた実測の根拠**（ブランド SERP）と、それに基づく訴求の変更点のみを記載した。
- **投稿はユーザー手動・自動投稿は禁止**。§6 ガードレール（スパム的大量登録・PBN・有料リンク）不変。有料PRは危害ゲート①＝要承認。
- **今週の変化（4週ぶりに実質的な新情報がある）**: §2.1-3 の実測で、ClipKeep は**自サイト名でも 6〜16 位**しか取れていない。これは「被リンクが足りない」という一般論を、**「ブランドクエリの実体解決すらできていない」という具体的で検証可能な症状**に落とし込む。ディレクトリ登録の訴求はこの症状に直接対応するため、**優先12 の説明文と outreach の位置づけを更新した**。

---

## 10. 参照

- `docs/strategy/growth-strategy.md`（正本・KPI 履歴に本日分=実測行を追記）
- `docs/strategy/linkable-asset-plan.md`（柱1' Track A 設計）／`docs/strategy/authority-plan.md`（柱4' Track B・outreach 正本）
- 前回: `docs/ops/weekly_review_2026-08-02.md`（#008・7日前）
- 中間記録: `docs/ops/health/2026-08-08.md`（週次健全性・**status: skipped**＝作業ツリー汚れ）／**daily log は 2026-08 に 1 件も無し**（最後は `docs/ops/daily/2026-07-30.md`・§4.1）
- スナップショット: `docs/analytics/history/growth-2026-08-08T20-43-03-182Z.json`（2026-08-09 as-of・実測）
- 生 CSV: `docs/analytics/latest-gsc-query-pages.csv`（**§2.1 の根拠＝全6行がブランドクエリ**）／`latest-gsc-pages.csv`（§2.2 の根拠・page 次元28d）／`latest-gsc-locale-summary.csv`
- 認証: SA（`D:\secrets\clipkeep\`・リポジトリ外）／`docs/ops/analytics-auth-recovery.md` §4.2
- スケジューラ実測（§4.1 の根拠）: `clipkeep-daily-growth-loop` = `enabled:false` / `lastRunAt 2026-07-29T21:46:04Z` / `nextRunAt` なし。`clipkeep-weekly-health-check` = `enabled:true` / `lastRunAt 2026-08-08T01:33:47Z`。`clipkeep-weekly-strategy-review` = `enabled:true` / `lastRunAt 2026-08-08T20:40:04Z`
