# 日次ループ 2026-07-24

status: blocked（実装・push は完了。**本番デプロイ不可＝wrangler 未ログイン＝要ユーザー1回操作**）

> 前回ループは 2026-07-14。**7/15〜7/23 の 9 日間、日次ループは未発火**（原因はスケジュールタスク登録の消失＝
> Claude 側の運用事故で、本リポジトリ起因ではない。7/23 に再登録済み）。本日が復帰後の初回。

## 0. 健全性チェック（ループ手順3）

| 対象 | 結果 |
|---|---|
| `https://clipkeep.net/api/v1/health` | **200** |
| `https://clipkeep.net/sitemap.xml` | **200**（`<loc>` = **509**） |
| `https://clipkeep.net/api/v1/platform-status` | **200**（6 platforms・overall=limited） |

本番は健全。修復差し替えは不要と判断。

## 1. 計測（バックログ #3「発見%＋参照ドメイン数の週次判定」）

`npm run growth:review` 実行。**認証 OK（サービスアカウント・キャッシュでなく fresh）**。
スナップショット: `docs/analytics/history/growth-2026-07-23T21-13-44-495Z.json`

| 指標(28d) | #006 2026-07-12 | **本日 2026-07-24** | 差分 |
|---|---:|---:|---|
| ad_script_load（北極星） | 24 | **85** | **+61** |
| セッション | 9 | **32** | +23 |
| GSC impressions | 4 | **5** | +1 |
| GSC clicks | 0 | **0** | 横ばい |
| GSC 平均順位 | 54.3 | **37.0** | **改善** |
| indexed（100件サンプル） | 33 | **34** | +1 |
| **「Google 未発見」** | 66/100 | **65/100** | **改善継続（72→68→66→65）** |
| 参照ドメイン数 | 0 | **取得不可**（後述） | — |

**判定**:
- **発見側の改善は継続しているが鈍化**（-6 → -2 → -1/週）。indexed も 33→34 とほぼ横ばい＝
  **#006 の「律速は発見→順位化＝権威へ移行」という読みを追認**。impressions 5・clicks 0 は依然ノイズ域。
- 平均順位 54.3→37.0 は改善だが、impression 母数 5 では**統計的に無意味**（1〜2 クエリの入替で動く）。
  順位を成果と読まないこと。
- ad_script_load 85 / セッション 32 の急増は **GSC impressions 5 と整合しない**＝検索流入起因ではない
  （直接/参照/ボット等）。**北極星の値を「獲得が改善した」と読んではいけない**。次回 review で流入元内訳を確認する。
- **撤退基準**: impressions 4→5＝微増。非成長の連続カウントは**リセット（連続=0）**。

### 参照ドメイン数が取れない件（本タスクの積み残し）

`docs/strategy/authority-plan.md` の主先行指標である「GSC リンクレポートの参照ドメイン数」は、
**Search Console API に links エンドポイントが存在しない**（sitemaps / searchanalytics / urlInspection のみ）ため
**自動取得できない**。現状の自動計測系では原理的に埋まらない。

- 対応方針（次回以降）: ①ユーザーが GSC UI の「リンク」→「上位のリンク元サイト」を月1で見る（1回きり操作でなく反復＝
  決定事項6 に照らすとループ施策としては非推奨）、または②**被リンクの代理指標を自前で持つ**
  （例: `/platform-status/badge` の外部 Referer 数を Worker 側で集計＝バッジ被埋め込みの実測）。
  **②を推奨**（自律運用可・Track B の効果に直結）。バックログへ新規項目として追加した。

## 2. 本日の実装タスク（バックログ #4）

**柱1' Track A: プローブ・カバレッジ拡大**（Pinterest / Facebook / Bilibili）。
`#3` は計測タスクでデプロイ単位を持たないため上記1で消化し、実装単位としては先頭の `#4` を選定。
KPI Gate 上は「linkable asset の被引用性強化」＝獲得側で**凍結対象外**。

### 実装（commit `3bd3cb6`）

ClipKeep が**実際に extractor を持つ**（＝上流の正確性を担保できる）3 プラットフォームを、
静的な列挙 `ALSO_SUPPORTED` から**実測プローブ**へ昇格。資産の網羅性 6→**9 プラットフォーム**。

1. `src/lib/platform-status/probes.ts`
   - `ProbeCandidate` に **`headers?`** を追加。Pinterest は `X-Pinterest-PWS-Handler`、Bilibili API は
     ブラウザ UA + `Referer` が無いと上流の実挙動を再現できない（extractor と同じヘッダを送る＝実測の忠実性）。
   - 追加ターゲット（accepted-status は**各 extractor の失敗モードに合わせて個別化**）:
     | platform | upstream | operational とみなす status | 設計意図 |
     |---|---|---|---|
     | pinterest | `pinterest.com` PinResource API（`id=1`） | 200/400/404 | 404「Pin not found」＝API 健全。**実在ピンに依存しない**（リンク切れ耐性） |
     | facebook | `facebook.com/watch/` | 200 のみ | ログインウォール(401/302)・403 は**実際に抽出を壊す**ので `limited` に落とす |
     | bilibili | `api.bilibili.com/x/web-interface/view`（`BV1xx411c7mD`=aid 2 の恒久動画） | 200 のみ | 412/429（風控）は `RATE_LIMITED` と同義＝`limited` |
   - **Reddit/Threads との非対称は意図的**（あちらは 403/429 がデータセンターIPへの通常応答で実害なし）。
     この非対称を methodology に明記＝「実測でないものを実測と偽らない」（linkable-asset-plan 品質節）。
   - `ALSO_SUPPORTED` を **Discord / Lemon8 のみ**に縮小（per-post の CDN/アプリ URL しか無く、
     単一の安定上流を合成プローブで正直に代表できないため）。`PROBED_PLATFORMS` を新規 export。
2. `src/lib/platform-status/export.ts`: Dataset description に 3 PF を追加、methodology に上記の非対称ルールを明記。
3. `app/platform-status/page.tsx`: meta description と「How this is measured」を同内容へ整合。
4. `src/lib/platform-status/probes.test.ts`（新規5件）: プローブ対象の不変条件
   （**probed と also-supported が重複しない**＝ページの自己矛盾を構造的に防止）・uptime 集計・ロールアップ。

### 事前実測（ヘッダ付きで上流の実応答を確認してから accepted-status を決定）

| 上流 | 応答（ローカルIP） |
|---|---|
| Pinterest PinResource `id=1` | **404** `{"error":{"message":"Pin not found"}}` 312ms＝正常応答 |
| facebook.com/watch/ | **200** HTML 289ms |
| api.bilibili.com view | **200** `code:0` 245ms |

### ローカル検証

- `npm run typecheck` PASS
- `npm run lint` PASS（No ESLint warnings or errors）
- `npm test` **44/44 PASS**（39→44、新規5件）
- `npm run build` PASS（`/platform-status` `/api/v1/platform-status` ともルート登録を確認）

## 3. Definition of Done — **未完（blocked）**

| # | ステップ | 結果 |
|---|---|---|
| 1 | typecheck / lint / build | ✅ PASS |
| 2 | main へ commit・push | ✅ `b5c5f7b..3bd3cb6` |
| 3 | `npm run deploy:prod` | ❌ **BLOCKED** |
| 4 | `npm run check:release:prod` | ⏭ 未実行（3 が前提） |
| 5 | 本番での変更箇所確認 | ⏭ 未実行（3 が前提） |
| 6 | daily log 記録 | ✅ 本文書 |

### ブロック理由（要ユーザー操作・1回きり）

```
X [ERROR] In a non-interactive environment, it's necessary to set a
CLOUDFLARE_API_TOKEN environment variable for wrangler to work.
```

- `npx wrangler whoami` → **`Not logged in.`**
- 資格情報 `%APPDATA%\xdg.config\.wrangler\config\default.toml` の最終更新は **2026-07-14 06:19**
  ＝**前回ループ実行日**。以後 9 日間ループが未発火でリフレッシュされず、**OAuth セッションが失効**した。
- OpenNext のバンドル生成自体は**成功**しており、失敗しているのは wrangler の認証段のみ
  （＝コード側の問題ではない。ログ: `wrangler-2026-07-23_21-24-30_746.log`）。

**復旧手順（ユーザー、どちらか一方）**:

```bash
npx wrangler login
```

（ブラウザ OAuth。以後は Claude が自律でデプロイ可＝growth-strategy 決定事項6 の「一度きりの人間による認証」例外に該当）
あるいは `CLOUDFLARE_API_TOKEN` を環境変数に設定（Workers Scripts:Edit / D1:Edit / Workers KV:Edit スコープ）。
**トークンの発行・登録は Claude 側では行わない**（危害ゲート④セキュリティ）。

復旧後の再開手順（コードは main に入っているので実装は不要）:

```bash
npm run deploy:prod && npm run check:release:prod
```

その後の本番確認項目:
1. `https://clipkeep.net/api/v1/platform-status` の `platforms` が **6→9**、`alsoSupported` が **Discord/Lemon8 の2件**になる
2. `https://clipkeep.net/platform-status` の表に Pinterest / Facebook / Bilibili の行が出る
3. 新3 PF の `uptimePct` は初回は `null`（履歴は 6h cron が積む）

## 4. 本日の重大な発見（次回ループの最優先候補）

本番 `/api/v1/platform-status` の実データで **Twitter/X が 56 サンプル連続 `http=401` / uptime 0%** だった。

```
twitter    limited      http=401 uptime=0% n=56   ← ~14日間ずっと 401
telegram   operational  http=200 uptime=95%
reddit     operational  http=403 uptime=100%
threads    operational  http=302 uptime=100%
bluesky    operational  http=200 uptime=100%
tiktok     operational  http=403 uptime=100%
```

- ローカル（residential IP）からは `https://api.fxtwitter.com/i/status/20` が **200** で正常。
  → **fxTwitter が Cloudflare Workers 側の egress を 401 で弾いている**可能性が高い。
- **`src/lib/extract/twitter.ts:187` は同じ `api.fxtwitter.com` を同じ条件（UA 無し）で叩いている**＝
  **主力プラットフォームである X の抽出が本番で恒常的に劣化している疑い**（`d.fxtwitter.com` / HTML フォールバックが
  効いていれば DL 自体は成功しうるので、未確認）。
- 資産（linkable asset）は正しく異常を可視化していたが、**14 日間だれも読んでいなかった**＝
  **自分のデータ資産を運用の入力に使えていない**というプロセス上の欠落でもある。
- 対応はスコープ外（1日1タスク厳守）とし、**バックログ先頭へ新規項目として追加**した（下記）。

## 5. task.md バックログ更新

- `#3`（発見%＋参照ドメイン数）: 本日分の計測を記録。**参照ドメイン数は API 非対応＝自動化不能**と確定し、
  代理指標（badge Referer 集計）の新規項目へ分割。
- `#4`（プローブ・カバレッジ拡大）: **実装＋push 完了・デプロイのみ blocked**（コードは main 済み）。
- **新規（最優先）**: 「柱1/修復: fxTwitter 401（本番 56/56）の切り分けと X 抽出経路の是正」。
- **新規**: 「柱4' 測定: badge の外部 Referer 集計＝被リンク代理指標の自前化」。
</content>
