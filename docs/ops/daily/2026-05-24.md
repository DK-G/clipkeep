# Daily Growth Log: 2026-05-24

## Data Window
- GA4 period: Last 28 days, generated 2026-05-21T00:02:15.163Z
- Comparison: Previous local growth snapshot
- Fresh export status: Blocked. OAuth refresh token is expired or revoked, and the local service account does not currently have GA4 property permission.

## Observations
- Top page: `/` with 8 views, 4 active users, 7 sessions.
- Growing page: No reliable fresh growth signal because export refresh is blocked.
- Weak funnel step: Measurement consistency. `processing_complete` is 6 while `extract_submit` is 0, so completion-rate decisions are not reliable from the current historical window.
- Error/platform issue: `error_displayed` is 4 from 1 active user. Sample size is too small for platform conclusions.

## Decision
- Today's task: Make analytics export resilient to expired OAuth tokens by falling back to a local service account when available.
- Reason: The growth loop should not stop at OAuth refresh before trying the configured service-account path.
- Expected metric: Successful `growth:review` execution after GA4/Search Console permissions are corrected.

## Change
- Files:
  - `scripts/fetch-ga4-report.mjs`
  - `scripts/fetch-gsc-report.mjs`
- Summary: Added OAuth failure fallback to service-account auth. Added service-account discovery to the GSC export path to match the GA4 export behavior.

## Measurement Follow-up
- Files:
  - `src/lib/analytics/funnel.ts`
  - `src/components/extractor-form.tsx`
  - `src/components/result-client.tsx`
  - `scripts/growth-summary.mjs`
- Summary: Added `extract_attempt` as the canonical funnel start, persisted per-job session attribution, and split result completions into `processing_complete_attributed` versus `processing_complete_direct`. The growth summary now reports direct result/gallery completions outside the submit funnel instead of treating them as failed submit-funnel consistency.

## Form Friction Follow-up
- Files:
  - `src/components/extractor-form.tsx`
  - `src/lib/i18n/types.ts`
  - `src/lib/i18n/locales/home/*.ts`
  - `scripts/growth-summary.mjs`
- Summary: Added localized helper copy under the extractor form, visible blocked-submit reasons for missing URL/security/Instagram maintenance states, and `extract_attempt_blocked` analytics with a reason payload. The growth summary now includes blocked attempts when present.

## Follow-up Date
- Recheck on: 2026-05-25
- Metric to compare: Confirm `npm run growth:review` refreshes `docs/analytics/latest-*` exports, then re-evaluate `extract_submit`, `processing_complete`, and `download_actual_start` consistency.
