# 日次ループ 2026-07-10

本日は戦略バックログ #005 優先2「**柱1: id/hi/tr not-working 本文充足（keyword-map 制作順 1–7）**」の
**制作順 #3 = id / tiktok-not-working の stub 充足**を1デプロイ。

## 選定と KPI ゲート

- 本番健全性: health **200** / sitemap **200**（len=357865）。修復差し替え不要。
  - 注: Bash の `curl` は本環境で exit 43 / `000`（`libcurl bad argument`）を返すため、
    HTTP 実測は PowerShell `Invoke-WebRequest`（StatusCode / 本文文字列一致）で行った（07-09 と同様）。
- 直前バックログ先頭（#005 優先1・柱2 protocolTimeout）は **2026-07-06 実装・デプロイ済み**（ver `83afb184`）。
  効果は次窓観測待ちの監視項目であり実装タスクではない。次順の優先2＝柱1 に着手。
- 柱1（多言語ロングテール SEO）は獲得（indexable 在庫増）系＝Launch-Phase KPI Gate の凍結対象
  （CVR/ファネル微最適化）に非該当＝選択可。
- D1 schema / Cloudflare bindings 変更なし（要人間判断項目に非該当）。
- 発注書: `docs/strategy/keyword-map-id-hi-tr.md` §4 推奨制作順 #3（id/tiktok-not-working）。
  根拠: インドネシアは TikTok 広告リーチ最大（1.8億到達）＝ダウンロード需要も最大級・Monetag tier-3
  （ボリューム最大）。クラスタ B（透かしなし）とも相乗（keyword-map §4 の相乗指摘）。

## 実装（`src/lib/solution-pages/store.ts`）

改修前の `tiktokWorkingGuide.id` は薄い単文 stub（s1「Periksa format URL.」/ s2「Hapus cache.」/
s3「Hanya video publik.」）で、ja/pt/ar（および他 not-working クラスタの telegram/twitter）と比べて
本文が退化していた。

- `tiktokWorkingGuide.id` を **s1/s2/s3 リッチ本文**へ充足（インドネシア語標準表記、ローカル定型クエリを
  自然文に織り込み＝keyword-map §5：`cara download video TikTok` / `tidak bisa di-download` /
  `tanpa watermark` / `kualitas HD`。stuffing はしない）:
  - s1: 最頻原因＝誤ったリンク形式 or 私的アカウント。`tiktok.com/@namapengguna/video/ID` または
    `vt.tiktok.com` 短縮リンク・アプリ内 Bagikan からコピー・私的/友達限定はどの外部ツールでも取得不可
  - s2: 保存失敗/途中停止は TikTok CDN の一時エラー/瞬間的制限＝数分待って再貼り付け・cache 削除・
    末尾 `?...` パラメータ除去で成功率向上
  - s3: 元ファイルを取得＝**MP4 tanpa watermark（TikTok ロゴなし）** を HD 最高画質で・公開動画のみ・
    元が低解像度なら高画質化不可・標準 MP4 で iPhone/Android/PC を変換なしで再生/編集可
  - title `Video TikTok Tidak Bisa Di-download — Solusi Lengkap` / cta `Coba Pengunduh TikTok`
- section 見出し三項に `id` 分岐を追加（s1=`Periksa format URL` / s2=`Hapus cache lalu coba lagi` /
  s3=`Hanya video publik`）＝英語フォールバック "Check the URL format" 等を解消。
- 新規 URL なし（既存 id view の本文充足）＝sitemap 不変。hreflang は既存方針どおり en/ja/pt/ar+x-default
  （id は `?locale=` クエリ形式のまま。path 化は keyword-map §6 のとおり将来の別タスク）。

## ローカル検証

- `npm run typecheck` PASS
- `npm run lint` PASS（No ESLint warnings or errors）
- `npm run build` PASS
- `npm test`（vitest）**33/33 PASS**

## Definition of Done

1. commit `6929fb5` → `git push origin main` 済み（`744c9ee..6929fb5`）。
2. `npm run deploy:prod` 済み。**Version ID `805d757f-cc28-483c-9c38-4dbb0c93b8e8`**。
3. `npm run check:release:prod` → **PASS=29 FAIL=0 SKIP=1**（ベースライン一致。SKIP=1 は Adsterra
   client-side marker で既知）。D1 migration status（remote）PASS。
4. 本番確認（https://clipkeep.net/solution/tiktok-video-downloader-not-working?locale=id）:
   - status **200**
   - title `Video TikTok Tidak Bisa Di-download`（… — Solusi Lengkap）表示
   - 3 section 見出しすべてインドネシア語表示（`Periksa format URL` /
     `Hapus cache lalu coba lagi` / `Hanya video publik`）
   - s1/s2/s3 本文すべて live に描画（`tidak dapat diambil oleh alat eksternal` /
     `cara download video TikTok` / `MP4 tanpa watermark` / `tanpa perlu konversi` を確認）
   - 英語フォールバック見出し "Check the URL format" 残置 = **0**（stub 見出し消失を実証）
   - canonical = 自己参照 path 形式 `.../tiktok-video-downloader-not-working`（`?locale=id` は base canonical へ畳み＝既存方針どおり）

## status: done

- 効果（id/tiktok の indexed/impression 増）は数日〜2週間 lag。次回週次 #006 で GSC 推移を監視。
- **id の残作業**: 本タスクは tiktok スコープを充足。id の他の not-working 系（twitter/telegram 等）は依然
  薄 stub（制作順 #4 id-twitter / #7 id-telegram で順次充足）。tiktok ページの関連リンク nav に他PF の
  id タイトルが薄いまま表示されるのは既知（各PFの制作順ターンで解消）。回帰ではない。
- **ja/pt/ar tiktok 本文の再充足**（現状は単文 stub で telegram/twitter クラスタより薄い）は本タスクの
  スコープ外（1言語×1PF/日 の規律）。別タスク候補として記録。
- 次順（制作順 #4）: **id / twitter-not-working** ← ※下記 §追記の戦略転換により **この続行は停止**。

---

## 追記: 戦略調査と A+B 転換（2026-07-10, ユーザー要請・同日）

日次タスク完了後、ユーザーから「このまま続けて立ち上がる可能性があるか調べよ」との要請。内部実測＋外部一次情報で調査した。

### 調査結果（要点）
- **本日実測（`growth:review`, as of 2026-07-10）**: impressions/28d=**4（横ばい）**・平均順位 33.5→**54.3（悪化）**・sessions=7・ad_script_load/28d=16・「Google 未発見」**68/100**（07-05 は 72）・indexed 29/100。discovery はわずかに動いたが**獲得・順位は不動〜悪化**。
- **crawl budget は誤診**: Google 公式（Mueller/Illyes）＝**URL 数千未満のサイトで crawl budget は非問題**。ClipKeep は 508 URL＝対象外。→ 週次 #005 §7 提案1（sitemap 剪定）は**発見改善策として取り下げ**。
- **真因＝ドメイン権威・被リンク・信頼の欠如**: 新規低権威ドメインの "unknown to Google"/"Discovered not indexed" の最大要因は被リンク（複数一次情報一致・「多くのページは永久に未 index もあり得る」）。**templated（変数だけ違う）多言語ページは 2025-26 品質更新で thin/doorway 判定→未 index 恒常化**。
- **ニッチの権威格差が桁違い**: ssstik.io（権威96・月9,153万訪問）、snaptik（参照ドメイン約8,000・top10 KW 22,005）。
- **急所**: 真因が権威なのに 2026-07-03 で柱4（被リンク）を廃止＝**唯一効くレバーを封じていた**。柱2 も産出ゼロ停止中で速攻口も閉。

### 判断（ユーザー承認）
選択肢 A（linkable asset 転換）＋ B（ホワイトハット権威解禁）を**両方採用**。C（撤退）は不採用。

### 本日準備した成果物（docs-only, no deploy）
- `docs/strategy/linkable-asset-plan.md`（Track A: プラットフォーム別 実稼働 status 資産。既存 `extractor_jobs`＋合成プローブで生成。A-v0→v3 を 1デプロイ単位に分解）
- `docs/strategy/authority-plan.md`（Track B: ホワイトハット限定。B-1 自律／B-2 ユーザー一度きり投稿チェックリスト（貼付文面付き）／B-3 禁止事項＝§6不変）
- `growth-strategy.md` 更新: 決定事項8 追加・§4 に柱1'(linkable asset)/柱4'(ホワイトハット権威)を新設/再開
- `task.md` 更新: templated stub 量産（旧優先2）を**停止**、Track A-v0 / B-1 / B-2 を新・優先1に。crawl budget 剪定タスクは誤診として取り下げ
- メモリ `clipkeep-organic-only-autonomous` を撤回反映で更新

### 次アクション（最小の第一歩）
- **Track A-v0**: 公開 `/platform-status` に合成プローブ表を実装（次の日次デプロイ）。これが被リンクの受け皿。
- **Track B**: A-v0 公開後に埋め込みバッジ（自律）＋ユーザー向け投稿チェックリスト提示。

### KPI 実測（本日 snapshot `growth-2026-07-10T05-59-49Z`, auth `blocked:false` で確認）
sessions/28d=**7**・activeUsers=7・ad_script_load 7d/28d=**9/16**（zone 8/8・err 0）・GSC impressions/28d=**4**（en のみ・pos **54.25**・clicks 1）・indexCoverage(100 sample): indexed **29** / URL unknown to Google **68** / crawled-not-indexed 3。→ 獲得はノイズ域で不動、発見（unknown 72→68）はわずかに改善も順位は悪化。§追記の A+B 転換の裏付け。

---

## 追記2: Track A-v0 ＋ B-1 実装・デプロイ（同日・ユーザー「②まで進めて」指示）

戦略転換の準備後、ユーザー指示で **Track A-v0（linkable asset 第一弾）＋ Track B-1（link magnet 自律分）** を実装し DoD 完遂。

### 実装
- `src/lib/platform-status/probes.ts`（新規）: 各対応PFの上流エンドポイントへ合成プローブ。判定閾値は
  リリースゲート（`prod_release_check.ps1`）の ExpectedList を流用＝**"Operational" の定義を release gate と一致**
  させた。結果は **KV `TREND_KV` に ~10分キャッシュ**（第三者への連打を防止）。overallStatus ロールアップ付き。
- `app/platform-status/page.tsx`（新規）: 公開 status ページ。overall バナー＋PF別テーブル（Platform/Upstream/
  Status/HTTP/Latency）＋方法論注記（403/429 は datacenter IP への正常応答＝down ではない旨を明記＝計測の正直性）
  ＋内部リンク（not-working 系）＋`schema.org/Dataset` JSON-LD＋**OG/Twitter カード（B-1）**。英語専用資産のため
  canonical は常に英語 URL。
- `app/platform-status/badge/route.ts`（新規, B-1）: 埋め込み用 shields 風 **SVG ステータスバッジ**
  （overall を色で表現）。ページに HTML/Markdown の貼付スニペットも掲載＝**貼られると自然被リンク**。
- `app/sitemap.ts`: `/platform-status` を英語専用エントリで追加（changeFrequency hourly）。
- **D1 スキーマ / bindings 変更なし**（KV キャッシュのみ）。

### ローカル検証
- typecheck / lint / build PASS・vitest **33/33** PASS。

### Definition of Done
1. commit `fd20fb1` → `git push origin main` 済み（`dac4d89..fd20fb1`）。
2. `npm run deploy:prod` 済み。**Version ID `bdc6b054-c162-4205-b250-862122f86a38`**。
3. `npm run check:release:prod` → **PASS=29 FAIL=0 SKIP=1** ＋ D1 remote PASS（ベースライン一致）。
4. 本番確認:
   - `https://clipkeep.net/platform-status` **200**・canonical 自己参照（英語）・PF別ライブデータ描画
     （**Twitter/X=Limited(HTTP 401)** / Telegram=Operational(200) / Reddit=Operational(403＝想定内) /
     Threads=Operational）・`Dataset` JSON-LD・Embed セクション・方法論注記あり。
   - `https://clipkeep.net/platform-status/badge` **200** / `image/svg+xml` / SVG 873B / 左ラベル `clipkeep`。
   - `https://clipkeep.net/sitemap.xml` に `/platform-status` 収録を確認。
5. 本記録（daily 2026-07-10 追記2）。

### status: done（A-v0 ＋ B-1）
- **残: Track B-2（ユーザー一度きり投稿チェックリスト）** ＝ asset 公開済みのため次に提示可能（AlternativeTo /
  awesome PR / 関連 subreddit / Show HN、貼付文面付き）。これはユーザー操作が要る唯一の分。
- A-v1（cron 定期プローブ＋7日 uptime 履歴・KV）と A のカバレッジ拡大（TikTok 等の上流プローブ追加）は後続。
- 注: X が Limited(401) は fxtwitter API/上流の一時応答（datacenter IP 起因の可能性）。ページは観測値を正直表示。

---

## 追記3: Track A-v1 実装・デプロイ（同日・ユーザー「次に進んで」指示）

Track A-v1（rolling uptime 履歴）を実装し DoD 完遂。**既存 6h cron に HTTP-only の別経路を追加**し、
柱2 の browser/auto-trend 障害系には一切触れない設計。

### 実装
- `src/lib/platform-status/probes.ts`: `getStatusHistory`/`computeUptime`/`probeAndRecord` を追加。
  cron が `probeAndRecord()` で fresh プローブ→`platform-status:v1` cache 更新＋`platform-status:history`
  に compact サンプルを追記（末尾 **56件=約14日分@4/日** で trim）。empty-safe。
- `app/api/admin/status-probe/route.ts`（新規）: secret ガード（auto-trend と同型）の HTTP-only エンドポイント。
  `waitUntil` でバックグラウンド実行。
- `src/worker.ts`: scheduled() に **独立した2本目の waitUntil** で `/api/admin/status-probe` を呼ぶ
  （auto-trend 呼び出しは不変・失敗が相互に影響しない）。
- `app/platform-status/page.tsx`: **Uptime 列**を追加（直近N自動チェックの operational 率、n を正直表示）。
  履歴ゼロ時は「collecting」注記。
- **D1 スキーマ / bindings 変更なし**（KV のみ）。

### ローカル検証
- typecheck / lint / build PASS・vitest **33/33** PASS。

### Definition of Done
1. commit `764ff33` → `git push origin main` 済み（`1662b1a..764ff33`）。
2. `npm run deploy:prod` 済み。**Version ID `8850a6d8-2798-433c-bc66-1e534cdf9bc9`**。
3. `npm run check:release:prod` → **PASS=29 FAIL=0 SKIP=1** ＋ D1 remote PASS。
4. 本番確認:
   - `GET /api/admin/status-probe`（secret なし）= **401**（ガード動作）。
   - `/platform-status` **200**・**Uptime 列**描画・履歴ゼロにつき「Uptime history is being collected」注記
     （empty-safe 経路が正しく動作）。
   - `/platform-status/badge` **200**・image/svg+xml。
   - CRON_SECRET は Cloudflare Secret のため手動トリガー不可＝**uptime データは次以降の 6h cron 窓で自動蓄積**。
5. 本記録。

### status: done（A-v1）
- uptime% は次 cron 窓（0/6/12/18 UTC）から蓄積開始。数サンプル貯まれば per-PF uptime が表示される。
- 次: **Track B-2 チェックリスト提示**（ユーザー操作分）。カバレッジ拡大（TikTok 等上流追加）は後続の A 改善。

---

## 追記4: Track B 実地結果 ＋ Track A カバレッジ拡大（同日）

### Track B-2 実地（ユーザーが実際に試行）＝重要な学び
- **① Show HN**: 新規アカウントは Show HN 投稿不可（`/showlim`＝アンチスパム。karma 蓄積＝継続手作業前提）
  ＝ユーザー方針（継続手作業回避）に反する→**見送り**。
- **② AlternativeTo**: 新規登録は**アカウント7日経過が必要**（"try again after July 17, 2026"）。ただし
  クローズド/ホスト型ツールも掲載可＝**ClipKeep は正当な適合**→ **7/17 に1回だけ**やれば良い（一度きり）。
- **⑤ awesome リスト**（awesome-video-downloaders を実調査）: **OSS 優先・GitHub リポジトリ URL 前提・
  広告型は非掲載**＝ClipKeep（クローズド/広告/公開リポなし）は**適合せず**。押し込みは §6 抵触のため**行わない**。
- **結論（正直）**: 「ゼロから冷たく登録して回る」手法は各サイトのアンチスパムで構造的に弾かれる。有効なのは
  (a) 適合する②を 7/17 に1回、(b) **アセット自体で自然リンク/バッジ埋め込みを稼ぐ（自律・本命）**、
  (c) ユーザーが本当に参加したい所だけ地道に。→ authority-plan.md B-2 に実地結果を反映。

### Track A: プローブ・カバレッジ拡大（自律・本命側の強化）
- `probes.ts` の `ProbeTarget` を **複数 candidate 上流**対応へ一般化（best-of: operational>limited>down）。
- **Bluesky 追加**（`public.api.bsky.app` の clean な公開 API＝正確なプローブ）。
- **TikTok 追加**（公式単一 endpoint 無し＝実際に使う **TikWM/Lovetik fixers** を複数プローブし
  **いずれか到達で operational**）。方法論注記に fixer 方式を明記＝計測の正直性維持。
- typecheck/lint/build/vitest 33 PASS。

### Definition of Done（カバレッジ拡大）
1. commit `f9d1b13` → push（`c60e1ad..f9d1b13`）。
2. `deploy:prod` → **Version ID `b574132e-9921-4a43-aae9-2dcbe4290d21`**。
3. `check:release:prod` → **PASS=29 FAIL=0 SKIP=1** ＋ D1 remote PASS。
4. 本番確認: `/platform-status` **200**・**6行**（Twitter/X・Telegram・Reddit・Threads・**Bluesky**・**TikTok**）
   描画・upstream ラベル（`bsky public API` / `TikWM / Lovetik fixers`）・fixer 方法論注記・
   also-supported が `Pinterest, Facebook, Bilibili, Discord, Lemon8` に更新を確認（Down:0）。
5. 本記録。

### status: done（Track A カバレッジ拡大）
- **本日の総括**: 日次(id-tiktok) ＋ 戦略転換(A+B) ＋ A-v0/B-1/A-v1/カバレッジ拡大 を実装・デプロイ。
  Track B は実地で「冷たい登録は構造的に不可」と判明し、**アセット主導の自然リンク獲得（自律）**へ重心を移す
  のが正しいと確認。ユーザー操作は **7/17 の AlternativeTo 登録1回**のみ（任意）。
