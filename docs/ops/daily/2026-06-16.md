# Daily Ops Log — 2026-06-16

status: done

## 選定タスク
計測/柱1（戦略バックログ **#2**）: **GSC カバレッジ取得（indexed 実数化）**。`growth:review` に URL Inspection API 由来の indexed カウントとインデックス除外理由（coverageState）の一次データを追加する。

2026-06-15 週次レビュー #002 で先頭近くに再配置されたタスク。律速はインデックス/クロール反映の遅延と推定されていたが、その**実数**が未取得だった（フェーズゲート indexed≥50 判定に必須）。Launch-Phase KPI Gate 上「sitemap/coverage 整備」は選択可（CVR 微調整凍結の対象外）。

前日（06-15）引継ぎの宿題「既存スコープ（`webmasters.readonly`）で URL Inspection API が叩けるか要確認」を本タスク冒頭で検証 → **叩けることを確認**（read-only スコープで `urlInspection/index:inspect` が 200 を返す）。

## 健全性チェック（実装前）
- `GET https://clipkeep.net/api/v1/health` => **200**（db ok / extractor ok / degraded=false）
- `GET https://clipkeep.net/sitemap.xml` => **200**（357,865 bytes / 508 `<loc>`、dedup 後 500 URL）
- 破損なし → 修復差し替え不要。
- 補足: 当環境では bash の `curl` がネットワーク不通（000）。Node の `fetch` と PowerShell の `Invoke-WebRequest` は疎通するため、健全性チェックとスクリプトは Node fetch / PowerShell を使用。

## スコープ判断（read-only で何ができるか）
現行 OAuth トークンのスコープ: `analytics.readonly` + `webmasters.readonly`。
- **URL Inspection API（`urlInspection/index:inspect`）**: `webmasters.readonly` で利用可 → **本タスクの主目的（indexed 実数化＋除外理由取得）は read-only で完遂可能**。
- **sitemap 再送信（`sitemaps.submit`）**: 書込スコープ `webmasters`（full）が必要 → read-only トークンでは不可。**GSC UI から手動再送信が必要**（要人間操作。下記「引継ぎ」参照）。本タスクは「indexed 実数化」を主目的とし、再送信は手動アクションとして分離。

## 実装内容
1. **`scripts/fetch-gsc-index-coverage.mjs`（新規）**: 本番 sitemap.xml を取得→`<loc>` を全抽出→各 URL を URL Inspection API で検査し、`docs/analytics/latest-gsc-index-coverage.csv`（url/verdict/coverageState/robots/indexing/pageFetch/lastCrawl/google・userCanonical/canonicalMatch/error）と `docs/analytics/latest-gsc-index-coverage-summary.json`（indexed 数・coverageState 内訳・canonical 不一致・blocked・フェーズゲート判定）を出力。
   - 認証は `fetch-gsc-report.mjs` と同じ OAuth(read-only)/サービスアカウント方式を踏襲。プロパティ解決も同ロジック（`sc-domain:clipkeep.net`）。
   - 並列度（既定6）と件数制限（`GSC_INSPECT_LIMIT`、既定 **100**＝日次サンプル、`0`＝全件スイープ）を環境変数で制御。429 はバックオフ再試行。**サイレント打ち切り禁止**のため「inspected N of M（limit=…）」をログ出力し、サンプル時は summary に「lower bound」と明記。
2. **`scripts/run-growth-review.mjs`**: GSC エクスポートの後に index-coverage 取得を追加（既存同様 try/catch でスキップ耐性、失敗しても summary は継続）。
3. **`scripts/growth-summary.mjs`**: `printIndexCoverage` を追加し **📚 INDEX COVERAGE（URL Inspection API）** ブロックを出力（indexed/inspected、フェーズゲート indexed≥50 判定、coverageState 内訳、canonical 不一致、blocked）。snapshot.searchConsole に `indexCoverage` を追加。
4. **`package.json`**: `analytics:gsc:coverage` スクリプトを追加。

ガードレール遵守: クローキング/ドアウェイなし、TikTok extractor 不変更、Instagram noindex 不変更、レート制限・広告ゾーン構成不変更、**D1 schema / Cloudflare bindings 不変更**、戦略文書不変更、法務導線不変更。広告・送信系の write 操作なし（read-only 計測のみ）。

## 検証
- `npm run typecheck` ✅ / `npm run lint` ✅（0 warnings）/ `npm run build` ✅
- `node --check` で新規・改修スクリプト3本の構文 ✅
- **`node scripts/fetch-gsc-index-coverage.mjs`（全件スイープ, limit=0）✅** — 本番プロパティ `sc-domain:clipkeep.net` に対し live 500 URL を検査、errors=0。実データで動作確認。
- `node scripts/growth-summary.mjs` ✅ — INDEX COVERAGE ブロックが summary JSON を読んで正しく描画。
- `npm run check:release:prod` ✅ — **PASS=29 FAIL=0 SKIP=1**（SKIP は Adsterra マーカーのクライアント注入で既知の想定内）。※初回 2 回は transient（wrangler D1 status 読取りflake／外部依存プローブ flake）で再実行クリーンPASS。D1 remote は `migrations list` 直叩きで「No migrations to apply」確認。

## 本番ライブ確認（https://clipkeep.net）
本タスクは **Cloudflare worker/サイトコードを一切変更しない計測ツール**のため、新規の本番レンダリング面は無い（→ `deploy:prod` は該当なし＝no-op redeploy 回避）。代わりに「機能のライブ面」＝**本番プロパティと本番 sitemap に対する URL Inspection 実行結果**を本番で直接確認した:

| 確認項目 | 結果 |
|---|---|
| 本番 sitemap 取得 | **200** / 500 URL 抽出 |
| URL Inspection API（read-only スコープ） | **200**（`sc-domain:clipkeep.net`、live 500 URL 検査・errors=0） |
| 本番リリースゲート | **PASS=29 / FAIL=0 / SKIP=1**（prod 健全・本変更で不変） |

## KPI（本日時点・実測）

### ⚠️ 重要な訂正（2026-06-16 追記 / ユーザー提供の GSC スクショで判明）
当初この欄に「indexed=20 / フェーズゲート indexed≥50 **未達**」と記載したが**誤り**。私の URL Inspection スイープは**現 sitemap の新パス URL 500件だけ**を母数にしており、これは「ゲート指標としての indexed 数」ではなく **新パス URL の発見率** を測っていた。GSC「ページのインデックス登録」レポート（正本、最終更新 2026-06-12）の実数は以下:

| GSC 正本（全既知 URL 254件が母数） | 値 |
|---|---:|
| **登録済み（indexed）** | **172** |
| 未登録 | 82 |
| └ 見つかりませんでした(404) | 27 |
| └ 代替ページ(適切な canonical あり) | 18 |
| └ クロール済み-インデックス未登録 | 29 |
| └ アクセス禁止(403) | 3 |
| └ 重複・正規ページ未選択 | 3 |
| └ ソフト404 | 2 |
| └ 検出-インデックス未登録 | 0 |

→ **フェーズゲート indexed≥50 は実は「✅ クリア済み」（172）**。Phase L が続くのは **impressions(28d)=15 « 1,000** の側のみ。

### URL Inspection スイープ（新パス URL 500件の発見率）
| 指標 | 値 |
|---|---:|
| 検査 URL（現 sitemap） | 500 |
| indexed（verdict=PASS） | 20 |
| URL is unknown to Google | 477 |
| Crawled - currently not indexed | 3 |
| canonical 不一致 / blocked | 0 / 0 |

locale prefix 別: **en 19 indexed / 104 unknown、ja 0/125、pt 0/124、ar 1/124**。indexed 20件は中核 canonical（`/`・`/download-*-video`・`/trending`・`/about`・`/faq`・`/legal/*`・古参 solution 4本）のみ。

### 突き合わせ（GSC 172 と スイープ 20 の整合）
- GSC「登録済み 172」の大半は **06-12 に sitemap から外した旧 `?locale=` URL**（現 sitemap に無く、スイープ対象外）。「代替ページ(canonical あり)18」もこの旧 URL 群。
- スイープ対象の**新パス URL（特に ja/pt/ar 約375本）は 100% 近く Google 未発見**。GSC「検出-インデックス未登録=0」も併せ、新 URL は**発見キューにすら入っていない**＝sitemap が新 URL のクロールを駆動できていない（内部リンク薄く孤立化＋sitemap 最終読込が刷新前の疑い）。
- **真の律速**: 「コンテンツ品質」でも「indexed 数不足」でもなく、**新パス URL（特に ja/pt/ar）の発見・移行**。登録済み 172 は旧・低意図 URL で表示回数ほぼゼロ、狙った高意図ページが未発見のまま。canonical 不一致 0 は OPS-1 の canonical 修正が機能している裏付け。

## 引継ぎメモ
- バックログ **#2 のうち「indexed 実数化＋除外理由の一次データ取得」を完遂**。`growth:review` で恒常可視化（日次は既定 100 サンプル、週次は `GSC_INSPECT_LIMIT=0` で全件スイープ推奨）。**ただしスイープの indexed 数は「新パス URL の発見率」であり、ゲート指標の indexed 数は GSC 正本（=172）を使うこと**（混同しない。上記「重要な訂正」参照）。週次 KPI 履歴表「GSC indexed」には **172**（GSC 正本, 06-12 時点）を記録。
- **フェーズゲート再判定**: indexed≥50 は **172 でクリア済み**。Phase L 継続は impressions(28d)=15 « 1,000 の側のみ。撤退基準は非該当。
- **#2 残（要人間操作）**: sitemap 再送信は書込スコープ未付与のため自律不可。**GSC UI で sitemap を再送信**（または `analytics:ga4:login` を `webmasters`(full) スコープ込みで再認可すれば API 化可能）。「検出-未登録=0」を踏まえると再送信は発見キュー投入に有効（昨日「低レバレッジ」と書いたが上方修正）。
- **次の実行可能タスク（ユーザー承認済み = バックログ #3）**: ja/pt/ar 約375本＋en 104本が Google 未発見＝**孤立ページ化の疑い**。home / solution 一覧 / 関連から新パス URL（特に ja/pt/ar）へ被リンクし crawl 発見性を上げる **内部リンク強化**。本日の一次データが #3 を最優先として裏付け。#2 残は人間操作待ちのため次ループは #3 を選ぶこと。
- 補足確認候補: GSC「サイトマップ」の最終読込日、「404=27」の中身（旧 URL 整理要否）。
- 補足: URL Inspection 日次クォータ 2000/プロパティ。全件 500 スイープは約 5 分（429 バックオフ込み）。日次 growth:review は既定 100 件で軽量運用。

## 追記: sitemap 手動再送信（2026-06-16, ユーザー操作）
- ユーザーが GSC UI（`sc-domain:clipkeep.net` → インデックス作成 → サイトマップ）で `sitemap.xml` を**手動再送信** → 「サイトマップを送信しました」確認。**#2 残の人間操作（再送信）を消し込み完了**（書込スコープ未付与で自律不可だった部分）。
- **結果（GSC サイトマップ画面で確認）**: 送信日 2026/06/16・**最終読み込み日時 2026/06/16（＝当日に Google が再読込）**・ステータス **成功しました**・**検出されたページ数 508**・検出動画 0。
  - 送信直後（成功ダイアログ表示中）の行は再処理前の古い値「検出 127」を表示していたが、ダイアログを閉じた後は **127 → 508** に更新。**新パス URL の発見ギャップ（実体508 vs 旧読込127、約381本未検出）が当日中に解消**。URL Inspection スイープの「unknown 477/500（新パス ja/pt/ar 未発見）」が再送信で是正された裏付け。
  - **注意: 「検出されたページ数 508」は discovered であり indexed ではない**。発見はクロール→インデックスの第一歩。実インデックス化は数日〜2週間。次回週次は GSC「ページのインデックス登録」レポートで **新パス（ja/pt/ar）の登録済み件数が増えるか**を監視（発見済み508のうち何件が index に昇格するか）。
- ライブ再確認（再送信前）: `GET /sitemap.xml` 200 / 508 `<loc>`、新パス `GET /ja/download-twitter-video`・`/pt/download-tiktok-video` ともに **200・自己参照 canonical 正**。ページ実体は健全で「発見されていないだけ」を再確認。
- 次タスクは **#3 内部リンク強化**（孤立解消をコード側からも後押し＝発見済みURLのクロール→index 昇格を促進、ループ自律実行可）。
