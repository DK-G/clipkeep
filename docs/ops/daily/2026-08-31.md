# 2026-08-31 日次グロースループ

status: **done**（実装＋push＋本番デプロイ＋本番検証まで完遂）
選択タスク: **バックログ優先1**（計測: 非ブランド impressions／page 次元 impressions／`session_start`–`page_view` 乖離）
デプロイ Version ID: **`15b8725c-1c2a-40b1-8dee-8b003f2f12a4`**
commit: `41c6038`（実装）／このログ・task.md 更新は後続コミット

## 1. 健全性チェック（`npm run check:loop`）

| 対象 | 結果 |
|---|---|
| `npx wrangler whoami`（デプロイ経路） | **認証済** |
| `https://clipkeep.net/api/v1/health` | **200** |
| `https://clipkeep.net/sitemap.xml` | **200**（`<loc>` = 509） |
| `https://clipkeep.net/api/v1/platform-status` | 6 platforms・劣化 1 件（twitter 0%） |

VERDICT: OK with 1 warning（twitter `limited` / http=401 / uptime 0% n=56 が継続＝約 52 日目。バックログ優先2 の対象）。

★★**この日の最大の変化はチェック①**＝**wrangler が認証済に戻っていた**。
デプロイ経路の断絶は **07-24 の `3bd3cb6` 以降 38 日間** 続いており、#008〜#010 が 3 週連続で
「実務上の律速は戦略ではなくデプロイ断絶」と書いていた当のブロッカーである。認証が生きている以上、
**DoD のデプロイ工程を回すこと自体が同時に優先0(b) を閉じる**ため、この日は本番デプロイまで実行した。

## 2. 実装（優先1・デプロイ不要枠）

`scripts/growth-summary.mjs` に 3 指標を追加。**いずれも取得済みエクスポートの再集計のみ＝新規 API 呼び出しゼロ**。

1. **非ブランド impressions**（`latest-gsc-query-pages.csv`）
   ブランド判定は「空白・記号を正規化して製品名 `clipkeep` を含むか」だけを見る監査可能な規則。
   `tubekeep` のような別名は**非ブランド demand として計上**する。ゼロのときは
   「撤退カウンタをブランドクエリでリセットするな（growth-strategy §7）」を明示警告。
2. **page 次元 impressions**（`latest-gsc-pages.csv`）＝ロケール別＋ position 帯（1-3 / 4-10 / 11-20 / 21+）。
   query 次元はプライバシー閾値で欠落するため過小になる。
3. **`session_start` と `page_view` の乖離**（`latest-ga4-events.csv`）＝比率が 10% 未満なら毎回警告。

`growth-summary.mjs` は**プロセスのエントリのときだけ `main()` を実行**するようにし、純集計関数を
importable にした。`scripts/growth-summary.test.ts` に 11 ケース追加（**vitest 73 → 84 pass**）。

### 実測（キャッシュ済みデータ＝GA4 2026-08-15 エクスポート）

- 非ブランド **1 impression**（`tubekeep` pos 56）／ブランド **6**＝**非ブランド比 14%**。
  ＝#010 の「query 次元は全件ブランド」は**厳密には 6/7 がブランド**で、残り 1 も別サービス名の誤入力。
  **戦略の成否を測れる query 側の信号は事実上まだ 0 である**という結論は変わらない。
- page 次元 **26 impressions**（query 次元 7 の **3.7 倍**）＝内訳 en 15 / **pt 6（pos 4.2）** / ja 3 / es 1 / hi 1。
  position 帯は 1-3: 3 / 4-10: 13 / 11-20: 9 / 21+: 1。**pt の pos 4.2 は #009・#010 と 3 週連続で再現**。
- `session_start` 223 に対し `page_view` **4（1 ユーザー）＝1.8%** → `humanShaped: false` 警告が出る。

## 3. ローカル検証

| コマンド | 結果 |
|---|---|
| `npx tsc --noEmit` | PASS（出力なし） |
| `npm run lint` | PASS（No ESLint warnings or errors） |
| `npm run build` | PASS |
| `npx vitest run` | **84 passed / 8 files** |
| `node scripts/growth-summary.mjs` | 3 ブロックが出力に出現・snapshot JSON に 3 指標が保存されることを確認 |

## 4. Definition of Done

| 工程 | 結果 |
|---|---|
| 1. ローカル検証 | ✅ 上表 |
| 2. main へ commit・`git push origin main` | ✅ `735a231..41c6038` |
| 3. `npm run deploy:prod` | ✅ **Version ID `15b8725c-1c2a-40b1-8dee-8b003f2f12a4`** |
| 4. `npm run check:release:prod` | ✅ **PASS=29 FAIL=0 SKIP=1**（SKIP は Adsterra marker が SSR HTML に出ない既知の項目）＋ D1 migration status PASS |
| 5. 本番の変更箇所を直接確認 | ✅ 下記 |
| 6. Version ID と本番確認をログに記録 | ✅ 本ファイル |

### 本番確認（https://clipkeep.net）

今日の変更自体はローカルの計測スクリプト（デプロイ非依存）なので、本番確認は
**同じデプロイで初めて本番へ届いた `3bd3cb6`（Track A プローブ 6→9 PF）** に対して行った。

```
GET /api/v1/platform-status  → platforms = 9（従来 6）
  twitter limited 401 uptime 0% / telegram operational 200 100% / reddit operational 403 100%
  threads operational 302 100% / bluesky operational 200 100% / tiktok operational 403 100%
  pinterest operational 404 / facebook operational 200 / bilibili limited 412   ← 新規3件
  alsoSupported = ["Discord","Lemon8"]
GET /api/v1/health   → 200
GET /sitemap.xml     → <loc> 509
```

＝**バックログ優先0(b) の成功指標（platforms 6→**9**・`alsoSupported` 2 件）を本番で充足**。

## 5. この日で状態が変わったこと（次のループへの申し送り）

- ★**デプロイ断絶は解消**（38 日ぶりに本番反映）。#008〜#010 が上位に集めていた「デプロイ不要枠」という
  制約は**もう効いていない**。次回以降は本番反映を要するタスク（優先4 pt 内容深化・優先10・優先11）を
  普通に選んでよい。
- ★**優先0(b) は完了**。残るユーザー操作は **(a) 日次ループの再有効化**（本日は手動発火で回った）と
  **(c) 優先9 用 KV namespace 作成**の 2 点。
  ※ただし本番 bindings 一覧に `env.TREND_KV`（`ab21329da7a143a29cf34e2d9621d1b3`）が**既に存在する**ことが
  今回のデプロイ出力で確認できた。優先9（badge Referer 集計）は**新規 namespace ではなく既存 TREND_KV に
  相乗りできる可能性がある**＝「binding 追加が必要」という 07-28 判定の前提が変わりうる。次回の週次で再判定すること。
- twitter 401 は**約 52 日継続**。優先2（401 の明示分類）は実装・テストが本日時点で**デプロイ経路も含めて
  完全に自律実行可能**になった＝次のループの第一候補。
- 非ブランド impressions は 1（実質 0）。**撤退カウンタはブランドクエリでリセットしてはならない**という
  #010 の指摘は、今日から自動で毎回可視化される。
