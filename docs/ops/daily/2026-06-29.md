# 日次ループ 2026-06-29

- status: done
- 柱/種別: 柱1（発見） — 内部リンク欠落の解消（06-28 path 形式統一の積み残し）
- バックログ: #004 翌週 優先1「06-28 path 形式統一の効果測定＋残る内部リンク欠落の解消」

## 選定理由

週次レビュー #004（同日 2026-06-29 実施・`docs/ops/weekly_review_2026-06-29.md`）で並べ替えた
翌週バックログの先頭。律速は「発見/インデックス遅延」へ復帰しており、URL Inspection で
新パス(ja/pt/ar)の **72/100 が「Google 未発見」**＝発見が時計を握る。06-28 に出荷した
内部リンクの path 形式統一（ホーム grid＋side-menu download 10 本）の **積み残し** を解消する。

Launch-Phase KPI Gate 適合: 発見/内部リンク系で凍結対象（CVR/funnel 微調整）ではない。

## 実装（2 デプロイ）

### デプロイ1 — ver `054cf49e-2167-4d58-85f5-b859f9d779bf`（commit `e4f4147`）
1. **side-menu に telegram を追加**（`src/components/side-menu.tsx`）。downloads リストは
   bilibili/bluesky/discord/facebook/lemon8/pinterest/reddit/threads/tiktok/twitter の 10 本で、
   **telegram のみ欠落**（dict `t.telegram` は存在・ホーム grid にも在る live indexable ページ）。
   reddit と threads の間に追加（getLocalizedPath で path 形式）。
   - **instagram は意図的に除外を継続**: `app/download-instagram-video/page.tsx` は
     `robots:{index:false}` の「Under Maintenance」ページ。戦略ガードレール「Instagram noindex 維持」
     に従い、発見メニューへ載せない（ホーム grid から外れているのも同理由で整合）。
     task.md の「telegram/instagram 欠落追加」のうち instagram 分は **不採用**（noindex 維持が正）。
2. **solution 本文の関連リンク/CTA/戻る導線を path 形式化**（`src/components/solution-content-client.tsx`）。
   `?locale=` クエリ生成だった `buildUrlWithLocale` を正本 `getLocalizedPath` へ置換。
   ja/pt/ar は `/ja/solution/...` の path 形式となりページ自身の canonical/sitemap と一致
   （query 形式はクローラーが新パスを辿れず未発見の主因の一つ）。es/fr/de 等は従来どおり English へ畳む。

### デプロイ2 — ver `7c6cc36b-f87a-4057-afd8-7e8969981306`（commit `bbceebe`）
ライブ検証（デプロイ1後）で **全ページの footer** が solution ガイドを `?locale=` で
リンクしている残置を発見（path-canonical と不一致）。ホーム guides grid も同様。
3. **グローバル footer の `buildUrl` を getLocalizedPath 化**（`src/components/footer.tsx`）。
   footer は全ページの内部リンクハブ＝効果大。about/faq/legal/status/solution すべてが
   06-19 で path-based canonical（buildLocaleAlternates）に統一済みのため、リンク側も
   path 形式へ揃えて一致させる。
4. **ホーム guides grid の 8 件の solution リンクを getLocalizedPath 化**（`app/page.tsx`）。
   ホーム＝最高権威ページからの solution 被リンク。

## ローカル検証
- 各デプロイで `npm run typecheck` / `npm run lint` / `npm run build` PASS。
- D1 schema / Cloudflare bindings 変更なし・新規 URL なし＝sitemap 不変。

## リリースゲート（本番）
- デプロイ1: `check:release:prod` **PASS=29 / FAIL=0 / SKIP=1**、D1 remote PASS。
- デプロイ2: 初回 FAIL=2（外部上流プローブの一過性 — 既知のフレーク）→ 再実行 **PASS=29 / FAIL=0 / SKIP=1**、D1 remote PASS。

## 本番ライブ検証（https://clipkeep.net）
- `/ja/solution/telegram-video-downloader-not-working` 200・canonical=`/ja/solution/telegram-video-downloader-not-working`。
  - 関連リンク nav が **path 形式**（`/ja/solution/twitter-...`, `/ja/solution/reddit-...`,
    `/ja/solution/tiktok-...`, `/ja/solution/how-to-save-on-iphone-android`, `/ja/solution/best-quality-...`）。
  - side-menu download に **`/ja/download-telegram-video`** が出現（telegram 追加確認）。
  - CTA=`/ja/download-telegram-video`・戻る=`/ja`（path 形式）。
  - 残る `?locale=` は **言語スイッチャー**（`/solution/...?locale=xx` 全 10 言語）のみ＝仕様どおり不変更。
- `/ja` ホーム 200。
  - guides grid 8 件すべて **`/ja/solution/...`**（path 形式）。
  - footer の solution ガイド `/ja/solution/how-to-save-on-iphone-android` ＋ `/ja/about`・`/ja/faq`・
    `/ja/legal/{terms,privacy,cookies,dmca}` すべて path 形式。
  - **solution への残置 `?locale=` リンク = 0**（デプロイ前にあった footer 残置を消去）。
- 健全性: `/api/v1/health` 200（db/extractor ok・degraded=false）・`/sitemap.xml` 200。

## 効果測定（06-28＋本日分）
- 直近の実測は #004（本日）: URL Inspection 「Google 未発見」72/100、GSC impressions/28d=3
  （旧 `?locale=` URL 全廃＋301 の移行ディップ）、初の実クリック 1（en, pos 23.3）。
- 内部リンク→発見→index 昇格は **数日〜2週間 lag**。本日の path 統一は 06-28 の発見対策を
  footer（全ページ）・ホーム・solution 本文へ拡張するもので、効果は次回週次（#005）で
  「未発見%」低下・新パス indexed/impression 増として帰属・監視する。

## 残課題（次候補・低クロール価値）
- downloader クライアント 4 本（sns/telegram/tiktok/twitter）の help リンク `/solution/{slug}?locale=`、
  および `extractor-form.tsx` / `result-client.tsx` の status 連動 help リンク（動的描画・低クロール価値）の
  path 形式化。今回は最高価値の footer＋ホームを優先し、今日デプロイできる単位に絞った。
