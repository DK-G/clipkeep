# 日次ループ 2026-07-13

status: done

## 選定タスク

戦略バックログ #006 優先1: **柱2 起動タイムアウトの緩和（30s→60s）**（柱2/修復・無課金・可逆・自律・D1/bindings 非依存）。

- KPI Gate（Launch-Phase）: 本タスクは CVR/funnel 微調整ではなく柱2 の trend-page パイプライン修復＝**凍結対象外**。
- 上位の A+B（Track A/B）は本命だが、優先1 が柱2 修復として先頭。本日ループはこの1件を消化。

## 背景（律速の推移）

- 07-06 `launchBrowserWithRetry`（各試行30sバウンド×3＋生存確認, ver `83afb184`）で **protocolTimeout の 180s ハングは除去**（`launchAttempts:3` で fail-fast 成立）。
- しかし産出ゼロは継続し、律速が `browser_launch exceeded 30000ms`（3試行とも 30s 超過・非429・取得予算空き＝コールドローンチのレイテンシ超過）へ移行（週次 #006 2026-07-12, heartbeat `2026-07-12T00:02Z`）。
- 週次 #006 の処方箋どおり、各試行のバウンドを **30s→60s** へ緩和し正常起動に十分な時間を与える。

## 実装

- `src/lib/auto-trend.ts`: `LAUNCH_TIMEOUT_MS = 30_000` → `60_000`。理由をコメントに追記（180s ハングは修正済み＝30s の厳格化は過剰、429 でも予算枯渇でもないためコールドローンチに時間を与える）。
- 挙動非変更の他要素（MAX_LAUNCH_ATTEMPTS=3・生存確認 LIVENESS_TIMEOUT_MS=10s・自己 close リーク防止）は不変。
- D1/bindings/sitemap/戦略文書 いずれも不変更。

## 検証（ローカル）

- `npm run typecheck` PASS
- `npm run lint` PASS（No ESLint warnings or errors）
- `npm test`（vitest）**33/33 PASS**
- `npm run build` PASS（全ルート従来どおり ƒ/○/● 生成、Middleware 32.6 kB）

## Definition of Done

1. commit `3ecf807`（main）→ `git push origin main` 完了（`3718fbc..3ecf807`）。
2. `npm run deploy:prod` 完了。**Version ID: `34b713c7-d528-47d6-818d-6c8321c1ce56`**。bindings 確認（`env.browser_rendering→Browser`・`env.TREND_KV ab21329d…`・`env.clipkeep_db`）、cron schedule `0 */6 * * *` 確認。
3. `npm run check:release:prod` **PASS=29 / FAIL=0 / SKIP=1**（SKIP=Adsterra marker は client-side・従来同）、**D1 migration status check (remote) PASS**、Release gate passed。

## 本番ライブ検証（https://clipkeep.net）

- `GET /api/v1/health` → **200**（db/extractor ok・degraded=false）
- `GET /sitemap.xml` → **200**・`<loc>`=**509**・`/trend/` エントリ=**0**（gate-passing トピック不在を正しく withhold＝ゲート正常）
- `GET /trending` → **200**
- ※当環境の `curl` は libcurl バグで 000 を返すため PowerShell `Invoke-WebRequest` で確認（既知・過去 daily と同）。

## 効果判定（次 cron 窓へ持ち越し）

本変更は cron パイプライン内部（ページ面なし）＝効果は次 cron 窓の heartbeat で被検証。cron は `0 */6 * * *`＝次窓は **2026-07-13T00:00Z**。本ループ時点（デプロイ直後）では新コード未実行のため、baseline を記録:

- **baseline heartbeat（デプロイ前・本番 KV `--remote` 読み）**: `at:2026-07-12T18:00:14Z`, `browserLaunched:false`, `launchAttempts:3`, `stageErrors:["browser_launch","browser_launch_retry_2","browser_launch_retry_3"]`, `allowedBrowserAcquisitions:1`/`timeUntilNextAcquisitionMs:0`（自己申告予算は空き）, **`browserLaunchError:"...code: 429: message: Rate limit exceeded"`**。

**重要な観測（正直な但し書き）**: 失敗モードは窓ごとに揺れている。
- 週次 #006 が見た `2026-07-12T00:02Z` 窓＝`browser_launch exceeded 30000ms`（タイムアウト・非429）。
- 本ループが見た `2026-07-12T18:00Z` 窓＝**429 Rate limit exceeded**（アカウ/プラン水準クォータ。`puppeteer.limits()` は露出しないため diag 上は予算空きに見える＝2026-07-03 daily §C の結論と整合）。

→ **60s 緩和はタイムアウト側（`exceeded 30000ms`）のみ解消し、アカウ水準 429 側には効かない**。本変更後の数窓の heartbeat で:
- `exceeded 30000ms` が消え `browserLaunched:true` になれば = コールドローンチ・レイテンシが真因だった（解決）。
- `429 Rate limit exceeded` が支配的に残れば = **無料枠 Browser Rendering が cron から安定起動しない**と結論づけ、有料 Browser Rendering への切替＝**危害①（金銭）＝要ユーザー承認**の分岐（週次 §7 提案2 / #007 提示）へ。

判定は次回 daily / 週次 #007 で実施。

## 次候補

- 効果判定（上記）後の柱2 分岐、または優先2「柱1'/柱4' アセット被引用性の強化（本命・自律）」。
