# Daily Ops Log — 2026-06-20

status: done

## 選定タスク
戦略バックログ **#6**: **柱1 — Schema.org（FAQ/HowTo）の ja/pt/ar 実装**。

選定理由: バックログ #1–#5 は完了済みで、先頭の未完が #6。Launch-Phase KPI Gate 上、構造化データ整備は「sitemap/canonical/hreflang correctness」と同系のインデックス支援タスクで選択可（CVR/ファネル微調整の凍結対象外）。調査の結果、solution ページには既に `HowTo` / `FAQPage` / `BreadcrumbList` の JSON-LD が出力されていたが、**ja/pt/ar で 2 つの実害**があった:
1. **BreadcrumbList のラベルが `ja` のみローカライズ**。pt/ar/その他は英語「Home / Solutions」にフォールバック（locale 不一致の構造化データ）。
2. **FAQPage が退化**: `mainEntity` がページタイトル 1 問のみで、回答は全 section 本文を連結した 1 文字列。section ごとの Q&A になっておらず、多言語 FAQ として機能していなかった。
3. 付随: 高意図クラスタの `tiktok-video-downloader-not-working` の section 見出しが ja/pt/ar でも英語「Step 1/2/3」のまま（**可視ページにも露出**＋ HowTo/FAQ schema にも露出）。

## 健全性チェック（実装前）
- `GET https://clipkeep.net/api/v1/health` => **200**
- `GET https://clipkeep.net/sitemap.xml` => **200**
- 破損なし → 修復差し替え不要。
- 補足: 当環境では bash の `curl` が本番に不通（exit 43 / 000）。本番検証は PowerShell `Invoke-WebRequest` を使用。

## 実装内容
**`app/solution/[slug]/page.tsx`**（JSON-LD 生成のみ）
- 全 10 ロケールの `breadcrumbLabels`（home / solutions）マップを追加し、BreadcrumbList の position 1/2 のラベルに適用。`locale === 'ja' ? ... : 'Home'` の二択フォールバックを廃止。
- `FAQPage.mainEntity` を **section ごとに 1 Question**（`heading` → 質問、`body` → 回答）へ書き換え。連結 1 問を撤去。section データはローカライズ済みのため、ja/pt/ar で完全な多言語 FAQ になる。

**`src/lib/solution-pages/store.ts`**
- `tiktok-video-downloader-not-working` の s1/s2/s3 見出しを ja/pt/ar でローカライズ（telegram/twitter/reddit と同方式の三項分岐）。en は従来文言を維持。

- 新規 URL 追加なし＝**sitemap URL 数は不変**。D1 schema / Cloudflare bindings 変更なし。

## ローカル検証
- `npm run typecheck` => PASS
- `npm run lint` => PASS（No ESLint warnings or errors）
- `npm run build` => PASS（`/solution/[slug]` 含む全ルートビルド成功）

## Definition of Done
1. commit `b5e6d93` → `git push origin main` 完了。
2. `npm run deploy:prod` 完了。**Current Version ID: `0b329ec7-f0b0-4dc9-9281-8f9c81b98d0f`**。
3. `npm run check:release:prod` => **PASS=29 / FAIL=0 / SKIP=1**（SKIP は Adsterra SSR マーカー＝client-side で既知）。D1 migration status（remote）PASS。Release gate passed。
4. 本番（https://clipkeep.net）で変更箇所を直接確認（`Invoke-WebRequest` で JSON-LD を抽出・パース、`@graph` ブロックを検査）:

| URL | Breadcrumb | FAQ 質問数 | HowTo steps |
|---|---|---:|---|
| `telegram-...-not-working?locale=ja` | ホーム > 解決策 > Telegram動画が保存できない場合 | **3** | 3（公開URLを使う / 一時的なエラーへの対処 / 画質と保存形式） |
| `twitter-...-not-working?locale=pt` | Início > Soluções > Downloader do Twitter (X) não funciona | **3** | 3（Posts privados... / Como colar... / Qualidade...） |
| `reddit-...-not-working?locale=ar` | الرئيسية > الحلول > محمل فيديوهات Reddit لا يعمل | **3** | 3（تحقق من صيغة... / الدمج التلقائي... / أنواع المنشورات...） |
| `tiktok-...-not-working?locale=ja` | ホーム > 解決策 > TikTok動画が保存できない時の解決策 | **3** | 3（URL形式を確認する / キャッシュを消して再試行 / 公開動画のみ対応） |

- 確認点: ①Breadcrumb の Home/Solutions が ja=ホーム/解決策、pt=Início/Soluções、ar=الرئيسية/الحلول にローカライズ（旧: 英語固定）、②FAQPage が **1→3 問**の section 別 Q&A 化、③TikTok ja 見出しが英語「Step 1/2/3」→ 日本語化（可視ページも同時改善）。

## 効果と次アクション
- 効果（多言語構造化データの整合性向上→クロール理解の改善）は数日〜2週間 lag。Google は 2023 以降 HowTo リッチリザルトを廃止・FAQ リッチリザルトを権威ドメインに限定しているため、**リッチリザルト表示は期待せず**、あくまで意味理解・i18n 整合の衛生改善として扱う。次回週次で ja/pt/ar 圏の impression 推移を監視。
- 残バックログ: #7 トレンド鮮度ページ生成パイプライン設計（bythink）、#8 workers.dev 重複対策、#9 id/hi/tr キーワードマップ。
- 律速は引き続き「新パス URL の発見→index 昇格」と impressions(28d)«1,000（Phase L 継続）。
