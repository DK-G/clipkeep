# 日次ループ 2026-07-02

## 選定
- 先頭走査:
  - **優先1（柱2 browser_rendering 429 の是正 step ②）＝要人間判断でスキップ**。真因は
    2026-07-01 に確定済み（`Error: Unable to create new browser: code: 429: Rate limit exceeded`
    ＝Cloudflare Browser Rendering のブラウザ作成レート制限）。残 step ② は「ダッシュボードで
    クォータ確認→(a)プラン引き上げ (b)cron 頻度低下/1ブラウザ再利用のいずれかを決定」＝
    Cloudflare プラン/クォータのユーザー判断待ち。ループのガードレール（要人間判断・bindings 系は
    実行せず記録）に従いスキップ。
  - **優先2（#5 id/hi/tr ロングテールキーワードマップ作成）＝採用**。柱1・KPI Gate 適格
    （ロングテール SEO の展開準備）・D1/bindings 非依存・成功指標=「docs/strategy/ に文書出力」で明快。

## 実施内容
- **成果物**: `docs/strategy/keyword-map-id-hi-tr.md`（新規）。id/hi/tr の柱1 展開の優先順位付けと
  ページ制作順（発注書）を設計。
- **手法の誠実性**: 有償キーワードツール未接続のため検索需要は**方向性推定**（市場シェア実測 +
  ダウンロード系クエリの定型 + 既存タクソノミー）とし、相対スコア1–5で表現・絶対値は主張しない旨を明記。
- **市場データ（2026 実測・出典付き）で3市場を確定**:
  - id: TikTok(広告リーチ最大) / X(3位) / Facebook / Telegram。tier-3・ボリューム最大。
  - hi: **Telegram 突出（世界1位DL国, 3.84億）** / Facebook / X。**TikTok はインド BAN＝除外**。tier-3。
  - tr: **X 突出（2位・歴史的に強い）** / Facebook(浸透91.7%) / TikTok(5位成長中)。tier-2〜3（CPM 最良）。
- **スコアリング・マトリクス**（需要/競合弱さ/収益/実装容易 の4軸）と**推奨制作順10件**を提示。
  既存 stub（store.ts 多言語 pages）充足を先行（1–7）、en fallback の facebook 系（8–10）は基盤整備後。
- **ガードレール反映**: Instagram noindex 対象外／hi-TikTok を作らない（BAN・需要ゼロ）／薄ページ禁止／
  path 化は本文充足とは別タスクである旨を明記（id/hi/tr は現状 `?locale=` クエリ形式）。

## Definition of Done
- 種別: **docs-only（戦略文書）**。サイト/worker/D1/bindings/sitemap いずれも不変更。
- typecheck/lint/build: `.md` のみでコード非依存＝影響なし（TS/Next ビルドに docs は含まれない）。
- **deploy:prod = 非該当**（Worker バンドルは `docs/` を含まず、本番サイト surface は不変）。
  → 設計文書タスクの前例（#7 2026-06-21, 計測ツール #16 2026-06-16）に準拠。
- **リリースゲート（本番）実行で prod 健全を確認**: `npm run check:release:prod` →
  **PASS=29 / FAIL=0 / SKIP=1**（Adsterra marker は client-side ＝既知 SKIP）、D1 remote PASS、
  "Release gate passed."。本番 health 200・sitemap 200（357,865 bytes, PowerShell 確認。当環境の
  curl は libcurl バグで 000）。
- **DoD 完遂経路 = commit → push（docs-only のため push が完了条件）**。

## 本番確認
- health: 200（`{"status":"ok","services":{"db":"ok","extractor":"ok"},"degraded":false}`）
- sitemap.xml: 200 / 357,865 bytes
- リリースゲート: PASS=29/0/1・D1 remote PASS

## 次アクション
- 本マップ 1–7（stub 充足）を `task.md` バックログ（柱1）へ反映。次期デイリー候補。
- 優先1（柱2 429）は引き続き**要人間判断**（Cloudflare クォータ/プラン確認）待ち。
- 週次 #005 で新パス（ja/pt/ar）の未発見%低下・indexed/impression 推移を帰属判定。

status: done（docs-only, pushed; deploy 非該当・release gate PASS で prod 健全確認）
