# 日次ループ 2026-07-07

本日は戦略バックログ #005 優先2「**柱1: id/hi/tr not-working 本文充足（keyword-map 制作順 1–7）**」の
**制作順 #1 = tr / twitter-not-working の stub 充足**を1デプロイ。

## 選定と KPI ゲート

- 本番健全性: health **200** / sitemap **508 URL**（いずれも文書化ベースラインと一致）。修復差し替え不要。
  - 注: Bash の `curl -o /dev/null -w` は schannel の SSL 再ネゴシエーションで `000` を返すため、
    `-v` 実測（`< HTTP/1.1 200 OK`）・本文取得（実コンテンツ描画）で 200 を確認。
- 直前バックログ先頭（#005 優先1・柱2 protocolTimeout）は **2026-07-06 に実装・デプロイ済み**（ver `83afb184`）。
  よって本日は次順の優先2＝柱1 に着手。
- 柱1（多言語ロングテール SEO）は獲得（indexable 在庫増）系＝Launch-Phase KPI Gate の凍結対象
  （CVR/ファネル微最適化）に非該当＝選択可。
- D1 schema / Cloudflare bindings 変更なし（要人間判断項目に非該当）。
- 発注書: `docs/strategy/keyword-map-id-hi-tr.md` §4 推奨制作順 #1（tr/twitter-not-working, stub 充足,
  根拠: tr 最有力スコア17・X はトルコ2位・Monetag tier-2〜3 で3市場中 CPM 最良）。

## 実装（`src/lib/solution-pages/store.ts`）

改修前の `twitterGuide.tr` は薄い ASCII stub（title/s1/s2 のみ・s3 なし・"calismiyor" など発音記号欠落）で、
section 見出しも `tr` フォールバックが無く "Step 1/2/3" 表示だった（ja/pt/ar は充足済み・s3 あり・見出しローカライズ済み）。

- `twitterGuide.tr` を ja/pt/ar 同等の **s1/s2/s3 リッチ本文**へ充足（トルコ語・正しい発音記号 ç/ş/ı/ğ/ü/ö/İ）:
  - s1: 非公開/年齢制限/センシティブ投稿は取得不可＝公開ツイート確認（"videosu indirilemiyor" を自然文に）
  - s2: 正しい URL 貼り付け（`x.com/…/status/…`・`?s=20` 等の追跡パラメータ除去）（"video nasıl indirilir" を織り込み）
  - s3（新設）: 動画/GIF 対応・**filigransız（logosuz）MP4** 最高画質・YouTube 埋め込み/Spaces/ライブ非対応・
    iPhone/Android/PC 再生（"filigransız video indirme" を自然文に）
  - title `Twitter (X) videosu indirilemiyor` / cta `Twitter video indiriciyi dene`
- section 見出し三項に `tr` 分岐を追加（s1=`Gizli ve kısıtlı gönderiler hakkında` /
  s2=`Doğru URL nasıl yapıştırılır` / s3=`GIF, kalite ve desteklenen içerik`）＝"Step N" 解消。
- キーワードは §5 の tr 定型（`video indir` / `nasıl indirilir` / `filigransız` / `videosu indirilemiyor`）を
  **自然文に織り込み**（stuffing なし・ガードレール遵守）。
- 新規 URL なし（既存 tr view の本文充足）＝sitemap 不変（508）。hreflang は既存方針どおり en/ja/pt/ar+x-default
  （tr は `?locale=` クエリ形式のまま。path 化は keyword-map §6 のとおり将来の別タスク）。

## ローカル検証

- `npm run typecheck` PASS
- `npm run lint` PASS（No ESLint warnings or errors）
- `npm run build` PASS
- `npm test` **33/33 PASS**

## Definition of Done

1. commit `0770e64` → `git push origin main` 済み（`0106372..0770e64`）。
2. `npm run deploy:prod` 済み。**Version ID `f888a8b9-731d-417c-a574-605b29f6c47b`**。
3. `npm run check:release:prod` → **PASS=29 FAIL=0 SKIP=1**（ベースライン一致。初回 run は外部プローブの
   一過性 flake で FAIL=1 だったが再 run で解消。SKIP=1 は Adsterra client-side marker で既知）。
4. 本番確認（https://clipkeep.net/solution/twitter-video-downloader-not-working?locale=tr）:
   - `< HTTP/1.1 200 OK`
   - title `Twitter (X) videosu indirilemiyor` 表示
   - 3 section 見出しすべてトルコ語表示（`Gizli ve kısıtlı gönderiler hakkında` /
     `Doğru URL nasıl yapıştırılır` / `GIF, kalite ve desteklenen içerik`）
   - s3 本文の `filigransız`、CTA `Twitter video indiriciyi dene` を確認
   - "Step 1/2/3" 残置 = **0**（stub 見出し消失を実証）
   - canonical = 自己参照 path 形式 `.../twitter-video-downloader-not-working`（`?locale=tr` は base canonical へ畳み＝既存方針どおり）

## status: done

- 効果（tr/twitter の indexed/impression 増）は数日〜2週間 lag。次回週次 #006 で GSC 推移を監視。
- 次順（制作順 #2）: **hi / telegram-not-working**（世界1位 DL 国）。着手時に keyword-map §4 のとおり
  hi 表記統一（デーヴァナーガリー基準＋Hinglish 併記）も併せて是正する。
