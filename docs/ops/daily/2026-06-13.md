# Daily Growth Log — 2026-06-13

status: done

## サマリ
柱1（多言語ロングテール SEO）として ja Solution「保存できない」ページ（X/Reddit/Telegram）の内容を充足。
あわせて、デプロイ前に存在していた gallery API のリグレッション（`platform=reddit` が 400）を発見・修復し、リリースゲートをクリーン化した。

## 健全性チェック（着手時）
- `GET /api/v1/health` → 200（`db: ok`, `extractor: ok`, degraded=false）
- `GET /sitemap.xml` → 200（357,865 bytes）
- 本番は健全。修復差し替えは不要。

## タスク選定（戦略バックログ先頭から）
- **#1 計測: ad_script_load の zone別28日集計を growth:review に追加** → **スキップ**。
  events エクスポート（`latest-ga4-events.csv`）は `eventName,eventCount,activeUsers` のみで zone ディメンションを持たない。zone別集計には GA4 のカスタムディメンション再取得が必要で、これは `task.md` 記載の**ブロック中（要ユーザー操作）**の GA4 認証復旧（`analytics:ga4:login`）に依存。自律実行では完遂・検証不可のため次へ。
- **#2 TikTok「保存できない/ロゴなし保存」solution（en/ja）新設 + home title 整合** → **既充足のため新規実装なし**。
  - `tiktok-video-downloader-not-working`（全ロケール）と `how-to-download-without-watermark`（全ロケール）は既存。en/ja とも本番 200。
  - home title は汎用（"Video Downloader & Extractor"）で TikTok の虚偽表記なし。かつ TikTok extractor（`src/lib/extract/tiktok.ts`、`store.ts` 経由）は実装済みで TikTok は実際に対応プラットフォーム（`tiktokTitle` / `successRateTikTok: "100% Success"`）。→ title と実態は整合、修正不要。
- **#3 柱1: ja Solution ページの内容充足（X/Reddit/Telegram 中心）** → **本日の実装タスクとして採用**。

## 実装（#3）
`src/lib/solution-pages/store.ts`：X(Twitter)/Reddit/Telegram の `*-video-downloader-not-working` ページについて、
- 日本語本文 s1/s2 を1文 → 複数文へ拡充（保存できない/ダウンロードできない/見れない/原因/対処法/画質/URLの貼り付け方 等のロングテール語を自然に内包）
- 第3セクション s3 を新設（ja のみ、ローカライズ見出し付き）
- guide 型に optional `s3?` を追加し、他ロケールは従来通り2セクションで後方互換（条件付き描画）
- 効果: meta description（`sections[0].body`）と HowTo/FAQ JSON-LD の情報量も同時に増加

ローカル検証: `npm run typecheck` / `npm run lint` / `npm run build` すべて成功。`/solution/[slug]` は動的（ƒ）レンダリングのためデプロイ即時反映。

## 発見・修復したリグレッション（rule #1: 本番破損の最優先修復）
- リリースゲートで `GET /api/v1/gallery/recent|trending?platform=reddit` が **400 INVALID_PLATFORM**（PASS=27/FAIL=2）。
- 原因: 直前の `46059c0 "fix: reconcile merged ClipKeep branches"`（ブランチ統合の reconcile）で gallery 両 route の `VALID_PLATFORMS` から `'reddit'` が脱落。前日ゲートは PASS=29/FAIL=0 だったため退行。reddit はダウンローダー・`/trending/reddit`・`/latest/reddit` が稼働する第一級プラットフォームで、ゲート（`prod_release_check.ps1`）も reddit gallery の 200 を明示検証している。
- 修復: `app/api/v1/gallery/recent/route.ts` と `.../trending/route.ts` の許可セットに `'reddit'` を復帰（contract 整合）。

## Definition of Done
- コミット:
  - `69f3c22` 柱1: ja Solution「保存できない」(X/Reddit/Telegram) 内容充足
  - `f8b0454` fix(gallery): restore reddit as valid gallery platform
- push: `origin/main` 済み（46059c0 → f8b0454）
- deploy:prod: 実施。最終 **Version ID = `a5c183d2-2ae6-4356-8862-a88c9f43ed6f`**（中間 `e8ac5669-...` は #3 のみのデプロイ）
- check:release:prod: **PASS=29 / FAIL=0 / SKIP=1**（SKIP=Adsterra marker は client-side のため想定通り）、D1 migration status check (remote) PASS、Release gate passed
- 本番ライブ確認（変更箇所そのもの）:
  - `GET /api/v1/gallery/recent?platform=reddit&limit=3` → 200 / `trending` → 200（リグレッション解消）
  - `https://clipkeep.net/ja/solution/twitter-video-downloader-not-working` → 200、s3見出し「GIF・動画の画質と対応範囲」描画あり、HowTo schema あり、canonical=self(ja)
  - 同 reddit → 200、s3「保存できない投稿の種類」描画あり、HowTo あり、canonical=self(ja)
  - 同 telegram → 200、s3「画質と保存形式」描画あり、HowTo あり、canonical=self(ja)

## 要人間判断 / 週次レビュー向け申し送り
- **戦略文書とコードの不整合**: `docs/strategy/growth-strategy.md` のガードレールと決定事項1は「TikTok extractor は作らない（規約リスク回避）」と明記するが、コードには既に TikTok extractor（`src/lib/extract/tiktok.ts`、TikWM/Lovetik/kktiktok fixer 経由）が存在し稼働中。本タスクの範囲外かつ撤去はリスクが高いため**不変更**とした。extractor を残すか撤去するか、戦略文書の記述を実態に合わせるかは**週次レビューでのユーザー判断事項**。
- gallery `VALID_PLATFORMS` には pinterest/bluesky/lemon8/facebook/bilibili/discord 等の対応プラットフォームが未登録（今回はゲート契約に沿って reddit のみ復帰）。これらを gallery 対象にすべきかは要検討（ギャラリーデータの有無も含め）。
