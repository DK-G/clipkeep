# Weekly Review Record #010 — ClipKeep Growth

日付: 2026-08-16（日／週次スロット）
対象期間: 直近28日（GA4/GSC as of 2026-08-16, snapshot `growth-2026-08-15T20-42-04-620Z.json`）
比較対象: 前回 週次レビュー #009（2026-08-09・**7日前**＝正常間隔を3週維持）
担当: 自動週次レビュー（無人実行）
正本: `docs/strategy/growth-strategy.md`（北極星: Monetag タグロード数/日 = GA4 `ad_script_load`）

> 注: Launch-Phase（Phase L）運用中。先行指標は **GSC impressions / URL Inspection「Google 未発見」比率 / インデックス数 / Google オーガニックセッション**。CVR・ファネル微調整は凍結（AGENTS.md / growth-strategy.md §2）。実数のみ記載・**TBD 禁止**（取得できない値は「取得不能: 理由」を明記）。
>
> **#007 で確定した読み方を踏襲**: `ad_script_load` とセッションは Phase L の判定指標から外す。**本週その根拠が計測レベルで決定的になった（§1.1）。**

---

## ✅ 0. 計測健全性（実装前ゲート）

**`npm run growth:review`: 成功（GA4・GSC・URL Inspection の3系統とも SA で fresh 取得）。**

- 認証: 🔐 ANALYTICS AUTH = **OK**。SA 化（06-28）以降 **7 週連続で無人失効なし**。`D:\secrets\clipkeep\`（リポジトリ外）経路も 2 週連続で無人稼働を実証。
- スナップショット: `docs/analytics/history/growth-2026-08-15T20-42-04-620Z.json`（28日窓 as of 2026-08-16 JST）。
- URL Inspection: 501 URL 中 100 をサンプル（`GSC_INSPECT_LIMIT=100`）＝**インデックス数は下限値**（運用不変）。

### 本番健全性（`npm run check:loop` 実測, 2026-08-16）

| 対象 | 実測 | 判定 |
|---|---|---|
| `npx wrangler whoami`（デプロイ経路） | **未ログイン＝デプロイ不能** | **BLOCKER（33日目）** |
| `GET /api/v1/health` | **200** | 健全 |
| `GET /sitemap.xml` | **200**（`<loc>` **509**） | 応答健全（**5週間 新規URLゼロ**） |
| `GET /api/v1/platform-status` | **200**（platforms **6** / 劣化1件） | 稼働・**旧構成**（`3bd3cb6` の 9 PF 化は未反映 **23日目**） |

`npm run check:loop` の総合判定: **BLOCKED — blocker 1 件（wrangler 未ログイン）／warning 1 件（twitter degraded）**。

### スケジューラ実測（#009 提案5 で定例化・本週から表として固定）

| taskId | enabled | lastRunAt (UTC) | nextRunAt (UTC) | 判定 |
|---|---|---|---|---|
| `clipkeep-daily-growth-loop` | **false** | 2026-07-29T21:46:04Z | **なし** | **無効化のまま・#009 から変化なし**（§4.1） |
| `clipkeep-weekly-health-check` | true | 2026-08-15T01:33:16Z | 2026-08-22T01:33:04Z | 正常（08-15 は status: green） |
| `clipkeep-weekly-strategy-review` | true | 2026-08-15T20:39:13Z | 2026-08-22T20:39:12Z | 正常（本レビュー） |

---

## 1. 北極星 & KPI サマリー（28日）

| Metric | 2026-08-16（実測） | 2026-08-09（#009） | WoW | 判定 |
|---|---:|---:|---:|---|
| ad_script_load（北極星, 28d 合計） | **429** | 336 | **+93** | 増だが**獲得の指標として無効**（§1.1 で決定的に確認） |
| ad_script_load（7d） | **123** | 112 | +11 | 同上 |
| zone 10760541（In-Page Push, 28d） | **214** loads / err 3 / timeout 7・**load 率 96%** | 169 / 3 / 9・93% | 量+45 | 成功率 横ばい〜微増 |
| zone 10969428（Push Notification, 28d） | **215** loads / err 2 / timeout 3・**load 率 98%** | 167 / 2 / 4・97% | 量+48 | 同上 |
| セッション/28d（GA4 summary） | **65** | 57 | **+8** | 前週の減少から戻し（§1.1） |
| アクティブユーザー/28d | **65** | 57 | +8 | 同上 |
| エンゲージメント率/28d | **52.3%** | 59.6% | **−7.3pp** | 母数小・再び低下 |
| イベント総数/28d | **521** | 458 | +63 | — |
| **GA4 `views`（screenPageViews）/28d** | **4** | **0** | **+4** | ★**本週の最重要**＝計測は生きている（§1.1） |
| **Google オーガニック セッション/28d** | **0** | 0 | ±0 | **4週連続で完全にゼロ** |
| GSC impressions/28d（**query 次元**＝従来系列・撤退基準の根拠） | **7** | 8 | **−1** | **非成長**。かつ**全件ブランドクエリ**（§2.1） |
| GSC impressions/28d（**page 次元**） | **26** | 32 | **−6** | **非成長**（§2.2） |
| **非ブランド impressions/28d** | **0** | 0 | ±0 | **3週連続でゼロ**（§2.1） |
| GSC clicks/28d | **0** | 0 | ±0 | 母数ゼロ域 |
| GSC 平均順位（en・query 次元） | **18.1** | 16.4 | 悪化 | 母数5行のノイズ域（§2.1） |
| GSC indexed（URL Inspection サンプル100） | **37 / 100** | 37 / 100 | **±0** | 横ばい |
| 「Google 未発見」URL（サンプル100） | **61 / 100** | 62 / 100 | **−1** | **2週連続で改善**（§2.0） |
| Crawled - not indexed（サンプル100） | **2** | 1 | +1 | ノイズ域 |
| canonical mismatch / blocked | 0 / 0 | 0 / 0 | ±0 | 健全 |
| sitemap URL 数 | **501**（`<loc>`509） | 501（509） | ±0 | **5週間 新規 URL ゼロ**（stub 量産停止方針の帰結＝想定内） |

### 1.1 ★【今週の最重要発見】`page_view` が初めて発火し、**北極星のトラフィックが page_view を1回も出していない**ことが確定した

**#009 までの本レビューは、GA4 の `views` が 0 であることを「当プロパティで実質未計測」と説明してきた。本週それが誤りであることが判明した。**

実測（28日窓・`topEvents` / `topPages`）:

| 事実 | 実測値 |
|---|---:|
| `session_start` | **223** |
| `first_visit` | **223** |
| `ad_diagnostic_context` | **223** |
| `ad_script_load` | **429** |
| `scroll` | 155 |
| **`page_view`** | **4（activeUsers = 1）** |

**＝`page_view` は壊れていない。実際に発火している。ただし発火させたのは 223 セッション中 1 ユーザーだけである。**

さらに `topPages` を見ると構図が完全に一致する:

| pagePath | views | sessions |
|---|---:|---:|
| `/trending` | **2** | 1 |
| `/result/job_twitter_b486c02256d10f0c` | 1 | 1 |
| `/result/job_twitter_e2a81a31794d8ce8` | 1 | 1 |
| **`/`** | **0** | **195** |
| `/ar/legal/terms` | 0 | 2 |
| `/ar/about` ほか | 0 | 各1 |

- **`/` は 195 セッションを記録しながら view が 0 件。** 通常のブラウザによるページ読み込みなら `page_view` は必ず発火する。
- 一方、**`/trending` → `gallery_card_click`(2) → `/result/...`(2 views) → `discovery_range_change`(1) → `trending_hub_view`(1) という、明確に人間の閲覧行動の形をした導線が 1 ユーザー分だけ存在する**。この 1 人だけが `page_view` を 4 回出している。
- **結論: 28日間で「人間の形をしたセッション」は 1 件しか観測されていない。残り 222 セッションは `session_start` と広告タグのロードだけを出し、ページビューを出していない。**

これは #007 以来「非人間トラフィック説」として傍証を積んできた読みを、**傍証ではなく計測上の直接的な事実**に格上げする:

1. **`ad_script_load` 429 は、`page_view` を 1 度も出さないトラフィックが作っている。** 北極星は「獲得の進捗」を測っていないどころか、**人間の閲覧をほぼ一切含んでいない**。
2. **#009 までの「views=0 は計測されていないから」という説明は破棄する。** 正しくは「**計測されているが、発生していない**」。過去のレビューでこの値を「未計測」として KPI 判断から外していたのは、**結果的に最も重要な信号を捨てていた**ことになる（プロセス上の反省・§7 提案5）。
3. セッション増（57→65）とエンゲージメント率低下（59.6%→52.3%）が同時に起きているのも同じ構図で説明できる。

**GA4 acquisition（28d）**:

| channelGroup | sourceMedium | sessions | engagedSessions |
|---|---|---:|---:|
| **Direct** | (direct) / (none) | **221** | 139 |
| Unassigned | (not set) | 4 | 0 |
| Organic Search | yandex / organic | 2 | 2 |

- **Google からのオーガニック セッションは 0 件（4週連続）。**
- **Direct が 221/227 = 97.4%**（#009 96%・#008 91%）＝**比率はさらに上昇**。
- **referral は 2週連続で 0 件**＝被リンク由来の流入は観測上ゼロ。
- yandex organic が 2 件（#009 は yandex 1 + yandex.com.tr referral 1）。**Google 以外の検索エンジンからは、少ないながら人が来ている**。
- 計測上の注意（不変・正直な記載）: acquisition 行の合計は **227** で、無ディメンションの summary セッション **65** と一致しない。GA4 はディメンションを付けるとセッションが重複計上されうるため、**チャネル構成比は方向性として読み、絶対値は summary の 65 を正とする**。

---

## 2. SEO シグナル（GSC, 28日）

### 2.0 発見（インデックス）— 2週連続で微改善、ただし在庫は5週間 不変

- Submitted and indexed: **37**（#009 37・±0）
- URL is unknown to Google: **61**（#009 62・**−1**）
- Crawled - currently not indexed: **2**（1・+1） / canonical mismatch: 0 / errors: 0

「未発見」比率の推移: **72 → 68 → 66 → 65 → 63 → 63 → 62 → 61**。

**#009 で訂正したとおり「頭打ち」ではなく、在庫（sitemap 501）の緩やかな消化が続いている**。ただし **sitemap は 5 週間 501 で不変**であり、この系列の上限は構造的に決まっている。**+1/−1 はいずれも 100 サンプル中 1 件＝ノイズ域**である点は不変。

### 2.1 撤退基準の系列は **今週も全件ブランドクエリ**、かつ **減少**した

`latest-gsc-query-pages.csv`（28日窓・`dimensions:["query","page","country","device"]`）の**全行**:

| query | page | country | device | impressions | position |
|---|---|---|---|---:|---:|
| **clipkeep** | `/` | can | MOBILE | 1 | 9 |
| **clipkeep** | `/` | ind | DESKTOP | 1 | 7 |
| **clipkeep** | `/` | nld | DESKTOP | 1 | 8 |
| **clipkeep** | `/` | usa | DESKTOP | 3 | 15.7 |
| **tubekeep** | `/` | gbr | DESKTOP | 1 | 56 |

- **5 行すべてがブランド／類似ブランドのナビゲーショナル・クエリ。非ブランド impressions は 3 週連続で 0。**
- 合計 **7**（#009 8）＝**−1 で非成長**。
- ブランドクエリ内訳: `clipkeep` **6**（#009 7）／`tubekeep` 1（不変）。**国の顔ぶれは入れ替わっている**（#009 の idn・phl が消え can が出現）＝**同一の関心が継続しているのではなく、単発の検索が毎週数件発生しているだけ**と読むのが妥当。
- **`clipkeep` の position は 7〜15.7・clicks 0（不変）。自サイト名で 1 位を取れない状態は 2 週連続**（#009 §2.1-3 の診断＝ドメイン権威不足の最も直接的な実測は今週も維持）。
- 平均順位 16.4 → **18.1** は「悪化」ではなく **5 行の構成が変わっただけ**（#009 で「16.4 は改善ではない」と書いたのと同じ理由で、**今週の悪化も成果指標として読んではならない**）。

### 2.2 page 次元: 総量は減少、**pt の優位だけが 3 週連続で再現**

`latest-gsc-pages.csv`（28日窓・`dimensions:["page"]`）をロケール別に集計:

| locale | impressions/28d（#009比） | 平均position | ページ内訳（position） |
|---|---:|---:|---|
| en | **15**（19・**−4**） | 14.1 | **`/` 8(16)** / `blog/twitter-video-download-private-account` 1(11) / `contact` 1(3) / `legal/cookies` 1(7) / `solution/bilibili-...-not-working` 1(9) / `solution/...-on-android` 1(42) / `trending/bilibili` 1(5) / `trending/threads` 1(6) |
| **pt** | **6**（6・±0） | **4.2** | **`/pt` 1(2)** / **`/pt/blog/safest-video-downloader-sites` 1(2)** / `blog/how-to-download-twitter-videos?locale=pt` 1(4) / `/pt/latest` 1(4) / `blog/tiktok-video-download-no-watermark?locale=pt` 1(6) / `/pt/blog/twitter-video-download-mp4` 1(7) |
| ja | **3**（4・−1） | 9.3 | `blog/tiktok-video-download-private?locale=ja` 2(9) / `?locale=ja` 1(10) |
| es | **1**（2・−1） | 8 | `?locale=es` 1(8) |
| hi | **1**（1・±0） | 9 | `?locale=hi` 1(9) |
| **ar** | **0**（0・±0） | — | **3週連続で impression ゼロ** |
| 合計 | **26**（32・**−6**） | | |

- **pt は 3 週連続で「6 件中 6 件が position 2〜7」（平均 4.2）**。対象 URL の顔ぶれも #009 と完全に同一。**#009 で通過したデータゲートは本週さらに補強された**（3週連続再現・構成不変）。
- **減少は en が主**（19→15、うち `/` が 12→8）。**pt だけが横ばいを保っている**。
- **ar は 3 週連続でゼロ**。柱1 の定義「ja/pt/ar 優先」のうち **ar を優先ロケールに置く実測的根拠は 3 週にわたり皆無**（§7 提案2 で再提示）。
- 但し書き（不変）: 各行 1〜2 impression のノイズ域。「3週連続の再現」は**偶然ではないことの根拠**にはなるが、「pt は上位表示できている」の根拠にはならない。

### 2.3 結論

1. **両系列がそろって減少した**（query 8→7・page 32→26）＝**#008 が定めた合成ルールで、今週は初めて「非成長」にカウントされる**（§6）。
2. **非ブランド検索からの露出は 3 週連続でゼロ**。柱1 が狙う需要には一度も到達していない。
3. **発見（未発見%）は微改善が続く**が、在庫 501 が 5 週間不変である以上、伸びしろは構造的に限られる。
4. **pt の優位は 3 週連続で再現**＝賭け先の絞り込みだけは実測に支持されている。
5. **律速の診断は #006 から不変**＝順位化＝ドメイン権威／被リンク。今週は §1.1 により「**そもそも人間がほぼ来ていない**」という、より手前の事実が計測で確定した。

---

## 3. ファネル / グロースループ（28日, 凍結中）

Phase L につき凍結。参考実測: Form Interest 0 / Intent-to-Clip 0 / processing_complete 0 / download_actual_start 0 / share 0。**ただし discovery clicks が 2（#009 は 0・前回実行比 +2）＝久しぶりの非ゼロ**であり、`gallery_card_click` 2・`result_view` 2・`trending_hub_view` 1・`discovery_range_change` 1 と合わせ、**§1.1 の「唯一の人間らしいセッション 1 件」に全て帰属する**（activeUsers はいずれも 1）。**回遊施策の成果ではない**（柱3 は凍結中で打ち手ゼロ）。Phase G（indexed≥50 かつ impressions≥1,000）到達まで着手しない。

---

## 4. 障害 / 健全性

- 5xx率: 0（health 200）／degraded 発動: なし（**アプリ配信本体は健全**）
- **柱1' status 資産が示す上流の異常（本番実測 2026-08-16）**:

  | platform | status | http | uptime | samples |
  |---|---|---:|---:|---:|
  | **twitter** | **limited** | **401** | **0%** | 56 |
  | telegram | operational | 200 | **98%** | 56 |
  | reddit | operational | 403 | 100% | 56 |
  | threads | operational | 302 | 100% | 56 |
  | bluesky | operational | 200 | 100% | 56 |
  | tiktok | operational | 403 | 100% | 56 |

  - **twitter は uptime 0%（n=56＝直近14日の全プローブ失敗）が約37日継続**。根因の起点は 07-30 特定済＝`src/lib/extract/twitter.ts:186-215` が **HTTP 401 をどの枝でも分類していない**。
  - telegram が 100%→**98%**（1/56 失敗）。**単発でありまだ異常とは言えない**が、#011 で 2 週連続なら起票する。
  - X 抽出の実地確認は依然として無人不可（`403 TURNSTILE_MISSING`＝Turnstile は設計通り機能。CAPTCHA 回避は行わない）。
- **【5週連続・最大の運用障害】デプロイ経路が 33 日間 断絶**: `wrangler whoami` → 未ログイン。07-14（ver `1d946b55`）を最後に OAuth 失効。`3bd3cb6`（6→9 PF）は**本番未反映 23 日目**。**復旧はユーザーの 1 回操作のみ**。

### 4.1 日次ループは **2週連続で無効化のまま**＝未発火 11 スロット・daily log 空白 17 日

`clipkeep-daily-growth-loop` の状態は **#009 から 1 ミリも変わっていない**（`enabled: false` / `lastRunAt 2026-07-29T21:46:04Z` / `nextRunAt` なし）。

- **未発火スロット: 07-31・08-03〜08-07（#009 時点で 6）＋ 08-10・08-11・08-12・08-13・08-14 ＝ 計 11 スロット連続。**
- **最後の daily log は `2026-07-30.md`＝17 日間ゼロ。**
- #009 は「**バックログをどれだけ丁寧に並べ替えても、消費する主体が停止している限り 1 件も実行されない**」と書き、優先0(a) を最上位に置いた。**その予告どおり、#009 が並べ替えた 13 件は 1 件も消費されなかった。**

**＝本週も出荷 0 件。** 3 つの自動ループのうち動いているのは観測系 2 つ（週次健全性チェック・本レビュー）のみで、**実装・出荷を担う唯一のループが 3 週間 停止している**。

### 4.2 本週の稼働状況

| ループ | 状態 |
|---|---|
| 日次グロースループ（月〜金 06:06） | **無効化により 11 スロット連続 未発火**（§4.1） |
| 週次 健全性チェック（土 10:33） | **正常稼働・status: green**（層B 変更0・層B+ 消化0・**HC-11 を新規起票**／`48eca09`） |
| 週次 戦略レビュー（日 05:39） | **本レビュー＝正常稼働** |

本週のコミットは **`48eca09`（08-15 健全性チェックの記録）1 件のみ**＝**アプリケーションコードの変更 0 行・本番出荷 0 件**。

### 4.3 健全性チェック（08-15）からの引き継ぎ — 柱2 の判断に**セキュリティ上の利得**が加わった

08-15 の health-check が **HC-11** を新規起票した（`docs/ops/health/2026-08-15.md`）:

- `@cloudflare/puppeteer`（**DIRECT high**）→ `@puppeteer/browsers@2.2.4` → `extract-zip`（GHSA-jmr9-qjv8-65gv）。
- **最新 1.3.0 も同じ脆弱版を固定**しており、**バージョンを上げても解消しない**。npm の `fixAvailable` は 0.0.11＝**ダウングレード**で採用不可。
- 出荷バンドルへの実測 grep では**脆弱な transitive は含まれない**（暫定評価・07-24 ビルド）。
- **選択肢 (a)「依存ごと削除」は、柱2（trend スクリーンショット）を凍結すれば成立する。**

★**これは戦略判断の材料が増えたことを意味する**: 柱2 凍結は #007 以来「産出ゼロだから」という費用対効果の話だったが、**本週から「DIRECT high の脆弱性を構造的に消す」という保守上の利得が加わった**。§7 提案3 に反映した。

---

## 5. 柱別 効果判定（変更単位ではなく柱単位）

| 柱 | 直近の打ち手（08-09〜08-16 出荷物） | 観測 | 判定 | 次アクション |
|---|---|---|---|---|
| 柱1 多言語ロングテール | **新規出荷なし**（sitemap 501 で 5 週間不変・日次ループ停止のため打ち手ゼロ） | 未発見 62→**61**・indexed 37→37。page 次元は 32→**26 で減少**（減少は en が主）。**pt は 6/6 が pos 2〜7 で 3 週連続再現**・ar は 3 週連続 0。★**非ブランド impressions は 3 週連続 0** | **仮説は棄却されないが、検証が一度も実行されていない**（打ち手が出荷されていないため、実測は「何もしなかった場合の推移」に過ぎない） | ①優先4（pt 深化）は**実装・push までデプロイ非依存で先行可**と明記（本週の変更）②ar は優先ロケールから外す提案を継続（§7 提案2） |
| 柱1' linkable asset（主軸） | **新規出荷なし**。`3bd3cb6` は 07-24 push 済・**23 日間 未デプロイ** | 資産は稼働（6 PF 全応答・`checkedAt` fresh）。**33 日間 更新が本番へ届いていない**。資産自身が twitter 0%（約37日）を出し続けている | **"稼働"は維持、"成長"は 5 週連続 停止**。律速は資産の設計でなく**デプロイ経路の断絶**（不変） | ①デプロイ復旧後ただちに `3bd3cb6` を出荷 ②その後 A-v2 |
| 柱2 トレンド鮮度 | **新規出荷なし**（最後の変更は 07-13 の 60s 緩和） | **34 日・約136 cron 窓 稼働して産出ゼロ**（`/trend/` が sitemap 0 件） | **4 週連続で「60s 緩和は失敗」を追認**。★**加えて本週、凍結すれば HC-11（DIRECT high）が構造的に消えることが判明**（§4.3） | **二択への回答が 3 週間 未取得**。推奨は **(b) 凍結**（費用対効果＋セキュリティの二重の理由・§7 提案3） |
| 柱3 回遊 | 凍結（Phase L） | discovery clicks 2 が初の非ゼロだが、**単一ユーザーの自然な閲覧**であり施策効果ではない（§3） | 凍結継続 | Phase G まで着手しない |
| 柱4' 外部流入・権威 | **新規出荷なし**（AlternativeTo 登録は **5週連続で未確認**） | **GA4 referral 0 件（2週連続）**。代理指標（badge 外部 Referer 集計）は KV binding 必須で**未実装のまま 5 週間**。`clipkeep` クエリの position 7〜15.7・clicks 0 は不変 | **効果は未観測。効果を測る計器が無い状態が 5 週間 継続** | ①KV namespace 作成＝ユーザー承認クラス ②AlternativeTo 登録（差分は `docs/ops/outreach/2026-08-16.md`） |

**律速の結論**: 戦略的な律速は #006 から不変（順位化＝ドメイン権威）。**実務上の律速は 5 週連続でデプロイ断絶（33日）＋ 2 週連続で日次ループの無効化（11 スロット・17日）である。本週も「戦略の問題は 1 つも検証されていない」**。§1.1 の発見はこの構図をさらに厳しくする＝**現在の実測はほぼ全て非人間トラフィックの観測であり、施策の成否を測れる母数がそもそも存在しない**。

---

## 6. 撤退基準の判定（毎回必須）

- 基準: **8週連続**で GSC impressions(28d) が成長しない → 改修停止・戦略変更提案。
- **query 次元（従来系列＝§7 の文言が指す系列）** = **7**（#009 = 8）→ **−1＝非成長**。
- **page 次元（#008 提案1 で併記開始）** = **26**（#009 = 32）→ **−6＝非成長**。
- #008 提案1(c) の運用（**両系列がともに非成長のときだけ非成長とカウント**）に従い、**今週は非成長にカウントする**。
- 実測ベースのカウント履歴: #002（17→15 非成長, 連続1）→ #003（測定不能, 非加算）→ #004（15→3 非成長, 連続2）→ #005（3→4 成長, リセット=0）→ #006（4→4 非成長, 連続1）→ #007（4→5 成長, リセット=0）→ #008（5→7 成長, 0）→ #009（7→8 成長, 0）→ **#010（8→7 かつ 32→26＝両系列 非成長, 連続=1）**。
- **判定: 撤退基準 非該当（連続非成長 = 1 / 8）。**
- **重要な留保（#009 から不変・今週さらに強化）**:
  1. **この基準は依然として測る対象を間違えている。** query 次元の 7 impressions は**全件ブランドクエリ**であり、**非ブランド impressions は 3 週連続で 0**。「8→7 の非成長」の実体は「**先週より 1 人少ない人が『clipkeep』と検索した**」以上の意味を持たない。
  2. **加えて本週、より根本的な問題が計測で確定した（§1.1）**＝サイトに来ているトラフィックの 99.5% は `page_view` すら出していない。**戦略の成否を測るための母数（人間の訪問）が実質存在しない。**
  3. **そして最も重要な留保: 8 期にわたり、この基準が判定しようとしている「戦略」は一度も実行されていない。** 直近 5 週の出荷は 0 件、日次ループは 3 週停止、デプロイは 33 日断絶。**撤退基準が「戦略の失敗」を検出しても、それは戦略の失敗ではなく実行の不在を検出している。**
  4. §7 の文言変更はユーザー承認が要るため、本レビューでは**基準そのものは変更せず、判定を機械的に適用したうえで実体を併記**した（§7 提案1）。

---

## 7. 戦略変更提案（growth-strategy.md は変更せず、ここに記載）

1. **【継続・最優先／計測の是正】撤退基準の主系列を「非ブランド impressions」に切り替える**（#009 提案1 と同一・**日次ループ停止のため未着手**）。是正案も不変: (a) `query != ブランド名` でフィルタした impressions を主系列に、(b) page 次元・query 次元も併記、(c) 移行期は「非ブランド系列が非成長 かつ page 次元も非成長」のときだけ非成長とカウント。**実装は `scripts/growth-summary.mjs` の集計追加のみ・新規 API 呼び出しゼロ・デプロイ不要**（優先1）。★**本週 (i) に加えて (iii)「`session_start` と `page_view` の乖離」もサマリー出力に含めることを追加提案**（§1.1 の発見を毎週自動で見えるようにする。同じスクリプト内・追加コストほぼゼロ）。

2. **【継続・要ユーザー承認】`ar` を柱1 の優先ロケールから外し、`pt` を筆頭に繰り上げる**（#009 提案2 の再提示・**2週間 未回答**）。実測は 3 週分に増えた: **pt 6（3週連続 6/6 が pos 2〜7）／ja 3／es 1／hi 1／ar 0（3週連続ゼロ）**。提案は不変＝優先順を **pt > en > ja**、ar は「優先」から外す（既存ページは維持し新規投資の対象外とする）。※§4 本文の書き換え＝**要ユーザー承認**。承認が無くても優先4（pt 深化）は実行可能。

3. **【4週連続・再提示／柱2】60s 緩和は失敗。★本週から凍結には セキュリティ上の利得が加わった**: 07-13 出荷の 60s 緩和は**本番 34 日・約136 cron 窓で `/trend/` 産出ゼロ**。加えて 08-15 health-check の HC-11 で、**`@cloudflare/puppeteer` が DIRECT high でありながら「版を上げても直らない」**ことが判明した（§4.3）。**柱2 を凍結すれば `src/lib/auto-trend.ts` / `src/lib/extract/browser.ts` の puppeteer 依存を落とせ、この脆弱性が構造的に消える。** ＝ **(b) 凍結の推奨は「産出ゼロだから」に「high 脆弱性が 1 件消えるから」が加わり、二重の理由で強化された**。選択肢は不変: (a) 有料 Browser Rendering＝危害ゲート①要承認 ／ **(b) 凍結（推奨）**。**ユーザーの一言が要る**。

4. **【継続・柱4'】ディレクトリ登録の位置づけは #009 のまま**（「被リンク一般」ではなく「**ブランドクエリの実体解決**」）。本週の実測でも `clipkeep` は position 7〜15.7・clicks 0 で不変。差分は `docs/ops/outreach/2026-08-16.md`（**フル下書きの再生成はしない**運用を継続）。

5. **★【新規・プロセス／最重要】「計測されていない」と判断した値を、根拠なく KPI から外さない**: 本レビューは #007〜#009 の 3 週にわたり GA4 `views = 0` を「当プロパティで実質未計測」と説明し、KPI 判断から除外してきた。**本週 4 件発火したことで、その説明が誤りだったと確定した**（§1.1）。正しくは「計測は生きているが、発生していなかった」であり、**この 0 こそが「人間が来ていない」という最重要の信号だった**。教訓: **ゼロが続く指標を「壊れている」と説明するときは、必ず「壊れている」側の証拠（実装確認・デバッグビューでの検証）を取る**。取れないなら「ゼロである」とだけ記録し、判断材料から外さない。※#008 の「発見は頭打ち」誤読（#009 で訂正）と**同じ型の誤り**＝**観測されない/動かない値に対して、根拠の弱い説明を与えて棚上げしてしまう**。

6. **★【新規・運用／ユーザー判断を要する】日次ループが停止している間、週次レビューにデプロイ不要タスクを 1 件消費させるか**: §4.1 のとおり、**#009 が並べ替えた 13 件は 1 件も消費されなかった**。現在動いている自動ループは観測系 2 つだけで、**実装能力を持つ主体が存在しない**。本レビューのスケジュール定義は「コード変更はこのレビューでは行わない（バックログに積むだけ）」と明示しているため、**この週次レビューは規定に従い今週もコード変更を一切行っていない**。しかし優先0(a) が長期化する場合、**バックログの精緻化を繰り返すだけの週が積み上がる**。選択肢: **(a) 日次ループを再有効化する（本筋・推奨）**／(b) 週次レビューの定義を改訂し「日次ループが `enabled:false` の週に限り、**デプロイ不要**のバックログ最上位 1 件を実装・push まで行う」権限を与える／(c) 現状維持（観測のみを続ける）。**(b) は本レビューのスケジュール定義の変更＝ユーザーの領分**なので、ここでは提案に留める。

> いずれも growth-strategy.md 本文は未変更（KPI 履歴行の追記のみ）。**提案1・5 は自律実行しうる**（計測運用）。**提案2 は §4 本文の書き換え＝要ユーザー承認**、**提案3 の (a) 有料切替は危害ゲート①**、**提案6 の (b) はスケジュール定義の変更**＝いずれもユーザー承認が必要。

---

## 8. 翌週アクション（task.md 戦略バックログへ反映済み）

`task.md` の戦略バックログを並べ替え・補充し、**計 13 件**（優先0〜12。うち 優先0・6・8・9・12 はユーザー操作/判断、**残り 8 件が日次ループの消費対象**）を維持した（≥7 要件クリア）。**うち 4 件（優先1・3・5・7）はデプロイ経路が塞がったままでも完遂できる**。

> ★**#009 と同じ但し書きを、より強い言葉で再掲する**: **日次ループが無効化されている限り、この並べ替えは今週も 1 件も消費されない**（#009 の予告は的中し、13 件すべてが未着手のまま残った）。**バックログの精緻化より優先0 の解消が先である。** 3 週間 同じことを書いている。

| 優先 | タスク | 柱/種別 | デプロイ要否 | 成功指標 |
|---|---|---|---|---|
| **0** | **【ユーザー操作・全タスクの前提／3点まとめて】** ★**(a) 日次ループの再有効化**＝`clipkeep-daily-growth-loop` が **`enabled: false`**（**11 スロット連続 未発火・daily log 空白 17 日**）。**(b) `npx wrangler login`**（または `CLOUDFLARE_API_TOKEN`）→ 直後に `npm run deploy:prod && npm run check:release:prod` で **`3bd3cb6`（6→9 PF）を出荷して閉じる**。**(c) 優先9 用の KV namespace 作成**（同じ Cloudflare 作業なので往復を1回に） | 運用/blocker | — | (a) `enabled: true` かつ翌営業日に daily log 生成／(b) `wrangler whoami` 認証済・platform-status が 6→**9**／(c) namespace 作成済み |
| **1** | **【自律可・デプロイ不要】計測: `growth:review` に (i) 非ブランド impressions (ii) page 次元 impressions（locale 別・position 別） (iii) ★`session_start` と `page_view` の乖離 を追加**（§7 提案1。CSV は既に毎回取得済み＝**新規 API 呼び出しゼロ**。`scripts/growth-summary.mjs` の集計追加のみ）。**#008 から 3 週連続で優先1 に置きながら日次ループ停止で未着手** | 計測 | **不要** | 出力とスナップショット JSON に非ブランド impressions・page 次元ロケール別内訳・session/page_view 乖離が出る |
| **2** | **柱1/修復: fxTwitter 401 の明示分類とログ化**（`src/lib/extract/twitter.ts:186-215` が 401 をどの枝でも分類せず黙って落ちる＝本番の**約37日間**の異常が extractor ログに現れない根因。**実装・テスト・push はデプロイ非依存で先行できる**） | 柱1/修復 | 検証のみ要 | 401 が失敗分類に現れる・vitest green／（復旧後）status の twitter が operational へ |
| **3** | **【自律可・デプロイ不要】健全性 HC-9: 認証/レート制限レイヤーのユニットテスト**（`src/lib/rate-limit/extract.ts` の `getClientKey`／窓・上限判定＝ほぼ純関数。**"fail-closed であること"を契約として固定**。08-15 health-check が「前半だけ切り出せば規模:小」と再確認済） | 健全性/品質 | **不要** | rate-limit の純関数にテストが入り green（production 非改変） |
| **4** | **柱1: pt ロケール既存ページの内容深化**（データゲートは #009 で通過済＋本週 **3 週連続再現**＝pt は 6/6 が pos 2〜7・平均4.2、ar は 3 週連続 0。対象候補: `/pt`(2) / `/pt/blog/safest-video-downloader-sites`(2) / `/pt/latest`(4) / `/pt/blog/twitter-video-download-mp4`(7)。**新規 URL は増やさない**）。★**本週の変更: 実装・push はデプロイ非依存で先行できる**ことを明記（本番反映のみ優先0(b) 待ち） | 柱1/内容 | 本番反映のみ要 | pt ページの impressions/position が 2 週連続で改善 |
| **5** | **【自律可・デプロイ不要】健全性 HC-7: `knip.json` の `ignoreBinaries: ["powershell"]` 削除**（設定の陳腐化除去・極小・production 非改変）。★**#009 の優先10 から繰り上げ**＝デプロイ断絶が 5 週・日次ループ停止が 3 週に及ぶため、**デプロイ不要枠を上位に集める**運用判断。※`powershell` は `check:*` で実使用中＝「常に Windows で実行する」前提の明示とセットで実施 | 健全性/品質 | **不要** | knip PASS・`npm run check:*` 従来どおり動作 |
| **6** | **柱2: 凍結の可否をユーザーに確認する（§7 提案3）**。★**本週 判断材料が増えた**＝凍結すれば `@cloudflare/puppeteer` の **DIRECT high（HC-11）が構造的に消える**（版上げでは直らないことが 08-15 に確定）。(a) 有料 Browser Rendering＝危害ゲート① / **(b) 凍結（推奨・費用対効果＋セキュリティの二重の理由）**。**3週間 未回答** | 柱2/判断 | — | ユーザーの一言を得て task.md に明記し、柱2 起因タスクと puppeteer 依存の扱いを確定 |
| **7** | **【自律可・デプロイ不要】健全性 HC-8 残: `extractTwitter`／`extractTikTok` の I/O モック付きテスト**（失敗分類の網羅。優先2 と同じレイヤーなので連続実施が自然） | 健全性/品質 | **不要** | extract 層の失敗分類にテストが入り green |
| **8** | **健全性 HC-6: `next` 15.2.9 → `next@15.5.23`（security・緊急度 高）**（middleware redirect SSRF / rewrites の request smuggling / App Router bypass / cache poisoning / CSP nonce XSS が構成に該当。★08-15 実測で修正先が 15.5.22→**15.5.23** に前進し、新規検出の `nanoid` high 2件も同梱解消される見込み。`package.json` の版指定変更を伴う＝**要ユーザー承認**・要本番フル検証） | 健全性/security | 要 | `next@15.5.23` で typecheck/lint/build/vitest PASS・本番フル検証 PASS |
| **9** | **柱4' 測定: badge 外部 Referer の KV 集計＝被リンク代理指標**（**要ユーザー承認: KV namespace 作成**。主軸 A+B の唯一の計器＝**5週間 不在**）。★**#009 の優先5 から降格**＝重要度の変更ではなく、**自律不可の項目を上位に置いても消費されない**ため（#008 で同じ判断を行った前例に倣う）。優先0(c) と同時依頼 | 柱4'/測定 | 要 | 外部 Referer ホスト数を週次で取得できる・本番200 |
| **10** | **柱1' Track A: A-v2 = `extractor_jobs` の per-PF 実成功率を status 資産へ併記**（合成プローブ＝上流到達性 と 実ジョブ成功率 は別物。二軸併記は被引用性を上げる。優先2 の結果とも接続） | 柱1'/資産 | 要 | status page/JSON に per-PF 実成功率が出る・本番200 |
| **11** | 柱1: downloader help リンク（sns/telegram/tiktok/twitter 4本）＋ extractor-form/result-client の status 連動 help リンクの `?locale=`→path 形式化（06-29 積み残し） | 柱1/発見 | 要 | 残る solution help リンクの path 形式化・本番200 |
| **12** | **柱4' B-2（ユーザー一度きり・任意）: AlternativeTo 登録**（**5週連続で未確認**。`clipkeep` クエリの position 7〜15.7・clicks 0 は本週も不変＝ブランド SERP 未確立。文面は `docs/ops/outreach/2026-08-02.md`／差分 `2026-08-16.md`／authority-plan.md B-2） | 柱4'/権威 | — | 登録1回・`clipkeep` クエリ position が 1 に近づく |

> **#010 で落としたもの・変えたもの（明示）**:
> - **HC-7 を 10 → 5 に繰り上げ**。重要度ではなく**デプロイ不要枠を上位へ集める運用判断**（#009 で 12→10 に繰り上げた延長。日次ループ停止が 3 週に及ぶため）。
> - **柱4' KV（badge Referer）を 5 → 9 に降格**。**自律不可（KV namespace 作成が要る）項目を上位に置いても消費されない**ため。**重要度の評価は不変＝主軸 A+B の唯一の計器**。#008 で同じ理由の入れ替えを行った前例に倣う。
> - **優先1 に (iii) `session_start`／`page_view` 乖離の記録を追加**（§1.1 の発見を毎週自動で可視化するため。同一スクリプト内で追加コストほぼゼロ）。
> - **優先4（pt 深化）に「実装・push はデプロイ非依存で先行可」を明記**。デプロイ断絶が 5 週に及ぶ現状で「要デプロイ」とだけ書くと着手不能に見えるため。
> - **優先6（柱2 判断）に HC-11 を結合**。凍結が脆弱性解消と同義になったため、判断の価値が上がった。
> - **HC-10（レート制限 env parse の NaN ガード欠落）は今週もバックログに積んでいない**。理由不変＝セキュリティ性質を変える修正であり本番検証なしに出荷すべきでない。実害は現時点でなし（3つの wrangler toml すべて正常値）。
> - HC-5（yaml moderate）は**ユーザーの承認マーカーが 6 週連続で未付与**のため据え置き。

---

## 9. 柱4' outreach（ホワイトハット限定）

- 本週分: **`docs/ops/outreach/2026-08-16.md`（差分のみ）**。#009 で切り替えた「変更のある週だけ更新する」運用を継続（AlternativeTo 未実施・KV 未承認がいずれも変わらないため、フル下書きは再生成しない）。
- **文面の正本**: `docs/ops/outreach/2026-08-02.md` ／ **戦略の正本**: `docs/strategy/authority-plan.md`。
- **投稿・登録はユーザー手動・自動投稿は禁止**。§6 ガードレール（スパム的大量登録・PBN・有料リンク）不変。有料PRは危害ゲート①＝要承認。
- 本週の差分: ブランドクエリ impressions 8→7・国の顔ぶれが入れ替わり（idn/phl が消え can が出現）＝**ブランド想起は継続的な関心ではなく単発の検索が数件**という読みを追加。referral 0 は 2 週連続。

---

## 10. 参照

- `docs/strategy/growth-strategy.md`（正本・KPI 履歴に本日分=実測行を追記）
- `docs/strategy/linkable-asset-plan.md`（柱1' Track A 設計）／`docs/strategy/authority-plan.md`（柱4' Track B・outreach 正本）
- 前回: `docs/ops/weekly_review_2026-08-09.md`（#009・7日前）
- 中間記録: `docs/ops/health/2026-08-15.md`（週次健全性・**status: green**・HC-11 新規）／**daily log は 2026-08 に 1 件も無し**（最後は `docs/ops/daily/2026-07-30.md`・§4.1）
- スナップショット: `docs/analytics/history/growth-2026-08-15T20-42-04-620Z.json`（2026-08-16 as-of・実測）
- 生 CSV（gitignore 済・ローカル成果物）: `docs/analytics/latest-gsc-query-pages.csv`（§2.1 の根拠＝全5行がブランドクエリ）／`latest-gsc-pages.csv`（§2.2）／`latest-ga4-events.csv`・`latest-ga4-acquisition.csv`（§1.1）
- 認証: SA（`D:\secrets\clipkeep\`・リポジトリ外）／`docs/ops/analytics-auth-recovery.md` §4.2
- スケジューラ実測: §0 の表を参照（#009 提案5 で定例化）
