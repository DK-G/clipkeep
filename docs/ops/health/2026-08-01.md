# 健全性チェック 2026-08-01（週次・守りのループ）

status: **green**（層B 自動修正なし／層B+ 消化対象なし／デプロイなし）

前回: [`2026-07-25.md`](2026-07-25.md)。

- 対象: `D:\dev\repos\clipkeep` / `main` / `git pull` 済（Already up to date・HEAD `19f3147`）
- 作業ツリー: クリーン（開始時・終了時とも）

---

## 層A: 計測・発見

| 項目 | 結果 | 前回比 |
|---|---|---|
| `npm run typecheck`（tsc --noEmit） | **PASS**（0 エラー） | 同 |
| `npm run lint`（next lint / eslint） | **PASS**（No ESLint warnings or errors） | 同 |
| `npx knip` | 未使用ファイル **1**（`scripts/growth-summary.mjs` ＝ **既知の偽陽性**・spawn 起動・削除禁止）／未使用 export **0**／未使用依存 **0** | 同 |
| `npx madge --circular` | **循環依存 0**（415 ファイル処理） | 同（412→415） |
| `npm test`（vitest） | **73 passed / 7 files** | **44→73**（HC-8 で +29） |
| リポジトリ衛生 | 良好。`.next`/`.open-next`/`.wrangler`/`.secrets`/`.env*`/`node_modules`/`*.tsbuildinfo` すべて gitignore 済・誤コミットなし（tracked は `.env.example` のみ）。tracked pack **1.75 MiB**（最大 `package-lock.json` 546KB） | 同 |

knip の Configuration hint 1件（`knip.json` の `ignoreBinaries: ["powershell"]`）は**前回と同一・未解消**
＝設定値のため層B対象外 → 層C（HC-7）に据え置き。

### クリティカルパスのテスト有無（手順5）

- 北極星 `ad_script_load`: **テストあり** — `src/lib/analytics/ad-config.test.ts`
- `middleware.ts`: **テストあり** — `middleware.test.ts`（ロケール rewrite / canonical host 301）
- `src/lib/extract/`: **テストあり（今週改善）** — `twitter-url` / `tiktok-url` / `freshness` の3本が
  07-30 日次ループで追加（HC-8 完了）。前回の指摘は解消済み。
- **★新規の欠落を検出（手順5 が指す「認証/レート制限」の実体）**: `middleware.ts` は **認証もレート制限も
  行っていない**（grep で該当語 0件・72行の locale rewrite + canonical 301 のみ）。実体は
  **`src/lib/rate-limit/extract.ts`（レート制限）と `src/lib/security/turnstile.ts`（Turnstile 検証）**で、
  **どちらもテスト0本**。→ 層C（**HC-9**）に追加。
- **★上記レイヤーの読解中に潜在バグを発見**（コード読解のみ・未修正）: `checkExtractRateLimit` が
  `parseInt(process.env.RATE_LIMIT_LIMIT ?? "30", 10)` を **NaN ガードなし**で使う。空文字や非数値が
  入ると `limit`/`windowMs` が `NaN` → `length >= NaN` が常に false ＝ **レート制限が fail-open で無効化**される。
  現行 3つの wrangler toml はいずれも `"30"` / `"60000"` と正常値のため**現時点で実害なし＝潜在**。
  挙動変更を伴う修正のため層Bでは触らず → 層C（**HC-10**）。

---

## 層B: 低リスク自動修正

**変更なし（該当0件）。**

- lint は元から 0 warning ＝ `--fix` の適用対象なし。
- knip の未使用 export / 未使用依存はいずれも **0**。唯一の未使用ファイルは削除禁止の既知偽陽性。
- よって **コミット・デプロイともに実施していない**（手順8「変更が無ければ deploy はしない」）。
- D1 schema / Cloudflare bindings には一切触れていない。

---

## 層B+: 承認済み健全性バックログの消化

**消化対象なし（3週連続）。** 理由: `task.md` の健全性バックログに **承認マーカー
`（承認済み・消化可）` が付いた項目が1件も存在しない**（grep 一致は「ルール本文」と
「未付与である旨の注記」の2行のみ＝前回と同じ）。未承認項目には手を付けない。

最も近い候補は HC-5 の `yaml` 分（非 `--force` の `npm audit fix` で semver 内修正可・規模:小・
bindings 非依存）だが、**承認マーカー未付与**のため今週も据え置き。

なお仮に消化対象があっても、**今週もデプロイ自体が不可能**だった（下記）。

### ★デプロイ能力のブロッカー（3週連続・悪化）

`wrangler whoami` → **`Not logged in.`**（`wrangler 4.34.0`。実行は `node_modules/wrangler/bin/wrangler.js` 直起動）。

- 最後の出荷は **2026-07-14**＝**18日間デプロイ断絶**。
- 実害は本番で可視: `/api/v1/platform-status` の `platforms` が **6 のまま**（`3bd3cb6` の 6→9 PF が未反映）、
  **twitter uptime 0% が約22日継続**。
- 解消はユーザーが1回実行するのみ（task.md ★ブロッカー①と同一）:

```bash
npx wrangler login
```

---

## 本番サニティ確認（デプロイなしだが「壊れた本番を放置しない」ため実施）

| 対象 | 結果 |
|---|---|
| `https://clipkeep.net/api/v1/health` | **200**（len 289） |
| `https://clipkeep.net/sitemap.xml` | **200**（`<loc>` **509**・前週と同数） |
| `https://clipkeep.net/api/v1/platform-status` | **200**（platforms **6** / alsoSupported **5** ＝未反映のまま） |

### per-PF uptime

| PF | status | uptime | 前回比 |
|---|---|---:|---|
| **twitter** | **limited** | **0%** | 変化なし（約15日→**約22日**継続） |
| telegram | operational | 100% | 95→100 改善 |
| reddit | operational | 100% | 同 |
| threads | operational | 100% | 同 |
| bluesky | operational | 93% | 同 |
| tiktok | operational | 100% | 同 |

- twitter の恒常劣化は**改善も悪化もしていない**。是正（task.md 優先1）は日次ループの担当で、
  本ループ（守り）では修正しない。ただし**デプロイ断絶が続く限り是正も出荷できない**点は変わらない。

---

## 依存・脆弱性サマリー（この場では更新しない＝層C）

### `npm outdated`: **18 パッケージ**（前回 18・件数横ばい。latest はさらに前進）

- major 乖離: `next` 15.2.9→**16.2.12** / `eslint-config-next` 15.2.9→**16.2.12** / `eslint` 9.19→**10.8.0** /
  `typescript` 5.7.2→**7.0.2** / `wrangler` 4.34→**4.86.0**（wrangler 実行時通知では既に **4.118.0**・前回 4.114.0）/
  `@types/node` 22.10→**26.1.2** / `@cloudflare/workers-types` 4.20260313→**5.20260801.1**
- minor/patch: `@opennextjs/cloudflare` 1.17.1→1.20.2 / `@cloudflare/puppeteer` 1.0.6→**1.2.0**（前回 1.1.0）/
  `tailwindcss`・`@tailwindcss/postcss` 4.2.1→4.3.3 / `react`・`react-dom` 19.0→19.2.8 /
  `@types/react` 19.0.2→**19.2.18** / `autoprefixer` 10.4.27→10.5.4 / `postcss` 8.5.15→**8.5.25** / `vitest` 4.1.9→4.1.10
- → **HC-4**（規模:大・要承認・自動更新禁止）

### `npm audit`: **24 件（high 17 / moderate 4 / low 3）** ← 前回と**同数・同内訳**

件数は横ばいだが、**`next` の advisory 内訳が前回の整理を無効化する方向に広がっている**（下記）。

- **★HC-6 の再評価が必要（今週の最重要発見）。**
  前回は `next` の high を「Image Optimization API の Cache Key Confusion / Content Injection の2件」と
  記録し、本番 `/_next/image` が 404 であることを根拠に**実露出は低い**と暫定評価した。
  今回 advisory 全文を確認したところ、影響範囲は `next 9.3.4-canary.0 - 16.3.0-preview.7` で
  **画像最適化以外の経路が多数含まれる**。ClipKeep の構成に**直接該当し得る**ものだけでも:
  - **Improper Middleware Redirect Handling Leads to SSRF**（GHSA-4342-x723-ch2f）
  - **HTTP request smuggling in rewrites**（GHSA-ggv3-7p47-pfv8）／
    **SSRF in rewrites via attacker-controlled destination hostname**（GHSA-p9j2-gv94-2wf4）
  - **Middleware / Proxy bypass in App Router via segment-prefetch routes**（GHSA-267c-6grr-h53f・
    その incomplete-fix follow-up GHSA-26hh-7cqf-hhc6）
  - **Middleware / Proxy redirects can be cache-poisoned**（GHSA-3g8h-86w9-wvmq）／
    **cache poisoning in RSC responses**（GHSA-wfc6-r584-vfw7 ほか）
  - **XSS in App Router with CSP nonces**（GHSA-ffhc-5mcf-pf4q）
  - **Unauthenticated disclosure of internal Server Function endpoints**（GHSA-955p-x3mx-jcvp）
  ClipKeep は **App Router ＋ `middleware.ts` による locale rewrite ＋ canonical host 301** を使う構成であり、
  **「`/_next/image` が 404 だから低露出」という前回の根拠は middleware/rewrites 系 advisory を全くカバーしない**。
  → **HC-6 の緊急度を「中」→「高（ただし規模は中のまま・要承認）」へ引き上げ**て層Cに記録。
  - 修正先は **`next@15.5.22`**（前回記録の 15.5.21 から前進）。実体は semver-minor（15.2.9→15.5.22）だが、
    `package.json` が **`"next": "15.2.9"` と完全固定**のため npm は `--force`（stated range 外）と表示する。
    ＝ **`package.json` の記述変更を伴う**ので、いずれにせよ層B/層B+ の対象外（要承認の専用タスク）。
  - `postcss` の high 3件（`</style>` XSS・sourceMappingURL 経由の任意ファイル読み取り・path traversal）も
    同じ `next@15.5.22` に紐づく。
- build-tool 系 transitive（本番バンドル外・従来分）: `wrangler`→`miniflare`→`sharp`/`ws` /
  `undici` / `js-yaml` / `flatted` / `picomatch` / `brace-expansion` / `basic-ftp` / `form-data` /
  `path-to-regexp` / `defu` / `fast-xml-parser`・`fast-xml-builder` / `ip-address` / `qs` / `body-parser` /
  `@eslint/plugin-kit`（`sharp`/`ws` は wrangler@4.86 major＝`--force` 要で HC-4 合流）
- `yaml` 2.0–2.8.2（moderate）＝ 唯一「非 `--force`・規模:小・bindings 非依存」に切り出せる層B+ 候補
  （**承認マーカー未付与のため今週も未消化**）
- → **HC-5**（数値更新）／**HC-6**（評価を上方修正）

---

## 層Cに積んだ提案（実装しない）

| ID | 内容 | 規模 |
|---|---|---|
| HC-4（更新） | 依存更新の段階バッチ（数値を 2026-08-01 実測へ更新） | 大 |
| HC-5（更新） | npm audit の切り分け（24件で横ばい・yaml のみ層B+ 候補のまま） | 中 |
| HC-6（**上方修正**） | `next` 15.2.9 → **15.5.22**。advisory が middleware/rewrites/App Router 系に及ぶことが判明し、前回の「低露出」評価は不成立 | 中（緊急度は高） |
| HC-7（据え置き） | `knip.json` の `ignoreBinaries: ["powershell"]` 削除 | 極小 |
| **HC-9（新規）** | **認証/レート制限レイヤー（`src/lib/rate-limit/extract.ts`・`src/lib/security/turnstile.ts`）が無テスト** | 小〜中 |
| **HC-10（新規）** | **`checkExtractRateLimit` の env `parseInt` に NaN ガードなし＝不正値でレート制限が fail-open**（現行 toml は正常値のため潜在） | 小 |

HC-8（`src/lib/extract/` のユニットテスト）は 07-30 日次ループで**完了**（vitest 44→73）。

---

## 所見

- **コードベース自体は今週も緑**（typecheck / lint / knip / madge / vitest 73 全通過・循環0・衛生良好）。
  守りのループとして 1 行も本番を触っていないのが正しい状態。
- **改善が1つ**: 前回の最大の指摘（中核抽出レイヤーが完全無テスト）は HC-8 の消化で解消し、
  テストは **44→73** に増えた。回帰検知の穴が実際に塞がった週。
- **悪化ではないが評価が変わったのが1つ**: `npm audit` は件数・内訳とも**横ばい（24 / high 17）**だが、
  `next` advisory の全文確認により **前回の「実露出は低い」根拠（`/_next/image` 404）が
  middleware / rewrites / App Router 系を全くカバーしていない**ことが判明した。
  件数を見ているだけでは劣化に見えないが、**中身は前回より悪い**。HC-6 の緊急度を上げた。
  ※ただし本ループでは適用しない（規模:中＝層B+ 対象外・`package.json` の版指定変更を伴う）。
- **新しい穴を2つ検出**: 手順5 が指す「認証/レート制限」の実体は `middleware.ts` ではなく
  `src/lib/rate-limit/` と `src/lib/security/` で、**両方とも無テスト**（HC-9）。
  加えてレート制限に **fail-open の潜在バグ**（HC-10）。どちらも挙動変更を伴うため層Cへ。
- **構造的な最大リスクは3週連続で同じ**: `wrangler` 未ログインで**18日間デプロイ不能**。
  守りの観点では「緑であること」より「**直せる状態にあること**」が重要で、今それが失われている。
  HC-6 が仮に緊急化しても**出荷できない**のがこの状態の本質的な危険。解消コストは `npx wrangler login` 1回。
- D1 schema / Cloudflare bindings 変更なし・新規 URL なし・戦略文書不変更。
</content>
