# 健全性チェック 2026-07-25（週次・守りのループ）

status: **green**（層B 自動修正なし／層B+ 消化対象なし／デプロイなし）

前回: [`2026-07-11.md`](2026-07-11.md)。**07-18 の回は未実施**（7/15〜7/23 のスケジュールタスク登録消失により
未発火。本リポジトリ起因ではない・7/23 再登録済）。よって本回は 2週分の間隔を挟んだ復帰初回。

- 対象: `D:\dev\repos\clipkeep` / `main` / `git pull` 済（Already up to date）
- 作業ツリー: クリーン（開始時・終了時とも）

---

## 層A: 計測・発見

| 項目 | 結果 |
|---|---|
| `npm run typecheck`（tsc --noEmit） | **PASS**（0 エラー） |
| `npm run lint`（next lint / eslint） | **PASS**（No ESLint warnings or errors） |
| `npx knip` | 未使用ファイル **1**（`scripts/growth-summary.mjs` ＝ **既知の偽陽性**・spawn 起動・削除禁止）／未使用 export **0**／未使用依存 **0** |
| `npx madge --circular` | **循環依存 0**（412 ファイル処理） |
| `npm test`（vitest） | **44 passed / 4 files**（middleware・ad-config・platform-status export/probes） |
| リポジトリ衛生 | 良好。`.next`/`.open-next`/`.wrangler`/`.secrets`/`.env`/`.env.local`/`node_modules`/`tsconfig.tsbuildinfo` すべて gitignore 済・誤コミットなし。tracked pack **1.75 MiB**（最大は `package-lock.json` 548KB） |

knip の Configuration hint 1件（`knip.json` の `ignoreBinaries: ["powershell"]` が不要）は
**設定値のため層B対象外** → 層C（HC-7）に積んだ。※`powershell` は `package.json` の
`check:release:*` 系 4 スクリプトで実際に使用中のため、削除は「Windows 前提で常に解決できる」
確認が要る（Linux CI で回すと再発する種類の指摘）。

### クリティカルパスのテスト有無（手順5）

- 北極星 `ad_script_load`: **テストあり** — `src/lib/analytics/ad-config.test.ts`（HC-3 で導入済）
- `middleware.ts`（ロケール rewrite / canonical host 301）: **テストあり** — `middleware.test.ts`
- → 手順5 が指定する2箇所に欠落はない。
- **ただし新規の欠落を検出**: `src/lib/extract/` 配下 **17 モジュール**（`twitter.ts`・`tiktok.ts`・
  `twitter-url.ts` 等＝**プロダクトの中核抽出経路**）に **テストが1本も無い**。現在の最優先課題
  （fxTwitter 401・本番 X 抽出の恒常劣化）が起きている当のレイヤーが無テスト。→ 層C（HC-8）に追加。

---

## 層B: 低リスク自動修正

**変更なし（該当0件）。**

- lint は元から 0 warning ＝ `--fix` の適用対象なし。
- knip の未使用 export / 未使用依存はいずれも **0**。唯一の未使用ファイルは削除禁止の既知偽陽性。
- よって **コミット・デプロイともに実施していない**（手順8 の「変更が無ければ deploy はしない」に従う）。
- D1 schema / Cloudflare bindings には一切触れていない。

---

## 層B+: 承認済み健全性バックログの消化

**消化対象なし。** 理由: `task.md` の健全性バックログに **承認マーカー `（承認済み・消化可）` が付いた項目が
1件も存在しない**（grep 一致は「ルール本文」と「未付与である旨の注記」のみ）。未承認項目には手を付けない。

候補として最も近いのは戦略バックログ #9 ＝ HC-5 の `yaml` 分（非 `--force` の `npm audit fix`）だが、
**承認マーカー未付与**のため今週も据え置き。

なお仮に消化対象があっても、**今週はデプロイ自体が不可能**だった（下記）。

### ★デプロイ能力のブロッカー（前週から継続）

`npx wrangler whoami` → **`Not logged in.`**。7/14 以降の未発火期間で OAuth が失効したまま。
`npm run deploy:prod` 実行不可＝**層B/層B+ に変更が出ていたら `status: blocked` になっていた**。
解消はユーザーが `npx wrangler login` を1回実行するのみ（task.md ★ブロッカー①と同一）。

```bash
npx wrangler login
```

---

## 本番サニティ確認（デプロイなしだが「壊れた本番を放置しない」ため実施）

| 対象 | 結果 |
|---|---|
| `https://clipkeep.net/api/v1/health` | **200**（len 289） |
| `https://clipkeep.net/sitemap.xml` | **200**（`<loc>` **509**・前週と同数） |
| `https://clipkeep.net/api/v1/platform-status` | **200** |

※ bash サンドボックスの curl が使用不可（`curl: (43)`）のため Node の `fetch` で確認。

### per-PF uptime（07-24 の教訓に従い健全性チェックにも取り込み）

| PF | status | uptime |
|---|---|---:|
| **twitter** | **limited** | **0%** |
| telegram | operational | 95% |
| reddit | operational | 100% |
| threads | operational | 100% |
| bluesky | operational | 93% |
| tiktok | operational | 100% |

- **twitter は依然 uptime 0%**（07-24 発見時点で約14日 → 現在**約15日継続**）。改善なし。
  task.md 優先0（fxTwitter 401 の切り分け）は**未着手のまま悪化も改善もしていない**＝日次ループの最優先で正しい。
  ※本ループは守り（層C まで）のため**修正はしない**。抽出経路の是正は日次ループの担当。
- `platforms` は **6・`alsoSupported` 5** ＝ commit `3bd3cb6`（6→9 PF 拡大）は **main にあるが本番未反映**。
  上記 wrangler 未ログインによるデプロイ blocked の実害がここに可視化されている。

---

## 依存・脆弱性サマリー（この場では更新しない＝層C）

### `npm outdated`: **18 パッケージ**（前回 18・件数横ばい。ただし各 latest が前進）

- major 乖離: `next` 15.2.9→**16.2.11** / `eslint-config-next` 15→16 / `eslint` 9.19→**10.8.0** /
  `typescript` 5.7.2→**7.0.2**（前回 7.0 系初出→さらに前進） / `wrangler` 4.34→**4.86.0**（npx 実行時の
  通知では既に **4.114.0** が存在） / `@types/node` 22→26 / `@cloudflare/workers-types` 4→**5.20260724.1**
- minor/patch: `@opennextjs/cloudflare` 1.17.1→1.20.2 / `@cloudflare/puppeteer` 1.0.6→1.1.0 /
  `tailwindcss`・`@tailwindcss/postcss` 4.2.1→4.3.3 / `react`・`react-dom` 19.0→19.2.8 /
  `@types/react(-dom)` 19.0→19.2 / `autoprefixer` 10.4.27→10.5.4 / `postcss` 8.5.15→8.5.23 /
  `vitest` 4.1.9→4.1.10
- → **HC-4**（規模:大・要承認・自動更新禁止）

### `npm audit`: **24 件（high 17 / moderate 4 / low 3）** ← 前回 22 件（high 12 / moderate 8 / low 2）から**悪化**

2週間で +2 件、かつ **high が 12→17 に増加**（新規 advisory と再分類の混在）。内訳の要点:

- **★方針転換が必要な新事実: 出荷ランタイム依存に high が出た。**
  前回まで「全て build-tool 系 transitive で本番ランタイム露出は低い」と整理していたが、今回は
  **`next` 本体が DIRECT high**（Image Optimization API の Cache Key Confusion / Content Injection）。
  修正は **`next@15.5.21`＝semver-minor（major 不要）**。`postcss` の high 2件
  （`</style>` 未エスケープ XSS・sourceMappingURL 経由の任意ファイル読み取り）も npm の解決経路上
  同じ `next@15.5.21` に紐づく。→ 新規 **HC-6** として分離。
  - **実露出の暫定評価（要再確認）**: `next/image` はコンポーネント4箇所で使用しているが、
    本番の `https://clipkeep.net/_next/image?...` は **404**（OpenNext/Cloudflare で当該最適化
    エンドポイントが提供されていない）＝ Image Optimization 系 advisory の実露出は**低い可能性が高い**。
    緊急度は下がるが「出荷依存に high が乗った」事実自体は据え置けないため層Cで正式に積む。
- build-tool 系 transitive（従来分・本番バンドル外）: `wrangler`(OS Command Injection) /
  `miniflare`→`ws`(未初期化メモリ開示・fragment DoS) / `undici` / `sharp` / `js-yaml` / `flatted` /
  `picomatch` / `brace-expansion` / `basic-ftp` / `form-data` / `path-to-regexp` / `defu` /
  `fast-xml-parser`・`fast-xml-builder` / `ip-address` / `qs` / `body-parser` / `@eslint/plugin-kit`
- `yaml` 2.0–2.8.2（moderate・Stack Overflow）＝ 非 `--force` の `npm audit fix` で semver 内修正可
  ＝ 唯一「規模:小・bindings 非依存」に切り出せる層B+ 候補（**承認マーカー未付与のため今週も未消化**）
- → **HC-5**（更新）／**HC-6**（新規）

---

## 層Cに積んだ提案（実装しない）

| ID | 内容 | 規模 |
|---|---|---|
| HC-4（更新） | 依存更新の段階バッチ（数値を 2026-07-25 実測へ更新） | 大 |
| HC-5（更新） | npm audit の切り分け（24件へ更新・「全て build-tool」という整理を HC-6 で訂正） | 中 |
| **HC-6（新規）** | **`next` 15.2.9 → 15.5.21（semver-minor・security）**。出荷ランタイム依存の high 解消 | 中 |
| **HC-7（新規）** | `knip.json` の `ignoreBinaries: ["powershell"]` 削除（knip の hint） | 極小 |
| **HC-8（新規）** | `src/lib/extract/` の純関数（URL パーサ等）にユニットテスト導入 | 小〜中 |

---

## 所見

- **コードベース自体は今週も緑**（typecheck / lint / knip / madge / vitest 44 全通過・衛生良好・循環0）。
  守りのループとして「変更を入れない」のが正しい健全状態であり、今週も1行も本番を触っていない。
- **一方で守りの指標は初めて悪化方向に動いた**。理由は2つとも**コード外**:
  1. `npm audit` が 22→24（high 12→17）で、**初めて出荷ランタイム依存（`next`）に high が乗った**。
     従来の「全部ビルド工具だから低露出」という整理はもう成立しない（HC-6 で訂正・分離）。
     ただし本番 `/_next/image` が 404 ＝ 実露出は低い見込みで、緊急デプロイは不要と判断した。
  2. **デプロイ経路が2週間 blocked**（wrangler 未ログイン）。結果として main にある `3bd3cb6`
     （platform-status 6→9 PF）が本番未反映のまま、かつ **twitter uptime 0% が約15日継続**している。
     守りの観点では「修正を出せない状態が続いていること」自体が最大のリスクで、
     解消コストはユーザーの `npx wrangler login` 1回のみ。
- **テストの穴を1つ発見**: 手順5が指定する2箇所（ad_script_load / middleware）は充足しているが、
  **中核の抽出レイヤー `src/lib/extract/`（17モジュール）が完全無テスト**。今まさに障害が出ている
  レイヤーであり、回帰検知が効かない。純関数（`twitter-url.ts` / `tiktok-url.ts` 等）から
  ネットワーク非依存で足せる＝ HC-8。
- D1 schema / Cloudflare bindings 変更なし・新規 URL なし・戦略文書不変更。
