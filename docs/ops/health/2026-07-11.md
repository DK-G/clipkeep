# ClipKeep 健全性レポート 2026-07-11（週次 health-check・守りのループ）

status: **green**

週次 health-check（守り＝コードベースの健全性維持）を1回実行。作業ディレクトリ
D:\dev\repos\clipkeep（main）。作業ツリーはクリーン、`git pull origin main` 済み（Already up to date）。
今週は層Bの自動修正・層B+の消化ともに対象なし＝**コード変更・本番デプロイなし**（健全）。

## 層A: 計測・発見（フル実行）

| 項目 | 結果 |
|---|---|
| `npm run typecheck`（tsc --noEmit） | **PASS**（エラー0） |
| `npm run lint`（next lint / eslint） | **PASS**（警告0・エラー0） |
| `npx knip` | 未使用 export **0**。未使用ファイル1件＝`scripts/growth-summary.mjs` のみ（`run-growth-review.mjs` が `spawn` で文字列起動する**偽陽性**・削除禁止で不変）。ほか config hint 1件（後述） |
| `npx madge --circular` | 408 ファイル処理・**循環依存なし**（81 warnings は未解決/スキップ import で循環ではない） |
| `npm test`（vitest run） | **33/33 PASS**（Test Files 2 passed） |

### knip config hint（層B対象外・記録のみ）
- knip が `knip.json` の `ignoreBinaries: ["powershell"]` を「Remove from ignoreBinaries」と示唆。
  knip v6 が `powershell` を既知バイナリとして解決できるため ignore 不要になった、という**dev専用設定の些末な整理**。
- ただし層Bの許容3カテゴリ（未使用 export / 未使用ファイル / 未使用 devDependency の削除）に該当せず、
  価値も無視できるほど小さいため**今週は着手せず**。HC として積むにも軽微すぎるため記録のみ。

### クリティカルパスのテスト（step 5）
- 北極星イベント `ad_script_load` 周り: `src/lib/analytics/ad-config.test.ts` が存在・green
  （`AD_SCRIPTS`/`looksLikeBot`/`buildAdEventPayload`/`adZoneEventName` を検証）。
- 認証/レート制限/ロケール rewrite の `middleware`: リポジトリ直下 `middleware.test.ts` が存在・green
  （ja/pt/ar rewrite ＋ canonical host 301）。
- 両クリティカルパスともテスト有り＝**欠落なし**（HC-3 で導入済み・維持）。テスト追加タスクの新規計上なし。

### リポジトリ衛生（step 6）
- `.gitignore` は `.next` / `.open-next` / `.wrangler` / `.secrets` / `.env` / `.env.local` を全て ignore 済み
  （`git check-ignore` で確認）。
- `git ls-files` 上に `.next` / `.open-next` / `.wrangler` / `.secrets` 配下・`.env*`・SA鍵/OAuthトークンの
  誤コミット**なし**。ビルド生成物の混入なし。

## 層B: 低リスク自動修正
**なし。** lint 警告0・knip 未使用 export 0・dead code なし・削除可能な未使用依存なし
（knip 検出ファイルは偽陽性で削除禁止）。よって**コミット・デプロイなし**。

## 層B+: 承認済み健全性バックログの消化
**消化対象なし。** バックログの HC-4（依存 major 更新）・HC-5（npm audit 切り分け）は
いずれも承認マーカー `（承認済み・消化可）` が無く、規模も 大/中 で層B+ の4条件を満たさない。
task.md 全体を検索しても承認マーカーの付いた項目は存在しない（マーカー文字列はルール定義箇所のみ）。
勝手に承認扱いせず据え置き。

## 層C: 承認待ちキュー（積むだけ・実装せず）
新規の構造的問題は検出されず（コードは緑）。既存 HC-4 / HC-5 を今週の実測値で更新（task.md 参照）。

### 依存・脆弱性サマリー（この場では更新しない＝層C）
- `npm outdated`: **18 パッケージ**が最新から乖離（前回 17 → 今週 `@cloudflare/puppeteer` 1.0.6→1.1.0
  minor が新規計上）。うち **major** = `next` 15.2.9→16.2.10 / `eslint-config-next` 15→16 /
  `eslint` 9.19→10.7 / `typescript` 5.7→7.0 / `wrangler` 4.34→4.86 / `@types/node` 22→26 /
  `@cloudflare/workers-types` 4→5。minor/patch = `@opennextjs/cloudflare` 1.17→1.20・
  `@cloudflare/puppeteer` 1.0→1.1・`tailwindcss`/`@tailwindcss/postcss` 4.2→4.3・
  `react`/`react-dom` 19.0→19.2・`@types/react(-dom)` 19.0→19.2・`autoprefixer`/`postcss`/`vitest` 等。→ **HC-4**。
- `npm audit`: **22 件（high 12 / moderate 8 / low 2）**＝前回と同数・同内訳。全て build-tool 系 transitive:
  - `wrangler`（OS Command Injection, `pages deploy`）／`miniflare`→`ws`（high: 未初期化メモリ開示・
    tiny fragment DoS）＝ 修正は `wrangler@4.86` への **major（--force）** 要 ＝ HC-4 と合流。
  - `yaml` 2.0–2.8.2（moderate: 深いネストで Stack Overflow）＝ **非 --force の `npm audit fix`**
    で semver 範囲内修正が可能。出荷ランタイム外（build-tool）だが唯一 major を要さない分。
  - いずれも Cloudflare Workers 出荷バンドル外（ビルド/デプロイ工具）で本番ランタイム露出は低い。
    → **HC-5**（yaml 分は「規模:小」に切り出せる層B+候補だが承認マーカー未付与＝未消化）。

## 本番サニティ確認（デプロイなしだが「壊れた本番を放置しない」ため実施）
- デプロイは行っていない（層B/B+ 変更なし）が、守りのループとして本番を確認:
  `https://clipkeep.net/api/v1/health` **200**（len 289） /
  `https://clipkeep.net/sitemap.xml` **200**（len 358,027、前週 357,865 から微増＝platform-status 資産の反映）。
  本番は健全。※bash サンドボックスからは curl 接続不可のため PowerShell（Invoke-WebRequest）で確認。

## 所見
- コードベースは緑（typecheck/lint/knip/madge/tests 全通過・衛生良好）。今週の守りは
  「変更を入れないこと」が正しい健全状態。1回のデプロイもせず本番を触らないのが最良。
- 滞留債務は依存系のみ（HC-4/HC-5）で前回から実質横ばい（outdated が 17→18 に1件増）。放置の主因は
  「major を要する OpenNext/Cloudflare ビルド設定の敏感さ」で、専用の検証付きバッチ（要ユーザー承認）が必要。
  層B+ で安全に消化できる 小規模・bindings 非依存の切片は現状 yaml audit のみ（未承認のため据え置き）。
- D1 schema / Cloudflare bindings 変更なし・新規 URL なし・戦略文書不変更。
