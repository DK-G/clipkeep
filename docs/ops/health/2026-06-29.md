# ClipKeep 健全性レポート 2026-06-29（層B+ 単発消化）

status: **autofixed**（層B+ で承認済み HC-2 を1件消化・本番デプロイ）

これは土曜の定期 health-check ではなく、**層B+（承認済み小規模 HC の消化）の単発実行**。
ユーザー指示で HC-2 を即時消化した。作業ディレクトリ D:\dev\repos\clipkeep（main）。

## 消化した項目: HC-2 未使用 export 5件の棚卸し

承認マーカー `（承認済み・消化可）` 付き・規模:小・D1/bindings 非依存・ガードレール非抵触 ＝
層B+ の4条件を満たすため消化。knip 検出の未使用 export 5件を個別判断（**配線1・除去4**）。

### 配線（DRY 統合・挙動非変更）
- `SUPPORTED_LOCALES`（`src/lib/metadata-helper.ts`）→ `app/layout.tsx` の WebSite JSON-LD
  `inLanguage` にハードコードされていた同一10ロケール配列 `['en',...,'tr']` を置換。
  正本を単一化。本番で `inLanguage` が同値（順序も同一）であることを確認＝挙動非変更。

### 除去（in-app 呼び出しなし・配線先なし。git 履歴から復元可）
- `hasServiceAccount`・`STATUS_FILE_PATH`（`scripts/lib/analytics-auth.mjs`）:
  SA フォールバックは `buildAuthError` 内部で完結、status ファイルは `readAuthStatus`/
  `recordAuthStatus` が encapsulate。両 export は呼ばれていなかった。
  `SERVICE_ACCOUNT_PATH`/`STATUS_PATH`（内部 const）は他で使用継続のため温存。
- `markTopicRemoved`・`unmarkTopicRemoved`（`src/lib/trends/topic-store.ts`）:
  撤去リスト（`topics:removed`）への**書き込み**はサイト本体に存在せず、運用ツール
  `scripts/trend-remove-topic.mjs`（`npm run trend:topic`）が wrangler CLI 経由で直接更新する。
  サイト側は `listRemovedSlugs` の**読み取り**のみ担う（`live.ts` の `isSlugRemoved`）。
  重複実装だった2関数を除去し書き込み経路を CLI に一本化。`docs/ops/trend-topic-removal.md`
  の参照も更新。

## 検証
- `npm run typecheck` PASS / `npm run lint` PASS（警告0） / `npm test`（vitest）**33/33 PASS** / `npm run build` PASS。
- `npx knip`: 未使用 export **5 → 0**。残るのは `scripts/growth-summary.mjs`（ファイル）のみ＝
  `run-growth-review.mjs` が `spawn` で文字列起動する**偽陽性**（削除禁止）で不変。

## デプロイ（DoD フル実行）
- commit `3419398`（`chore(health):`）→ push origin main。
- `npm run deploy:prod` → **Version ID `0bc6a5a7-3d82-400a-89f6-4fdd58c1ed55`**。
- `npm run check:release:prod` **PASS=29 / FAIL=0 / SKIP=1**、D1 remote PASS（初回成功）。
- 本番ライブ検証（https://clipkeep.net）:
  - `/` の JSON-LD `inLanguage` = `["en","ar","ja","es","pt","fr","id","hi","de","tr"]`（配線の同値確認）。
  - `/api/v1/health` 200・`/sitemap.xml` 200（len 不変）。

## 所見
- 層B+ の初稼働（手動トリガ）。承認ゲート＋DoD フル検証で安全に負債1件を排出できることを実証。
- 滞留中の HC: HC-4（依存 major 更新・規模:大）/ HC-5（npm audit 切り分け・規模:中）は層B+ 対象外
  （未承認かつ非小規模）。消化するには規模:小へ分割し承認マーカーを付すか、daily で個別実施。
- D1/bindings 変更なし・新規 URL なし・sitemap 不変・戦略文書不変更。
