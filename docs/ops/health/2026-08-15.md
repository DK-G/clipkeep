# 健全性チェック 2026-08-15（週次・守りのループ）

status: **green**（層B 自動修正なし／層B+ 消化対象なし／デプロイなし）

前回: [`2026-08-08.md`](2026-08-08.md)（status: skipped＝未コミット docs で作業ツリーが汚れていたため）。
実質的な前回実行は [`2026-08-01.md`](2026-08-01.md)。

- 対象: `D:\dev\repos\clipkeep` / `main` / `git pull origin main` 済（Already up to date・HEAD `7786c15`）
- 作業ツリー: **クリーン**（開始時・終了時とも）＝前回 skip の原因（#009 レビューでコミット済）は解消

---

## 今週の要点（先に結論）

1. **コードは緑・4週連続で層B の修正対象ゼロ**（typecheck / lint / knip / madge / vitest 73 すべて PASS・循環0・衛生良好）。
2. **`npm audit` が 24 → 28 件（high 17 → 22）に悪化**。増分4件はすべて **puppeteer チェーン**で、
   **`@cloudflare/puppeteer` が初めて DIRECT high** になった（出荷コードが import している依存）。
3. ただし **★今回はバンドル実体を検証した**: 出荷 Worker バンドルに脆弱な transitive は**含まれていない**
   （下記 HC-11）。HC-6 で「`/_next/image` が404だから低露出」という**推論ベースの評価が後に不成立**になった
   反省を踏まえ、今回は grep による**実測**を根拠にした。
4. **`@cloudflare/puppeteer` には前進する修正版が存在しない**ことが判明（最新 1.3.0 も同一の脆弱版
   `@puppeteer/browsers@2.2.4` を固定・npm の提案する「修正」1.3.0→**0.0.11 はダウングレード**）。
   ＝ HC-4/HC-5 の「バージョンを上げれば解決」枠に**入らない**新種の負債 → **HC-11 として新規起票**。
5. **デプロイ経路の断絶は5週連続**（`npx wrangler whoami` = **Not logged in**）。本番自体は健全
   （health 200 / sitemap 200・loc **509**＝`3bd3cb6` 未反映のまま不変）。**これは健全性チェックでは解消できない
   （要・人間の `npx wrangler login`）。**

---

## 層A: 計測・発見

| 項目 | 結果 | 前回比（08-01） |
|---|---|---|
| `npm run typecheck`（tsc --noEmit） | **PASS**（0 エラー） | 同 |
| `npm run lint`（next lint / eslint） | **PASS**（No ESLint warnings or errors） | 同 |
| `npx knip` | 未使用ファイル **1**（`scripts/growth-summary.mjs` ＝ **既知の偽陽性**・spawn 起動・削除禁止）／未使用 export **0**／未使用依存 **0** | 同 |
| `npx madge --circular` | **循環依存 0**（415 ファイル処理・82 warnings） | 同 |
| `npm test`（vitest） | **73 passed / 7 files**（1.60s） | 同（73） |
| `npm outdated` | **18 件**（4週連続で同数） | 同数・latest は前進 |
| `npm audit` | **28 件（high 22 / moderate 3 / low 3）** | **24（17/4/3）から悪化（+4・high +5）** |
| リポジトリ衛生 | 良好。`.next`/`.open-next`/`.wrangler`/`.secrets`/`.env*`/`node_modules` すべて gitignore 済・**誤コミット 0**（tracked 610 ファイル） | 同 |

knip の Configuration hint 1件（`knip.json` の `ignoreBinaries: ["powershell"]`）は**5週連続で同一・未解消**
＝設定値のため層B対象外。層C **HC-7** に据え置き（#009 で戦略バックログ 優先10 へ繰り上げ済＝日次側で消費される想定）。

### クリティカルパスのテスト有無（手順5）

- 北極星 `ad_script_load`: **テストあり** — `src/lib/analytics/ad-config.test.ts`
- `middleware.ts`: **テストあり** — `middleware.test.ts`
- `src/lib/extract/`: **テストあり** — `twitter-url` / `tiktok-url` / `freshness`（HC-8 完了分）
- **`src/lib/rate-limit/extract.ts` / `src/lib/security/turnstile.ts`: 依然テスト0本**
  （`src/**/*.test.ts` 6本を grep して一致0件を確認）＝ **HC-9 未解消・2週目**。
- **HC-10（レート制限 env parse の NaN ガード欠落＝潜在 fail-open）も未修正**。
  3つの wrangler toml は正常値のままで**実害なし＝潜在**という評価も不変。
  セキュリティ性質を変える修正のため層B/層B+ では触らない（＝本番検証が可能になってから HC-9 とセットで扱う）。

### 依存・脆弱性の詳細

**`npm audit` 28 件の内訳（DIRECT のみ抜粋）**

| パッケージ | severity | direct | npm の fixAvailable | 扱い |
|---|---|---|---|---|
| `next` | high | ✔ | `next@15.5.23`（semver-minor 相当だが `package.json` が `15.2.9` 完全固定） | **HC-6**（緊急度: 高・要承認・規模: 中） |
| `postcss` | high | ✔ | 同上（`next@15.5.23` に紐づく） | HC-6 に同梱 |
| `wrangler` | high | ✔ | `wrangler@4.86.0` | HC-4/HC-5（build-tool 分） |
| `@cloudflare/puppeteer` | high | ✔ | `@cloudflare/puppeteer@0.0.11`＝**ダウングレード** | **HC-11（新規）** |

**増分4件（24→28）はすべて puppeteer チェーン**:
`@cloudflare/puppeteer` ← `@puppeteer/browsers`（<=2.13.2）← `extract-zip`
（**GHSA-jmr9-qjv8-65gv** unvalidated symlink path traversal）＋ `nanoid`（GHSA-28wg-ghj8-5hjv /
GHSA-2v37-7h3g-55p8）。うち **`nanoid` は `postcss` 経由＝ HC-6（`next@15.5.23`）で一緒に解消される**ため
新規負債ではない。実質の新規は **puppeteer 3件**。

**★露出評価（今回は推論でなく実測）**: `@cloudflare/puppeteer` は出荷コード
（`src/lib/auto-trend.ts:1`・`src/lib/extract/browser.ts:1`）が import しているため
「ビルド工具だから低露出」の整理は使えない。そこで**既存の OpenNext 出力（`.open-next/`・1,353 バンドルファイル）を
直接 grep** した:

| 文字列 | 出荷バンドル内のヒット |
|---|---|
| `cloudflare/puppeteer` | **2 ファイル** |
| `@puppeteer/browsers` | **0** |
| `extract-zip` | **0** |
| `yauzl` | **0** |
| `getInstalledBrowsers` | **0** |

＝**脆弱な transitive（ブラウザバイナリのダウンロード/解凍ヘルパー）は Worker バンドルに含まれていない**。
Cloudflare では Browser Rendering binding 経由のリモート起動でありローカル Chrome の取得経路を通らない、
という設計とも整合する。**よってランタイム露出は現時点で確認できない**。
ただし限界を明記する: ①この `.open-next/` は **2026-07-24 のビルド**（デプロイ断絶のため以降未更新）、
②minify 済みバンドルへの文字列 grep であり形式的証明ではない。**「露出なし」は暫定評価**とし、
デプロイ復旧後の再ビルドで再確認すること（HC-6 で同種の暫定評価が覆った前例がある）。

**`npm outdated` 18 件**（HC-4）: latest がさらに前進 —
`next`/`eslint-config-next` **16.3.1**（前回 16.2.12）・`@cloudflare/workers-types` **5.20260815.1**・
`eslint` **10.8.1**・`typescript` **7.0.2**・`@types/node` **26.2.0**・`@cloudflare/puppeteer` **1.3.0**（前回 1.2.0）・
`wrangler` は npx 通知で **4.123.0**（前回 4.118.0）。minor/patch: `@opennextjs/cloudflare` 1.17.1→1.20.2 /
`tailwindcss`・`@tailwindcss/postcss` 4.2.1→4.3.3 / `postcss` 8.5.15→8.5.26 / `autoprefixer` 10.4.27→10.5.4 /
`react`・`react-dom` 19.0.0→19.2.8 / `vitest` 4.1.9→4.1.10。**この場では一切更新していない**（層C）。

### デプロイ経路（参考・層外）

- `npx wrangler whoami` → **Not logged in**（**5週連続**）。`npm run deploy:prod` は実行不能。
- 本番は健全: `https://clipkeep.net/api/v1/health` **200**（`status: ok` / db ok / extractor ok / errorRatio 0）、
  `https://clipkeep.net/sitemap.xml` **200**（`<loc>` **509**）。
- sitemap の loc 数が 509 のまま不変＝ **`3bd3cb6` が本番未反映**の状態が継続していることの傍証。
- 本チェックでは**デプロイを伴う変更を一切行っていない**ため、この断絶は今週の作業を阻害していない。
  ただし HC-6（`next` security）を含む**要承認タスクはすべてデプロイ復旧待ちで滞留**している。

---

## 層B: 低リスク自動修正

**変更なし（該当0件）。4週連続。**

- lint は元から 0 warning ＝ `--fix` の適用対象なし。
- knip の未使用 export / 未使用依存はいずれも **0**。唯一の未使用ファイルは削除禁止の既知偽陽性。
- 唯一の機械的候補である knip Configuration hint（`ignoreBinaries`）は**設定値＝層B対象外**（HC-7 で管理）。
- よって **コミット・デプロイともに実施していない**（手順8「変更が無ければ deploy はしない」）。
- D1 schema / Cloudflare bindings には一切触れていない。

---

## 層B+: 承認済み健全性バックログの消化

**消化対象なし（4週連続）。**

理由: `task.md` の健全性バックログに **承認マーカー `（承認済み・消化可）` が付いた項目が1件も存在しない**
（grep 一致2行はいずれも「ルール本文」と「未付与である旨の注記」＝前回と同じ）。未承認項目には手を付けない。

参考: 現在マーカーさえ付けば消化しうる**「規模:小・bindings 非依存」候補**は次の2件。
判断はユーザーに委ねる（このループからは着手しない）。

- **HC-7**（`knip.json` の `ignoreBinaries` 削除・規模:極小・挙動非変更）
  ※ただし #009 で戦略バックログ 優先10 に繰り上げ済＝**日次ループ側で消費される想定**のため、
  層B+ で二重に取りに行くべきではない。
- **HC-9 の前半のみ**（`getClientKey` と in-memory バケットの窓/上限判定＝ほぼ純関数・テスト追加のみ・
  production 非改変）。`verifyTurnstileToken` 側はモックが要り「中」なので分離が前提。

**HC-5（`yaml` moderate）は6週連続で承認マーカー未付与のため据え置き**（据え置き自体は正しい運用）。

---

## 層C: 承認待ちキューに積んだもの

今週の新規は **HC-11 の1件**。既存 HC-4/5/6/7/9/10 は実測値を更新（`task.md` 参照）。

- **HC-11（新規）**: `@cloudflare/puppeteer` の high advisory に**前進する修正版が存在しない**。
  - 何を: `@cloudflare/puppeteer` → `@puppeteer/browsers@2.2.4` → `extract-zip`（GHSA-jmr9-qjv8-65gv）。
    **最新 1.3.0 も `@puppeteer/browsers@2.2.4` を固定**しており（`npm view` で両版の dependencies を照合済）、
    アップグレードでは解消しない。npm の `fixAvailable` は **1.0.6 → 0.0.11＝ダウングレード**で採用不可。
  - なぜ: 出荷コードが import する DIRECT high であり、HC-4「版を上げるバッチ」の枠組みで処理できない
    唯一の項目だから。放置方針を採るにしても**その旨を明示的に決める**必要がある。
  - 影響: 現時点で**ランタイム露出は確認されず**（上記バンドル grep）。実害は audit の件数のみ。
  - 選択肢: (a) **依存ごと削除** ← 柱2（trend スクリーンショット）は #007 で「有料化か**凍結**の二択・推奨は凍結」に
    到達しており、**凍結を確定すれば `auto-trend.ts` / `extract/browser.ts` の puppeteer 依存を落とせて
    脆弱性が構造的に消える**（＝最も筋が良い。ただし機能削除＝要ユーザー承認・規模:中）。
    (b) `overrides` で `@puppeteer/browsers` を >2.13.2 に強制（未検証・puppeteer 本体との整合リスク）。
    (c) 露出なしを根拠に**受容を明文化**し、audit の既知例外として記録（規模:小・コード変更なし）。
  - 規模: (a) 中 / (b) 中 / (c) 小。**要ユーザー判断**（どれも層B+ の自動消化対象外）。

---

## まとめ

| 層 | 実施 |
|---|---|
| 層A（計測） | フル実行。typecheck/lint/knip/madge/vitest/outdated/audit/衛生 すべて記録 |
| 層B（自動修正） | **なし**（該当0件・4週連続） |
| 層B+（承認済み消化） | **なし**（承認マーカー0件・4週連続） |
| 層C（積むだけ） | **HC-11 新規**＋既存6件の実測更新 |
| デプロイ | **なし**（変更がないため。なお経路自体は wrangler 未ログインで5週連続断絶） |

**status: green** — コード健全性は維持されている。悪化しているのは
**①依存の脆弱性（+4・puppeteer チェーン）と ②出荷経路の断絶（5週）**の2点で、
どちらも**層Cの承認待ち／人間操作待ち**であり、このループが自動で解消できる範囲にはない。
