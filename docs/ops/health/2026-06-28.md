# ClipKeep 週次健全性レポート 2026-06-28

status: **green**（層A 静的解析クリーン / 層B 自動修正なし / 層C 提案5件を task.md へ）

初回の health-check（手動実行）。作業ディレクトリ D:\dev\repos\clipkeep（main, HEAD `dab44d0`）。作業ツリーはクリーン。

## 層A: 計測・発見

| 項目 | 結果 |
|---|---|
| `npm run typecheck`（tsc --noEmit） | ✅ PASS |
| `npm run lint`（eslint） | ✅ PASS（警告0 / エラー0） |
| `npx madge --circular`（src, app, 403ファイル） | ✅ 循環依存なし |
| `npx knip` | ⚠ 未使用ファイル2・未使用export5（下記。1件は偽陽性） |
| `npm outdated` | ⚠ 17パッケージが更新可（major含む） |
| `npm audit` | ⚠ 22件（high 12 / moderate 8 / low 2） |
| テストファイル | ❌ 0件（クリティカルパス無防備） |

### knip 詳細
- 未使用ファイル:
  - `scripts/growth-summary.mjs` — **偽陽性**。`scripts/run-growth-review.mjs:77` が `spawn("scripts/growth-summary.mjs")` で文字列起動しており knip が追跡できないだけ。**削除禁止**。
  - `src/components/share-button.tsx` — 完成済みの ShareButton（10言語i18n＋analytics）だがどこからも未import。死蔵ではなく**未配線**の可能性が高く、自動削除せず層C（HC-1）へ。
- 未使用export（全て層C HC-2 へ。auth/i18n/trend の未配線APIの可能性）:
  - `hasServiceAccount`, `STATUS_FILE_PATH`（scripts/lib/analytics-auth.mjs）
  - `SUPPORTED_LOCALES`（src/lib/metadata-helper.ts、i18n＝除外対象）
  - `markTopicRemoved`, `unmarkTopicRemoved`（src/lib/trends/topic-store.ts）

### 依存・脆弱性
- outdated 注目: `next` 15.2.9→16.2.9、`react`/`react-dom` 19.0→19.2、`typescript` 5.7→6.0、`wrangler` 4.34→4.86、`@types/node` 22→26、`@opennextjs/cloudflare` 1.17→1.20。→ HC-4。
- audit high 12件の大半は build-tool 系 transitive（wrangler/miniflare/yaml/esbuild チェーン）と推定。runtime 出荷依存の切り分けが必要。→ HC-5。

### クリティカルパス
- 北極星イベント `ad_script_load`: `src/components/analytics/ad-diagnostics.tsx` に実装あり、テストなし。
- `middleware.ts`（認証・レート制限）: テストなし。
- → HC-3（テスト基盤の方針はユーザー判断）。

### repo衛生
- `.next` / `.open-next` / `.wrangler` / `.secrets` は gitignore 済み・誤コミットなし。作業ツリーはクリーン。

## 層B: 低リスク自動修正

**なし。** typecheck/lint/madge が全てクリーンで、knip 指摘も「偽陽性」または「完成済み未配線機能／外部参照され得るexport」のみ。安全に機械削除できる対象が無いと判断し、自動修正・デプロイは行わなかった（＝層Cへ送付）。本番デプロイなし。

## 層C: 承認待ちキュー（task.md「健全性バックログ」へ追記）

| ID | 内容 | 規模 | 要ユーザー判断 |
|---|---|---|---|
| HC-1 | share-button.tsx を統合 or 削除 | 小 | ✅ |
| HC-2 | 未使用 export 5件の棚卸し | 小 | — |
| HC-3 | クリティカルパスのテスト導入（現状0件） | 中 | ✅（テスト基盤方針） |
| HC-4 | 依存更新の段階バッチ（major含む） | 大 | — |
| HC-5 | npm audit 22件の切り分け・修正 | 中 | — |

## 所見

初回として健全。攻めの日次ループが「弥縫策」を溜めている兆候は静的解析レベルでは見られず（型・lint・循環依存すべてクリーン）。一方、**テストゼロ**と**依存負債の蓄積**が中期リスク。次の判断ポイントは HC-1（完成済み ShareButton を活かすか）と HC-3（収益パスのテスト基盤を持つか）。
